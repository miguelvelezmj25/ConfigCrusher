[
  "(!DUPLICATES && !SEQUENTIAL)",
  "!SHAREDCACHE",
  "((JECACHESIZE && (REPLICATED || SEQUENTIAL || DUPLICATES || !SHAREDCACHE)) || (!JECACHESIZE && ((SHAREDCACHE && ((DUPLICATES && (REPLICATED || SEQUENTIAL)) || (!DUPLICATES && ((REPLICATED && !SEQUENTIAL) || (!REPLICATED && SEQUENTIAL))))) || (!SHAREDCACHE && (REPLICATED || !SEQUENTIAL || !DUPLICATES)))))",
  "SHAREDCACHE",
  "(DUPLICATES && SEQUENTIAL)",
  "(!DUPLICATES && SEQUENTIAL)",
  "JECACHESIZE",
  "DUPLICATES",
  "SEQUENTIAL",
  "!SEQUENTIAL",
  "(JECACHESIZE || DUPLICATES)",
  "(!SHAREDCACHE && DUPLICATES)",
  "(SHAREDCACHE && DUPLICATES)",
  "((!DUPLICATES && SHAREDCACHE && !REPLICATED && !SEQUENTIAL && JECACHESIZE) || (!JECACHESIZE && ((SHAREDCACHE && ((!REPLICATED && !SEQUENTIAL && DUPLICATES) || (!DUPLICATES && ((REPLICATED && SEQUENTIAL) || (!REPLICATED && !SEQUENTIAL))))) || (!REPLICATED && SEQUENTIAL && DUPLICATES && !SHAREDCACHE))))",
  "((JECACHESIZE && ((SHAREDCACHE && (!REPLICATED || SEQUENTIAL || !DUPLICATES)) || (!SHAREDCACHE && (!REPLICATED || SEQUENTIAL || DUPLICATES)))) || (!JECACHESIZE && ((REPLICATED && SEQUENTIAL) || (!REPLICATED && !SEQUENTIAL) || !DUPLICATES || !SHAREDCACHE)))",
  "(DUPLICATES && !SEQUENTIAL)",
  "(!SHAREDCACHE && !DUPLICATES)",
  "!JECACHESIZE",
  "!DUPLICATES",
  "(SHAREDCACHE && !DUPLICATES)",
  "((JECACHESIZE && ((REPLICATED && !SEQUENTIAL && DUPLICATES && SHAREDCACHE) || (REPLICATED && !SEQUENTIAL && !DUPLICATES && !SHAREDCACHE))) || (DUPLICATES && ((REPLICATED && !SEQUENTIAL) || (!REPLICATED && SEQUENTIAL)) && SHAREDCACHE && !JECACHESIZE))",
  "(!JECACHESIZE && !DUPLICATES)"
]