[
  {
    "packageName": "org.h2.index",
    "className": "IndexCondition",
    "methodSignature": "isStart()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "1d11b3aa-d840-47eb-8863-973e902d06f3",
    "startBlock": "609353438",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeIndex",
    "methodSignature": "child(Lorg/h2/pagestore/db/TreeNode;Z)Lorg/h2/pagestore/db/TreeNode;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ef0e3785-6d42-4531-aa9f-d748fe3dd548",
    "startBlock": "1404962612",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "ConditionAndOr",
    "methodSignature": "createIndexConditions(Lorg/h2/engine/Session;Lorg/h2/table/TableFilter;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "22336fa7-9ad6-41fa-836b-5dca0365e4de",
    "startBlock": "361956460",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "checkSchema(Lorg/h2/schema/Schema;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7b34953d-dce3-4f44-95d9-6c89dec0a553",
    "startBlock": "845159788",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileLock",
    "methodSignature": "load()Ljava/util/Properties;",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "fe639513-85c8-47ff-ab79-e6eae3b78153",
    "startBlock": "750728826",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType",
    "methodSignature": "newType(I)Lorg/h2/mvstore/type/ObjectDataType$AutoDetectDataType;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "3e3db50e-a1b8-487a-b493-a8483f8d3fb4",
    "startBlock": "250091907",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "CreateIndex",
    "methodSignature": "update()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "a9fdaf8b-8f9f-4206-a832-c0863d74bbb1",
    "startBlock": "81399313",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "findOldChunks(I)Ljava/lang/Iterable;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "10117150-e8b7-4790-abb1-e6f0c63616a0",
    "startBlock": "422952786",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVDelegateIndex",
    "methodSignature": "<init>(Lorg/h2/mvstore/db/MVTable;ILjava/lang/String;Lorg/h2/mvstore/db/MVPrimaryIndex;Lorg/h2/index/IndexType;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "35f67db9-2bf5-4fe8-af75-4f6a7edceadd",
    "startBlock": "1336246045",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "create(Lorg/h2/pagestore/db/PageBtreeIndex;II)Lorg/h2/pagestore/db/PageBtreeNode;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "31e31d89-ce70-4761-96e4-320887bd77ed",
    "startBlock": "177930821",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "write([BII)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "3ee36433-d724-4416-80de-f3467bddda97",
    "startBlock": "1969394364",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "fillPage()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6c683d66-ed12-45d8-a575-1afbde86592f",
    "startBlock": "1802337602",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntArray",
    "methodSignature": "ensureCapacity(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8c0d9d27-316c-4e6c-ad84-815bcfa1960c",
    "startBlock": "96365427",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStoreTool",
    "methodSignature": "compact(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/MVStore;)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "c1b59373-e2f8-418f-a582-b0b148f08c5b",
    "startBlock": "278821296",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.api",
    "className": "ErrorCode",
    "methodSignature": "getState(I)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "IFEXISTS"
    ],
    "id": "a80bc539-c9da-4d6c-b825-496ea1314105",
    "startBlock": "1291538334",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.analysis",
    "className": "DataAnalysisOperation",
    "methodSignature": "updateAggregate(Lorg/h2/engine/Session;I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "f55c21fb-3cdd-4095-a271-877b2c734b0b",
    "startBlock": "32862946",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueDecimal",
    "methodSignature": "<init>(Ljava/math/BigDecimal;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5235d8e0-e539-4457-8d07-9bbc2d4d5d60",
    "startBlock": "537736142",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueTimestamp",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "05570436-ecef-4a47-b699-6ab54d3a73f7",
    "startBlock": "995593643",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IOUtils",
    "methodSignature": "trace(Ljava/lang/String;Ljava/lang/String;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)"
    ],
    "id": "c7be77e4-4e26-4185-9732-a50eda8aceb7",
    "startBlock": "2143180102",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "getMinMaxColumnIndex()Lorg/h2/index/Index;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e7628c72-c646-40ce-b62c-99b320a4ebe0",
    "startBlock": "985564046",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "IndexColumn",
    "methodSignature": "writeColumns(Ljava/lang/StringBuilder;[Lorg/h2/table/IndexColumn;Z)Ljava/lang/StringBuilder;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "bdaccf8a-5685-4bc4-8f41-19257ff5acad",
    "startBlock": "1196083043",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "HashBase",
    "methodSignature": "checkSizeRemove()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "52275ce3-b6bb-414e-8dcf-d019552d2a0a",
    "startBlock": "1099549298",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TxDecisionMaker",
    "methodSignature": "decide(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/mvstore/MVMap$Decision;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)"
    ],
    "id": "a7bf1791-c76d-4ac0-b5cd-f822393cedc1",
    "startBlock": "978201295",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "getSimpleValue(Lorg/h2/engine/Session;Lorg/h2/value/Value;[Lorg/h2/expression/Expression;[Lorg/h2/value/Value;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "fa655add-c381-4468-99f3-22379b798e7f",
    "startBlock": "1893038057",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "getTransaction()Lorg/h2/mvstore/tx/Transaction;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "b38407d1-3da3-473d-95f4-f14103555a4e",
    "startBlock": "218295638",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "trimSubstring(Ljava/lang/String;II)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "6752f632-dc8b-409a-a6f8-61cb787ec906",
    "startBlock": "1771721684",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "getStringWithoutLengthLen(Ljava/lang/String;I)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "3c4c7272-d1c7-4691-aa17-8005b98685e8",
    "startBlock": "1309530432",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FileStore",
    "methodSignature": "close()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "9c837fcb-4f52-4dba-bc9a-ffb8613b79d3",
    "startBlock": "1285243867",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "startStatementWithinTransaction(Lorg/h2/command/Command;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "e3549697-aeed-4942-8601-aa079e97b8b9",
    "startBlock": "711812663",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readAnd(Lorg/h2/expression/Expression;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0b98309c-e132-4e9c-aeda-e95b5644a7aa",
    "startBlock": "694456999",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "getCache()[Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "08f92492-b2c2-432d-ac9e-810243fcedf7",
    "startBlock": "1211397144",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "accountForWrittenPage(IZ)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8ff561b2-bdc3-4919-933e-082c24e41d0f",
    "startBlock": "1943239038",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Cursor",
    "methodSignature": "traverseDown(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Lorg/h2/mvstore/CursorPos;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "86ad4faf-978c-4425-ad88-a44a2e448908",
    "startBlock": "1767397669",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS$Segment",
    "methodSignature": "remove(JI)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e2127827-7c3e-4d93-ba52-d691fe23ebb4",
    "startBlock": "2063523399",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TxDecisionMaker",
    "methodSignature": "isThisTransaction(I)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "32244fa9-88ac-491d-9fb8-e4824476bbe2",
    "startBlock": "1366337244",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "ValueDataType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "54115d20-7ba4-4a78-b1ea-b3fccd3fc201",
    "startBlock": "1902803576",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Command",
    "methodSignature": "start()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "353ed660-601b-4d43-a463-582e0f568070",
    "startBlock": "1936937993",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "registerUnsavedMemory(I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "a37569e8-9c1b-4508-b4b1-0aaf67358cac",
    "startBlock": "265794968",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getLobStorage()Lorg/h2/store/LobStorageInterface;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "daeb2a86-76c9-4a9b-a72a-be53d4131ced",
    "startBlock": "87659540",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Query",
    "methodSignature": "query(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "0aa29905-b115-43f7-8772-7ee045f939b8",
    "startBlock": "2421801",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "readString(I)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "9ebc636a-73b4-4a53-b6aa-36c8dccbd378",
    "startBlock": "557535124",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "getFirstUncommittedSection()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "d19c3ae7-cb84-48ee-bf9e-591d328066ac",
    "startBlock": "1893095079",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Delete",
    "methodSignature": "update()I",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "446ecab4-9240-4180-b81e-dd271acd8993",
    "startBlock": "538176032",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeIndex",
    "methodSignature": "replace(Lorg/h2/pagestore/db/TreeNode;Lorg/h2/pagestore/db/TreeNode;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "5c66b471-0d20-40f3-9a44-82a108871dc7",
    "startBlock": "1405218045",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType$LongType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "8765dfc2-19a7-4ac7-80f8-eb180bc15f16",
    "startBlock": "315368114",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "throwLastBackgroundException()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "3ef380e7-625e-4529-919d-c57c2739de4f",
    "startBlock": "273464780",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "moveTo(Lorg/h2/engine/Session;I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "8b81028b-9c85-46fd-9a60-46a85cc4c7d6",
    "startBlock": "1951695966",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS$Segment",
    "methodSignature": "getNewMapLen()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "78347d87-24f6-4bfb-bc09-22ebf51d9994",
    "startBlock": "1656143171",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "lock(Lorg/h2/engine/Session;ZZ)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "02e49bb1-c3fa-40b4-a871-fc0db7de851e",
    "startBlock": "575550460",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "incrementChangeCount()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5412c3c4-ebe3-408d-bb29-a539f34736fc",
    "startBlock": "206330887",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "isMemoryChangeRequired()Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "20ff4c53-4dc0-4c50-9641-532261d1cbc6",
    "startBlock": "884306605",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "parseUInt31(Ljava/lang/String;II)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "3db8ec9b-d4fd-4542-b473-015a440d6640",
    "startBlock": "88421778",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "delete()V",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS || (!FILE_LOCK && !DEFRAG_ALWAYS) || FORBID_CREATION)",
      "(FILE_LOCK && !DEFRAG_ALWAYS && !IFEXISTS && !FORBID_CREATION)",
      "(FILE_LOCK && DEFRAG_ALWAYS && !IFEXISTS && !FORBID_CREATION)",
      "(!FILE_LOCK && DEFRAG_ALWAYS && !IFEXISTS && !FORBID_CREATION)"
    ],
    "id": "20401681-2c08-43b3-b283-7160be1799e6",
    "startBlock": "1849359896",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseSet()Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "0edf0f4a-f493-45fb-a625-e6c6e4155d91",
    "startBlock": "1747320204",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIfExists(Z)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e9170978-6877-46b2-ad33-84e1d7a3fd3b",
    "startBlock": "1518738790",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "findPrimaryKey()Lorg/h2/index/Index;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "99c4a153-1085-4989-9058-5281138af7d3",
    "startBlock": "1049964231",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readFromAlias(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "4f7781c3-70d0-4160-9c83-a4bd21b312e0",
    "startBlock": "1949696729",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex",
    "methodSignature": "add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "c1fdeeb3-af53-4d1d-ae1d-2b9646e4b6c1",
    "startBlock": "539575645",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Plan",
    "methodSignature": "removeUnusableIndexConditions()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "e1aa17d0-e283-4d4d-bf3e-36211cadaa73",
    "startBlock": "1925750636",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "writeValue(Lorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "9540d604-c39c-432d-bc0f-3c0ec202b14c",
    "startBlock": "620311221",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "lockMeta(Lorg/h2/engine/Session;)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "6de3e616-337c-408d-b66c-5b8a2e3b5d0a",
    "startBlock": "219115544",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "getMapNames()Ljava/util/Set;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "cf8156aa-335e-4cba-b3b7-b1cde34b5623",
    "startBlock": "1276472636",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "read(Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "3894209d-fcd1-44c3-82bd-89fb0dc34f3d",
    "startBlock": "1614789061",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "freeUnused(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "e2561caf-c656-4e6b-9aba-7c20b78367ed",
    "startBlock": "2004639085",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "freeLogPages(Lorg/h2/util/IntArray;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE || IFEXISTS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "6eddc442-a369-469c-b59d-3b646741dca7",
    "startBlock": "31034864",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntIntHashMap",
    "methodSignature": "get(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "2c8355d8-bbfb-4ba9-a379-c35731b30598",
    "startBlock": "271161151",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "CursorPos",
    "methodSignature": "traverseDown(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Lorg/h2/mvstore/CursorPos;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "32cbe6ec-b4c8-4447-951d-a120a91be4d6",
    "startBlock": "1939614383",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "copy(Lorg/h2/mvstore/Page;Lorg/h2/mvstore/Page;I)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "2545c654-86f4-4132-b23f-31843d990db5",
    "startBlock": "1976965986",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseAlterTableAddColumn(Ljava/lang/String;Lorg/h2/schema/Schema;Z)Lorg/h2/command/ddl/AlterTableAlterColumn;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "6c74e65e-d4b5-4a4a-bde4-0effce00018b",
    "startBlock": "1265952436",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "ParserUtil",
    "methodSignature": "getSaveTokenType(Ljava/lang/String;ZIIZ)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "7cc946c5-8901-42d1-b0f3-9c8e54e585d1",
    "startBlock": "1590038409",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "readChunkHeaderOptionally(J)Lorg/h2/mvstore/Chunk;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "72155de3-7115-45df-9442-f2e34fe8ea60",
    "startBlock": "927349865",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeIndex",
    "methodSignature": "<init>(Lorg/h2/pagestore/db/PageStoreTable;ILjava/lang/String;[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ff72b10d-8618-4b8d-a0e2-10ed02d95439",
    "startBlock": "2289526",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStreamTrunk",
    "methodSignature": "free(I)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "5c498097-c05e-4de3-9b95-67cdfdd995d6",
    "startBlock": "528495009",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "95adce68-62fd-4df9-a27e-3dd1f6d90ca6",
    "startBlock": "1474582758",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "memoryChange(ZLorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f6ee1eeb-3f82-4e78-bd6e-c14c00e8db33",
    "startBlock": "223921470",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "rollbackTo(Lorg/h2/mvstore/tx/Transaction;JJ)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "b39e1258-37da-4c49-8b3e-cb4d05dafcf2",
    "startBlock": "1519990165",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "write([BII)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "ab546228-3957-4468-8e5b-32046fec993e",
    "startBlock": "1238741414",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "skipOffset(Lorg/h2/command/dml/Select$LazyResultSelect;JZ)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "e85540be-88b6-4e9e-b6d7-9638976c0d6d",
    "startBlock": "584154551",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIdentifierWithSchema(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "2bc89b08-0301-43e0-a48b-ecb6f0003be3",
    "startBlock": "1685246933",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "parseMap(Ljava/lang/String;)Ljava/util/HashMap;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "57a4c242-32e0-4e61-8963-ecd70f9ff612",
    "startBlock": "1694199810",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "calculateTotalCount()J",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "bed10dcf-23d2-427a-bed7-fd6ae9b78539",
    "startBlock": "1650093653",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueString",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b80d9876-514e-4e8a-9bd2-0bf74554663f",
    "startBlock": "207382209",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "setColumns([Lorg/h2/table/Column;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "2e04fc3a-d85f-4d27-aefb-453ab414685d",
    "startBlock": "526877214",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "writeData()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "084d4b26-140c-458e-a2bb-7f456eec37ed",
    "startBlock": "892980522",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "newConcurrentStringMap()Ljava/util/concurrent/ConcurrentHashMap;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "6e48020e-3edf-43c5-81fb-221ba100a6c2",
    "startBlock": "1367405512",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Utils",
    "methodSignature": "parseBoolean(Ljava/lang/String;ZZ)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9de4d782-656d-44c7-b171-39175d9f3ca0",
    "startBlock": "1531111395",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Plan",
    "methodSignature": "setEvaluatable(Lorg/h2/table/TableFilter;Z)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "2225d24f-ffd8-4501-a35d-3eac9136249d",
    "startBlock": "804032550",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "addRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "892e6acd-8510-454f-b8a7-f3ed52ef1699",
    "startBlock": "1463135130",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "swap(III)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "2d0df3da-a560-4582-85c1-deaa4d105790",
    "startBlock": "736595391",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "hasPendingTransaction()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "8df49d94-d1fe-49ad-b820-423733f2c007",
    "startBlock": "2128210172",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileLock",
    "methodSignature": "unlock()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "14f18f41-1104-47e9-9a55-60e01f2ad20c",
    "startBlock": "1758456226",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "isPageSaved(J)Z",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "6a549861-ad10-4d4d-b268-b7c470ba024a",
    "startBlock": "110532507",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "User",
    "methodSignature": "hasRight(Lorg/h2/table/Table;I)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "7bb5927b-ddfb-4bea-a8c5-509be5ea3aa4",
    "startBlock": "1524879744",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "addDatabaseObject(Lorg/h2/engine/Session;Lorg/h2/engine/DbObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "1015bf3e-555c-4dc6-93f1-dedf7753f2d4",
    "startBlock": "1343194744",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "getFirstLast(Lorg/h2/mvstore/Page;Z)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "6e92757b-a5eb-4178-8e44-5142b25a773a",
    "startBlock": "357866967",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "createNode(Lorg/h2/mvstore/MVMap;[Ljava/lang/Object;[Lorg/h2/mvstore/Page$PageReference;JI)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "c5ee40fe-2115-42ac-9a00-3cbfb08ad4f7",
    "startBlock": "538067079",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Command",
    "methodSignature": "executeQuery(IZ)Lorg/h2/result/ResultInterface;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "dfc1ffa1-bd60-445b-9c3d-93653e48c446",
    "startBlock": "1444497324",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueStringFixed",
    "methodSignature": "get(Ljava/lang/String;)Lorg/h2/value/ValueStringFixed;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "2f5d5b77-a6f5-476f-a697-4837b63d8c77",
    "startBlock": "1665620989",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseAlterTableRename(Lorg/h2/schema/Schema;Ljava/lang/String;Z)Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "0a3ffbf7-4107-40e5-8705-6ce94e902e8f",
    "startBlock": "1116839774",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "createShallowCopy(Lorg/h2/engine/SessionInterface;)Lorg/h2/result/LocalResultImpl;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "708971d1-fdec-419c-b712-4f0472114817",
    "startBlock": "1705394995",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "compact(II)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "254b9ab9-7ed9-4730-b18d-1ce9534e9737",
    "startBlock": "494437811",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileDisk",
    "methodSignature": "truncate(J)Ljava/nio/channels/FileChannel;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "b0f98a23-d528-4536-a77a-8aa9847119ab",
    "startBlock": "1190136960",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "equalsToken(Ljava/lang/String;Ljava/lang/String;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "291d3c85-9666-4048-84f1-6716f8db1d76",
    "startBlock": "1786983834",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ValueExpression",
    "methodSignature": "isNullConstant()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "14fbc05a-8666-4a51-8478-0830418b6b7d",
    "startBlock": "1892463679",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "isClosed()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "1916d546-bf86-4ef5-b597-6d136ec22e03",
    "startBlock": "106714072",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "convertToLong(Ljava/lang/Object;)Lorg/h2/value/ValueLong;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "47113d16-56ea-4b33-af22-e083ebcd1372",
    "startBlock": "1886256663",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "beforeWrite(Lorg/h2/mvstore/MVMap;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "ef72cfe2-7e9d-4da2-8f53-1db19488cc66",
    "startBlock": "1646224128",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "put(Lorg/h2/util/CacheObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "c85736c5-1fa7-4ba5-8e7c-9b6e8fc8c687",
    "startBlock": "993996597",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "checkArgument(ZLjava/lang/String;[Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "9ed790b3-6050-47fd-a60f-2b50783f0fcd",
    "startBlock": "696851933",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "translateFileName(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)"
    ],
    "id": "d949b358-aec5-4fdd-8c23-04dea4c71174",
    "startBlock": "532329407",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "accountForRemovedPage(JJZ)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "f320a9ce-689e-4cf0-87d4-9c3fcc560ee6",
    "startBlock": "883354857",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "convertToLong(Ljava/math/BigDecimal;Ljava/lang/Object;)J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "abaa69cb-bdc8-4088-85c9-c2ad50425be8",
    "startBlock": "1895826037",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTableEngine$Store",
    "methodSignature": "close(J)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "5b13c798-ed73-41c8-84a6-46d5d148db50",
    "startBlock": "1984177186",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "<init>(Ljava/util/Map;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "412f3430-693d-4b5d-b38b-d318e64211de",
    "startBlock": "1158614409",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "setSortSetting(IZZ)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7ff27041-7097-45df-8469-b769ba25809f",
    "startBlock": "2131093361",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStreamTrunk",
    "methodSignature": "write()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "79365035-b273-4fa8-9547-95c6a80656d7",
    "startBlock": "1826145260",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "expandUserHomeDirectory(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)"
    ],
    "id": "e69bfab5-0e70-4f67-a285-64e4273ae9e8",
    "startBlock": "1126259084",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Column",
    "methodSignature": "validateConvertUpdateSequence(Lorg/h2/engine/Session;Lorg/h2/value/Value;)Lorg/h2/value/Value;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "1e0a1409-c9f1-4e4e-a2a8-52bfbc6f36cd",
    "startBlock": "36753753",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Optimizer",
    "methodSignature": "testPlan([Lorg/h2/table/TableFilter;)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "8d83a03a-8e46-4c23-b393-8d11314ce50a",
    "startBlock": "131084724",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "moveChild(II)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "225a950e-9047-4163-82ba-6eea7cfd4b10",
    "startBlock": "154510101",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.constraint",
    "className": "Constraint$Type",
    "methodSignature": "getSqlName()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "f4280437-e8d8-4786-9a28-27afed734f39",
    "startBlock": "1206196858",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "queryQuick(ILorg/h2/result/ResultTarget;Z)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8d0ef1bf-105a-4b81-8243-d4860921ccbb",
    "startBlock": "468231747",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "length()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "40a86fd8-a018-4aba-82ad-5b0840db012c",
    "startBlock": "1776419237",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "getPage(I)Lorg/h2/pagestore/db/PageBtree;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b58cbbbb-1ba2-4aa1-9d58-6dcf8f9e1b02",
    "startBlock": "717935670",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "ConditionAndOr",
    "methodSignature": "getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "66e34205-aa3a-45ea-81bd-2186efbb98e5",
    "startBlock": "95930813",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeIndex",
    "methodSignature": "balance(Lorg/h2/pagestore/db/TreeNode;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "5712fe4f-104f-4d09-93c0-92efbb73c550",
    "startBlock": "1612405876",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "DbSettings",
    "methodSignature": "<init>(Ljava/util/HashMap;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "07eb623a-3739-48e7-bdf6-fced30f2a0af",
    "startBlock": "3725437",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "setParameter(ILorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "cd800e9d-4e49-4f73-bb60-6b3d3a63d9bf",
    "startBlock": "6200517",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "writeHead()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "11b7df57-2927-4fde-a822-0c0b5d4559be",
    "startBlock": "1556207663",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "prepareStatement(Ljava/lang/String;)Ljava/sql/PreparedStatement;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "e1e0ff88-2240-4491-8984-4d4e2cf67597",
    "startBlock": "635043250",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "checksumSet([BI)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d26f48b0-9943-49c5-b3b7-05292c919014",
    "startBlock": "917737025",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "readPage(ILorg/h2/store/Data;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8b504456-6dd4-4525-8cf5-660d1d0495fd",
    "startBlock": "1264830226",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVPrimaryIndex$MVStoreCursor",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "e5fd9ab1-088c-4977-b73f-9142c5eb2562",
    "startBlock": "2095561376",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "quoteIdentifier(Ljava/lang/StringBuilder;Ljava/lang/String;)Ljava/lang/StringBuilder;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "IFEXISTS",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0996df01-0e37-438b-9c0a-a404a78ef616",
    "startBlock": "1667872768",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$Leaf",
    "methodSignature": "remove(I)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "40bdc7fc-12ed-49c1-b44f-8a55a12775e3",
    "startBlock": "97266983",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceSystem",
    "methodSignature": "isEnabled(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "a3317675-3751-4878-9ff6-2eca428062fb",
    "startBlock": "287946945",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "ddb81ec8-fcee-4a35-af39-a0cbdf304fc7",
    "startBlock": "1559943900",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "doLock2(Lorg/h2/engine/Session;IZ)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "915d354d-7380-49b9-b2ab-4b68ca00081b",
    "startBlock": "1288680455",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "writeBack(Lorg/h2/util/CacheObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "0af1d87f-e68c-45b8-9854-7bd2be9c61e6",
    "startBlock": "1847535227",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueLong",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "f0aa4645-56ce-4bb6-855d-8160d6898d8a",
    "startBlock": "256819884",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionColumn",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "c9112d9d-3c0c-491e-b7f2-ac16a5fa6d19",
    "startBlock": "115737293",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileLock",
    "methodSignature": "save()Ljava/util/Properties;",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "0f8941d5-ba99-4704-bab3-4716ee59d8c3",
    "startBlock": "273054115",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "getFunction(Lorg/h2/engine/Database;Ljava/lang/String;)Lorg/h2/expression/function/Function;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9fe5fb45-1c39-4f06-8e48-b53ffca52351",
    "startBlock": "160702077",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "getTotalCount()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "9220c963-8f80-4745-805b-ccf7db208e20",
    "startBlock": "938644225",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "rowForResult([Lorg/h2/value/Value;I)[Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "0a38c31b-c8e2-42ce-ba72-695a22917654",
    "startBlock": "581709174",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTableEngine$Store",
    "methodSignature": "closeImmediately()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "e501e726-4bac-41cc-9f56-3a0e16d441b9",
    "startBlock": "952017806",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "removeRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "21189739-6d49-4894-bbd8-b29436a43a98",
    "startBlock": "1767797918",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "SelectGroups$Grouped",
    "methodSignature": "done()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "3e15fdd5-72d5-4bef-94b7-3034effa524b",
    "startBlock": "1164915780",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "WriteBuffer",
    "methodSignature": "clear()Lorg/h2/mvstore/WriteBuffer;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "f25fc050-aee3-4d84-9070-5bb681447076",
    "startBlock": "293389017",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS$Segment",
    "methodSignature": "put(JILjava/lang/Object;I)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "00b54ed0-f0d9-40f5-9c38-034a6b59bbca",
    "startBlock": "1886585289",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "MathUtils",
    "methodSignature": "convertLongToInt(J)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f247c27a-dd6e-4e32-8d4e-1a583b93419b",
    "startBlock": "1602209295",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDelegateIndex",
    "methodSignature": "findFirstOrLast(Lorg/h2/engine/Session;Z)Lorg/h2/index/Cursor;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8b2d7477-8cdb-4f83-becf-a0960aec3799",
    "startBlock": "1472871260",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isReadOnly()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "ee3c1f15-69fa-40cc-a61e-40d317c999a4",
    "startBlock": "695684688",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getIgnoreCase()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "a3e63ad9-5277-4717-8a2a-a2edf44d5948",
    "startBlock": "1909774061",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "newDirectoryStream()Ljava/util/List;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "21b4a08c-ff1c-400f-865b-e4303c99e82d",
    "startBlock": "252938273",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "translateSQL(Ljava/lang/String;Z)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "754eca88-896a-4fe7-b2a9-70a034f89d15",
    "startBlock": "183714934",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "setString(ILjava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "e7e8855b-8ac3-423c-9ae0-ef99c0442478",
    "startBlock": "1677243419",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.schema",
    "className": "Schema",
    "methodSignature": "getMap(I)Ljava/util/Map;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "cac8cd12-b5b9-4fc4-a612-feee0f124cb8",
    "startBlock": "1461785164",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Snapshot",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "1b50eb21-841e-4bd0-8d22-3d0ebbd1f180",
    "startBlock": "1381821400",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "removeUnusedOldVersions(J)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1562d633-2c58-4e48-bc41-2bae478d5f44",
    "startBlock": "1891006529",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore$3",
    "methodSignature": "compare(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/Chunk;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "2568e83e-140a-4fd5-bc1e-362f0ba114ff",
    "startBlock": "638185071",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Column",
    "methodSignature": "getCreateSQL(Z)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9d68ec30-db4e-4d97-94c3-2e2fdbe47fe8",
    "startBlock": "189410469",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileLock",
    "methodSignature": "lockFile()V",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "5fb6b51f-0b2c-42da-a633-4ffe62e93eb8",
    "startBlock": "885181697",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "Page",
    "methodSignature": "remove([JII)[J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1e946157-7205-4d52-86fa-e63fc5be8dea",
    "startBlock": "619017025",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "VersionedValueType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "f5b4f1e9-d377-461b-ba21-b55a53fc92c9",
    "startBlock": "541295742",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "readBufferForPage(Lorg/h2/mvstore/FileStore;JI)Ljava/nio/ByteBuffer;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "e579c94e-35ca-4dd5-89e4-8a02d221a637",
    "startBlock": "1170963767",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "store(JJ)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "974fdafe-f23d-4fa7-872a-6e03390fa06e",
    "startBlock": "1759353333",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "expandColumnList()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "d1a32701-421a-48f1-89d0-8cc41493e9dc",
    "startBlock": "356471446",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "RegularTable",
    "methodSignature": "addRowsToIndex(Lorg/h2/engine/Session;Ljava/util/ArrayList;Lorg/h2/index/Index;)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "d5db2fc6-c6ad-4fd7-aa44-94bb65e3ba21",
    "startBlock": "215107026",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex",
    "methodSignature": "convertToKey(Lorg/h2/result/SearchRow;Lorg/h2/value/ValueLong;)Lorg/h2/value/ValueArray;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "26ded82a-8ebb-432d-9b99-b46398504304",
    "startBlock": "276381006",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "parseDatabaseShortName()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "b5c08f96-eae0-4088-b2a7-5c594041e85b",
    "startBlock": "575638324",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "writeStringWithoutLength(Ljava/lang/String;I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "4e44900e-422c-4cca-9f4e-33457df7859d",
    "startBlock": "1238567504",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "isUndoEmpty()Z",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "fa544579-c2a5-45f0-bd51-bcf96c46fc0b",
    "startBlock": "1140776997",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "Comparison",
    "methodSignature": "getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "8ee1879e-bedc-444a-b3ce-1280b408ddde",
    "startBlock": "1684507639",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "closeOpenFilesAndUnlock(Z)V",
    "decisionIndex": 1,
    "info": [
      "(FILE_LOCK && !DEFRAG_ALWAYS && !IFEXISTS && !FORBID_CREATION)",
      "(FILE_LOCK && DEFRAG_ALWAYS && !IFEXISTS && !FORBID_CREATION)",
      "(!FILE_LOCK && DEFRAG_ALWAYS && !IFEXISTS && !FORBID_CREATION)",
      "(!FILE_LOCK && !DEFRAG_ALWAYS && !IFEXISTS && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "c4aed5d9-de53-4a53-9fef-350fee8f70bc",
    "startBlock": "398650862",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "getSearchRow(Lorg/h2/result/Row;)Lorg/h2/result/SearchRow;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "950d04f3-84db-4cbc-8e63-38ac5f0eb9c6",
    "startBlock": "225309104",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readTermWithIdentifier(Ljava/lang/String;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0b824c3a-3d20-436c-a55e-7daba1c9bbf8",
    "startBlock": "1150004304",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStoreTool",
    "methodSignature": "compact(Ljava/lang/String;Ljava/lang/String;Z)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "5eb9df33-e512-4ffe-8687-47b8de0f245a",
    "startBlock": "1532133997",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "removeOrphanedLobs()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "0f4a9586-c366-4194-a5f1-10886750cfa2",
    "startBlock": "1039828819",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "dropUnusedVersions()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b74b2488-8db8-44a3-9ab5-fae14275d116",
    "startBlock": "1833889536",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select$LazyResultQueryFlat",
    "methodSignature": "fetchNextRow()[Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "f088113c-16a9-4801-afcf-185f3921e771",
    "startBlock": "1067098670",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "AbstractAggregate",
    "methodSignature": "setEvaluatable(Lorg/h2/table/TableFilter;Z)V",
    "decisionIndex": -1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "276111b3-2a0d-4455-8e46-bb5aaf338335",
    "startBlock": "581014932",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS",
    "methodSignature": "setMaxMemory(J)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "196e10c4-ccb1-42f1-bea6-ba037d406fb0",
    "startBlock": "1391501667",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "writeChunkHeader(Lorg/h2/mvstore/WriteBuffer;I)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "54d2a917-523b-4f23-919c-fc8a0a0065ac",
    "startBlock": "532783555",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "increaseFileSize()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE)"
    ],
    "id": "1a100fd8-f2cd-43fc-ac2c-f6a8660a9613",
    "startBlock": "1181364823",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Engine",
    "methodSignature": "checkClustering(Lorg/h2/engine/ConnectionInfo;Lorg/h2/engine/Database;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ee1729df-a768-40bf-9b1e-1d7abf75a21b",
    "startBlock": "1507356160",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "createSnapshot(I)Lorg/h2/mvstore/tx/Snapshot;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "464b080e-9848-4f93-9c91-5523bb6058e6",
    "startBlock": "1901750293",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "getStatementSavepoint()J",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "9a1b479f-cfaa-47de-a111-32911ddb68a4",
    "startBlock": "1647565642",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowImpl",
    "methodSignature": "getMemory()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "18302577-b59f-4b2e-b797-30a985b41f48",
    "startBlock": "1459324877",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "addSchemaObject(Lorg/h2/engine/Session;Lorg/h2/schema/SchemaObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "286d92e7-afa7-4016-b383-8d6e7e179a77",
    "startBlock": "114341253",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "free()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b5f048e1-161c-4556-9dde-92a989c50da0",
    "startBlock": "786367306",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "updateRowCount(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "6454eb56-c4a9-4859-98c4-bac760b8e3f5",
    "startBlock": "1139429743",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "deregisterVersionUsage(Lorg/h2/mvstore/MVStore$TxCounter;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "266d06b5-cb5f-4628-b506-0fd5a9daeb34",
    "startBlock": "1561050658",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "UndoLogRecord",
    "methodSignature": "undo(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "9bff8814-1b96-4899-9ed5-53e4547bc1b8",
    "startBlock": "1803807730",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceObject",
    "methodSignature": "debugCodeCall(Ljava/lang/String;J)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "d3964dad-5b53-48dc-bf76-0aafa7f4a1a3",
    "startBlock": "387079227",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "initMemoryAccount(I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "221ad00b-ac2b-4041-98a2-f4a3d7c986a5",
    "startBlock": "142321654",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "isHavingNullOrFalse([Lorg/h2/value/Value;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "3abcc943-cb1a-4e71-ba97-4adbf6df0a18",
    "startBlock": "1419555191",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Utils",
    "methodSignature": "copyBytes([BI)[B",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE || IFEXISTS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "96b24a6a-411c-46d5-b021-f55672ac7b6b",
    "startBlock": "98627185",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "getValueLen(Lorg/h2/value/Value;Z)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8bd6ced2-3ab8-423d-8b47-f9fea4ba3cbc",
    "startBlock": "690948663",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FreeSpaceBitSet",
    "methodSignature": "free(JI)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "53d0880b-4665-478d-8bd9-c6b917d4b40b",
    "startBlock": "587443790",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readFunctionParameters(Lorg/h2/expression/function/Function;)Lorg/h2/expression/function/Function;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7bfe346e-d64b-4615-a1cd-1adf338652a7",
    "startBlock": "2012238253",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Utils",
    "methodSignature": "compareSecure([B[B)Z",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "5e7c77aa-a5fe-41ab-8bda-974453f80141",
    "startBlock": "214205777",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "JSR310Utils$WithTimeZone8",
    "methodSignature": "yearForCalendar(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "50591d9a-7568-4463-865c-7e70a4027d1a",
    "startBlock": "154366149",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseTableColumnDefinition(Lorg/h2/command/ddl/CommandWithColumns;Lorg/h2/schema/Schema;Ljava/lang/String;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "957ddbd5-b3f5-41d4-bfad-22aad1f1fd1f",
    "startBlock": "409958013",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readCommentIf()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "2bd8c58c-acd5-44d0-a0ae-7cfaf9980588",
    "startBlock": "40608400",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$Leaf",
    "methodSignature": "split(I)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "6146af39-4644-4dbe-babd-048ebbc45f74",
    "startBlock": "1575278791",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "edu.cmu.cs.mvelezce.bench.c",
    "className": "BenchCThread",
    "methodSignature": "processOrderStatus()V",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "c3ae471f-e4b3-43c2-a17c-6797c228b302",
    "startBlock": "284520196",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseValuesForCommand(Lorg/h2/command/dml/CommandWithValues;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "f2fb6ef2-278c-4211-a3cd-76a5f77c97b1",
    "startBlock": "1521532595",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowImpl",
    "methodSignature": "getValue(I)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "d37966ce-9e29-4622-9694-fbea84c3aa26",
    "startBlock": "1573560280",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "getObjectIds()Ljava/util/BitSet;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7b3588df-6276-49cb-b547-e261e2df69a0",
    "startBlock": "490285375",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "7e9589b9-2e18-4e13-af34-1af43e09d67d",
    "startBlock": "423394596",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$Leaf",
    "methodSignature": "setValue(ILjava/lang/Object;)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "fb3041a2-4c05-4f78-ba8a-9966700e158e",
    "startBlock": "1033486733",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageData",
    "methodSignature": "find(J)I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f8e7f323-384c-49a5-be36-77d2e854041d",
    "startBlock": "349580148",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeCursor",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "48bb048f-c7b2-49a3-b861-af1d49e8d1d6",
    "startBlock": "859084069",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "writeHead()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "ec31fb53-392b-4912-a4e3-6dd06272d61e",
    "startBlock": "1638786095",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "addIndex(Lorg/h2/engine/Session;Ljava/lang/String;I[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;ZLjava/lang/String;)Lorg/h2/index/Index;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ca42843c-e3ad-4e87-95ef-1f9178e5f614",
    "startBlock": "357770844",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "clearIt()Lorg/h2/mvstore/RootReference;",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "f238ab0d-d3a4-4be9-a0c0-7ed678a9d9b9",
    "startBlock": "1323141642",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readInt()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "a947059a-ca61-4bfb-a50f-435f05ae62e5",
    "startBlock": "416980255",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readFunction(Lorg/h2/schema/Schema;Ljava/lang/String;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "b4f050fb-6d24-462c-92bc-f34c23fc20ee",
    "startBlock": "120176735",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "isEmpty()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "7a80701f-7756-4fe1-b80b-e8aa24e5710b",
    "startBlock": "805744373",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIfMore()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "869d8bcd-5642-4ecd-bd44-46ca5309ae1a",
    "startBlock": "1840398355",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntArray",
    "methodSignature": "removeRange(II)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "841bf8c5-a5c4-4082-9db2-29847b89181d",
    "startBlock": "613802120",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "<init>(Lorg/h2/mvstore/MVStore;Lorg/h2/mvstore/type/DataType;Lorg/h2/mvstore/type/DataType;IJLjava/util/concurrent/atomic/AtomicReference;IZ)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "396652c6-8fad-4632-8832-bcffc35e7fd7",
    "startBlock": "469433569",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "isLive()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8bc036fa-7114-49da-840b-37072c2e8f25",
    "startBlock": "1195528174",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TxDecisionMaker$PutIfAbsentDecisionMaker",
    "methodSignature": "decide(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/mvstore/MVMap$Decision;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)"
    ],
    "id": "8e0a7414-48f2-4e69-9540-89393558b392",
    "startBlock": "1402954371",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "logAddOrRemoveRow(Lorg/h2/engine/Session;ILorg/h2/result/Row;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "c6766d13-ee25-47f7-b5fd-d88932d42d3f",
    "startBlock": "1496394136",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "log(ILjava/lang/Object;Lorg/h2/value/VersionedValue;)J",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "0dd8addb-e6fa-4494-a7a8-811daf019fe2",
    "startBlock": "683863098",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueBoolean",
    "methodSignature": "get(Z)Lorg/h2/value/ValueBoolean;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "5ac34b0b-7c0f-4363-8448-f6bbbdca962a",
    "startBlock": "395750350",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "DataType",
    "methodSignature": "supportsAdd(I)Z",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "ed83f6fe-66a3-4175-995d-37f0f261485f",
    "startBlock": "2064872638",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "releaseWriteBuffer(Lorg/h2/mvstore/WriteBuffer;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5e803948-f0b2-458d-a00c-eeda0a4bc0ef",
    "startBlock": "1749464699",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "2693c31f-14e8-400a-b3c7-fda13dd468a9",
    "startBlock": "1071917249",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStreamTrunk",
    "methodSignature": "read()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e017b5a0-5790-4e32-8b31-98c1074ba1c6",
    "startBlock": "572432885",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "memoryChange()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "c078e923-547d-47d6-9cb6-19748d5e69c8",
    "startBlock": "893873575",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "getFletcher32([BII)I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "cd59d7e8-ba2e-403b-9a0f-1cf988fd954d",
    "startBlock": "2001885805",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "isConditionMet()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6459114c-a82d-4969-aa95-0841bc931be0",
    "startBlock": "2030789567",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "Page",
    "methodSignature": "remove([III)[I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "6d1e2180-a63d-4cc4-91e8-60bcdb815e51",
    "startBlock": "611041975",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore$BackgroundWriterThread",
    "methodSignature": "run()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)"
    ],
    "id": "f1d52eac-bf08-4771-b150-bc18e37e0289",
    "startBlock": "55389070",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "doLock1(Lorg/h2/engine/Session;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "1612f10b-db3b-46e2-9f87-914b3c23a8f4",
    "startBlock": "679026886",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "writeVarInt(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "c46a4daf-19aa-469c-aa22-613aed9b1c1a",
    "startBlock": "258351100",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "freeReserved()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "40bb4078-761e-4ce2-b0f9-46545d877f32",
    "startBlock": "1022699294",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "BinaryOperation",
    "methodSignature": "getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "8d3e5e7f-f7d0-4a9c-accd-b3112550a4fd",
    "startBlock": "344400767",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "TypeInfo",
    "methodSignature": "getTypeInfo(IJILorg/h2/value/ExtTypeInfo;)Lorg/h2/value/TypeInfo;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9ab27d50-4527-4eb3-9915-0e952eba9de1",
    "startBlock": "893387360",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "<init>(Lorg/h2/command/ddl/CreateTableData;Lorg/h2/mvstore/db/MVTableEngine$Store;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !ANALYZE_AUTO && !FORBID_CREATION)",
      "(!IFEXISTS && ANALYZE_AUTO && FORBID_CREATION)",
      "(IFEXISTS && !ANALYZE_AUTO && !FORBID_CREATION)",
      "(!IFEXISTS && ANALYZE_AUTO && !FORBID_CREATION)",
      "(IFEXISTS && ANALYZE_AUTO && !FORBID_CREATION)",
      "(IFEXISTS && ANALYZE_AUTO && FORBID_CREATION)",
      "(IFEXISTS && !ANALYZE_AUTO && FORBID_CREATION)",
      "(!IFEXISTS && !ANALYZE_AUTO && FORBID_CREATION)"
    ],
    "id": "442660ff-2b05-4d74-9523-51dfdc959446",
    "startBlock": "543837961",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "convertToString(Lorg/h2/engine/CastDataProvider;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "a2746468-8fbb-42ff-9e7f-0d733af3fbee",
    "startBlock": "1392303535",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "getAggregateType(Ljava/lang/String;)Lorg/h2/expression/aggregate/AggregateType;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "2f5f9855-e9c2-4bcd-935d-f74077318fad",
    "startBlock": "772347360",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS$Segment",
    "methodSignature": "pruneStack()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "0c81e968-11ba-451b-9254-3a2313c34bfc",
    "startBlock": "587858534",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "1a26c75f-b8db-4c3f-a5b3-275257bba52d",
    "startBlock": "1802317245",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "removeAllRecursive(J)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "72573045-a57f-48c9-ab78-79e4916f0a2e",
    "startBlock": "448588697",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageData",
    "methodSignature": "setParentPageId(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d1b125fd-a23c-4131-802f-cef83fdfacea",
    "startBlock": "990849932",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "cache(Lorg/h2/value/Value;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "ce83406c-e8f0-4485-ad54-060b811acdc8",
    "startBlock": "1134643965",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueLong",
    "methodSignature": "get(J)Lorg/h2/value/ValueLong;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "7913923c-3e75-4b34-95f2-60153f0767ef",
    "startBlock": "2051710337",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "getUndoLogName(I)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "c4f39cbb-8c09-4bf2-9021-d7f98c46a6d5",
    "startBlock": "1051680788",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType$IntegerType",
    "methodSignature": "getMemory(Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "32df5164-e2de-4f22-b127-92468b6b2ddc",
    "startBlock": "536975270",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getAllSchemaObjects(I)Ljava/util/ArrayList;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "3f040f7f-465e-4f41-887c-d02a2bc2d517",
    "startBlock": "595156908",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "split(I)Lorg/h2/pagestore/db/PageData;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "ccb986c0-38a0-43b3-921b-d8defde94bd8",
    "startBlock": "401588148",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "endLeftoverTransactions()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7ce75702-82e1-4233-8ae3-da2bd355927d",
    "startBlock": "178151720",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "addIndex(Lorg/h2/engine/Session;Ljava/lang/String;I[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;ZLjava/lang/String;)Lorg/h2/index/Index;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "acf317a5-9f2f-49b5-910c-900ba49a2d8c",
    "startBlock": "1722241843",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "ValueDataType",
    "methodSignature": "writeValue(Lorg/h2/mvstore/WriteBuffer;Lorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "e3418cde-e5d9-4cba-8aed-4e65edc07c0b",
    "startBlock": "884511024",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "setCurrentCommand(Lorg/h2/command/Command;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "c73cef01-f4f9-4b21-92b3-664108778601",
    "startBlock": "713294501",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "LobStorageBackend",
    "methodSignature": "removeAllForTable(I)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "3f199cf2-967a-48dc-abfa-40a5683064fb",
    "startBlock": "1698370849",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "tryUnlockAndUpdateVersion(JI)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "eceb248f-4d2d-4671-acd8-a18afe9035c7",
    "startBlock": "227501473",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "removeFromLinkedList(Lorg/h2/util/CacheObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "48d5ab28-82eb-4b0f-93a8-cba6912f90a5",
    "startBlock": "1864333355",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTableEngine",
    "methodSignature": "init(Lorg/h2/engine/Database;)Lorg/h2/mvstore/db/MVTableEngine$Store;",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && COMPRESS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !COMPRESS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "dba5a271-b69d-45ee-83e9-4dc89b9ae6a4",
    "startBlock": "664987556",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "getOrder(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6175c7a7-791b-4a3a-a844-480fd1423e9d",
    "startBlock": "175307034",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "Page",
    "methodSignature": "add([IIII)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "858d2b9c-9701-4e9e-9acd-cf9eb4416c46",
    "startBlock": "2031310416",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowList",
    "methodSignature": "<init>(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "05fb8e32-828b-4173-8854-668cc516bacb",
    "startBlock": "1517679786",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "CommandContainer",
    "methodSignature": "query(I)Lorg/h2/result/ResultInterface;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "5d563c7f-e7c8-4396-a9df-34e33c46a258",
    "startBlock": "1201988677",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "calculateMemory()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5672e6fc-5c65-4770-b3df-7418a8b9289b",
    "startBlock": "1690407560",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "allocatePages(Lorg/h2/util/IntArray;ILjava/util/BitSet;I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "cf56a417-3375-49cc-a245-b13d9346e58f",
    "startBlock": "391267111",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "ConnectionInfo",
    "methodSignature": "removeProperty(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "aabe5f72-dbd1-44ed-995f-f16d49362475",
    "startBlock": "552179342",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "ParserUtil",
    "methodSignature": "eq(Ljava/lang/String;Ljava/lang/String;ZII)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "fc33ce0c-5b84-4099-94e0-bfb0f8b80c72",
    "startBlock": "1963721943",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "getBuffer()Lorg/h2/store/Data;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f3dad846-d410-4c82-bcb6-1413f3ff98d0",
    "startBlock": "926649259",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "find(I)Lorg/h2/util/CacheObject;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "912b263f-ca33-4483-a5e6-c52b96e53b98",
    "startBlock": "2055395605",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "next()Z",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "c0146604-ffde-4b52-afc7-0118adb2d470",
    "startBlock": "2089642065",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntArray",
    "methodSignature": "get(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "a47f2377-95b3-4228-9fb2-1ab15599394f",
    "startBlock": "2043282859",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getAllowLiterals()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "be2b8d4f-19bd-4f7c-8d8f-dbe2f2c09751",
    "startBlock": "2113673843",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtree",
    "methodSignature": "readAllRows()V",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "((MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)) || (!MV_STORE && (FORBID_CREATION || IFEXISTS)))",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "7d1ad0b7-5cc2-45f1-b6c9-0fe9968e6596",
    "startBlock": "2147112368",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "compareWithNull(Lorg/h2/value/Value;ZLorg/h2/engine/CastDataProvider;Lorg/h2/value/CompareMode;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b80cbde6-8bb4-4f50-bb27-43fa7f12c8af",
    "startBlock": "69329693",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceSystem",
    "methodSignature": "setLevelFile(I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "dc54ba8b-b605-4b60-a2cf-22bf15828cbd",
    "startBlock": "1829032595",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "AlterTableAlterColumn",
    "methodSignature": "update()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "a34f0f58-e94b-46d0-a81a-56a9504554d4",
    "startBlock": "327868393",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "convertBytesToHex([BI)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "d03a15b9-9775-4318-889e-6fa780efe9af",
    "startBlock": "249372140",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileUtils",
    "methodSignature": "createDirectories(Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "29f718b0-1993-439f-8a3b-0db1d164a9d1",
    "startBlock": "933993331",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "SortedProperties",
    "methodSignature": "keys()Ljava/util/Enumeration;",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "5ad8f24e-d2a2-458f-a41d-c390a86b5feb",
    "startBlock": "1417450582",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVPrimaryIndex",
    "methodSignature": "<init>(Lorg/h2/engine/Database;Lorg/h2/mvstore/db/MVTable;I[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "40794412-ac45-480a-97cc-06b8a9940001",
    "startBlock": "70453857",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "readAllRows()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "cd5aff13-7dca-49ef-8ef7-897d5e82f0ef",
    "startBlock": "423235467",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "applyOffsetAndLimit()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "39b2805f-b43f-4325-b9c7-90832488fc67",
    "startBlock": "2127984558",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "AlterTableAddConstraint",
    "methodSignature": "tryUpdate()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "5dc17eb3-0ce3-4243-b433-ca46a2d426de",
    "startBlock": "165604078",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileUtils",
    "methodSignature": "readFully(Ljava/nio/channels/FileChannel;Ljava/nio/ByteBuffer;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "ccbf3a98-2ba5-4c27-b7b9-87b9c3b902d6",
    "startBlock": "1574760626",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableBase",
    "methodSignature": "getCreateSQL()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "2b916c2f-8176-4988-a50d-d5f9e0c09320",
    "startBlock": "1476168300",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "compareKeys(Lorg/h2/result/SearchRow;Lorg/h2/result/SearchRow;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "4f76b3f7-1bdf-4f09-b43c-8de52db0b861",
    "startBlock": "1994988343",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Permutations",
    "methodSignature": "<init>([Ljava/lang/Object;[Ljava/lang/Object;I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "cd735760-18ac-4b4f-9f3c-a02de8291e90",
    "startBlock": "252693090",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueDecimal",
    "methodSignature": "checkPrecision(J)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "a0c2dd6e-0054-40e2-a4ef-74d585bdf2a3",
    "startBlock": "1999732562",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowImpl",
    "methodSignature": "setValue(ILorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "671a908a-90e6-4a69-b48e-7ae3cb3b5069",
    "startBlock": "199917383",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "encodeLength(I)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "01179a2d-6bf5-478f-aa29-b326816ce014",
    "startBlock": "1828427548",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "checkRowCount(Lorg/h2/engine/Session;Lorg/h2/index/Index;I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "ae72c508-d458-46f5-ab49-6c41bbfcb663",
    "startBlock": "1247659928",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "<init>(Lorg/h2/mvstore/RootReference;Lorg/h2/mvstore/Page;ZI)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "c68897c5-0530-41dc-998f-76b9d0b270f8",
    "startBlock": "136897240",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FreeSpaceBitSet",
    "methodSignature": "markUsed(JI)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "c46fd60e-640a-4ce4-b558-b852dfa1cf9f",
    "startBlock": "1777845580",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Insert",
    "methodSignature": "prepare()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "67d7464e-d5a9-494b-b04f-ea55c75e7f2d",
    "startBlock": "190260061",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "isPersistent()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "39b773f4-b9d4-4f52-8989-6e4c037c7bec",
    "startBlock": "2140990488",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "append(Ljava/lang/Object;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "4ba2742f-92bc-44f6-80a6-c588ff6e1ce4",
    "startBlock": "2075042187",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueInt",
    "methodSignature": "get(I)Lorg/h2/value/ValueInt;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5bbf9216-6456-490f-aa76-6d90f62387a6",
    "startBlock": "847118768",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "IsolationLevel",
    "methodSignature": "allowNonRepeatableRead()Z",
    "decisionIndex": 1,
    "info": [
      "((FORBID_CREATION && AUTOCOMMIT) || (IFEXISTS && AUTOCOMMIT && !FORBID_CREATION))",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "((FORBID_CREATION && !AUTOCOMMIT) || (IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION))"
    ],
    "id": "02b1f131-5357-4647-a293-fca537713cf7",
    "startBlock": "2040988842",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "addRowTry(Lorg/h2/result/Row;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "b4f182b3-137e-45bb-8eb5-3ec089b25806",
    "startBlock": "2081203470",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileNio",
    "methodSignature": "truncate(J)Ljava/nio/channels/FileChannel;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "dc2a4fbd-8bc5-4fd1-a89c-0e7997d37f93",
    "startBlock": "1318600509",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "isRewritable()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "8deb9ffe-1be5-403c-a9cf-681f8b056fb1",
    "startBlock": "698779038",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "edu.cmu.cs.mvelezce.bench.c",
    "className": "BenchCThread",
    "methodSignature": "processNewOrder()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "3ffd96c4-5576-48e1-9e55-14ba9f504473",
    "startBlock": "1256688828",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "read(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "5847e2a0-3230-4756-a154-d28f24d10456",
    "startBlock": "1139539984",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$PageReference",
    "methodSignature": "<init>(JJ)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "677de9ac-15d0-4075-84bc-a96ff17aea85",
    "startBlock": "436345473",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "Comparison",
    "methodSignature": "getReversedCompareType(I)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "4ce88e11-7638-4b11-bc6c-31128c734d57",
    "startBlock": "364324410",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "setStatus(I)J",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "37df4967-4f23-4604-96de-47d2a54e3802",
    "startBlock": "2129912354",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "setOldestVersionToKeep(J)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "ad092485-a906-405a-8ebc-494d7b052b38",
    "startBlock": "163787146",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readFactor()Lorg/h2/expression/Expression;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "2ca44095-f9ff-43d4-8158-c048c6fddc37",
    "startBlock": "1441242081",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCondition",
    "methodSignature": "getMask(Ljava/util/ArrayList;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "78a828bd-582f-415a-b0b6-fcf543eca5a3",
    "startBlock": "976942844",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStoreTool",
    "methodSignature": "compactCleanUp(Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "97ba91da-59a9-4286-be63-ddbcb73cc7be",
    "startBlock": "2054121104",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "remapChildren(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "a7017481-bb58-4b86-ad08-155fe6b90aaa",
    "startBlock": "2141002500",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCondition",
    "methodSignature": "isEnd()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b54ff47f-6115-4d41-9986-dcc4adc40f79",
    "startBlock": "1488990995",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6536f23d-20c4-404e-b59c-d6d6b6c10327",
    "startBlock": "129186379",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "storeNow(JJ)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d0b105fd-2821-4238-9581-59bc3178ccb4",
    "startBlock": "1756670527",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "recalculateMemory()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "9554f868-7d5e-40bf-8bee-05fb54a7e520",
    "startBlock": "1649964280",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageFreeList",
    "methodSignature": "allocate(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "bc0f9a1a-5ab1-478d-af4d-be079e2085d5",
    "startBlock": "1914899904",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "addRowTry(Lorg/h2/result/SearchRow;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "b497df86-d8dd-4eeb-b890-db2d5deb670e",
    "startBlock": "1339873243",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "HashBase",
    "methodSignature": "reset(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)) || (!MV_STORE && (FORBID_CREATION || IFEXISTS)))",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d14e7492-f014-4c17-9b9d-4423beab3cc7",
    "startBlock": "1053580237",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "acceptChunkOccupancyChanges(JJ)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "002ac860-0a02-41c7-9715-a1380314eb7f",
    "startBlock": "2101929314",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "Trace",
    "methodSignature": "debug(Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "73be9bf9-252b-44dc-ab8c-886512c4907a",
    "startBlock": "1176120783",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "isLockedByCurrentThread()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "d88a0414-3007-4468-8497-7f045301ea77",
    "startBlock": "1011399667",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "unlock(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "65d112d0-c8eb-486a-806c-f02715afe8ff",
    "startBlock": "1229075741",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isIdle()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "2f1b600c-d56f-4dea-ac8f-0a3112ba9148",
    "startBlock": "290697947",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionColumn",
    "methodSignature": "getSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "c50769c6-0251-42b9-8a07-eb5e544ee7c6",
    "startBlock": "2116175659",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIfEqualOrTo()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "f52b72cd-e13f-4667-88ae-875dda95bdfc",
    "startBlock": "1623253847",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "beforeWrite()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e18203bc-6c52-474c-adaf-7e171fb33df5",
    "startBlock": "612873761",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isSeasonedChunk(Lorg/h2/mvstore/Chunk;J)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "8d267b21-ad95-4481-bd43-33ed3b5ebe7f",
    "startBlock": "845388580",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "SortedProperties",
    "methodSignature": "loadProperties(Ljava/lang/String;)Lorg/h2/util/SortedProperties;",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "be04621d-0d49-4f08-a5e7-4a1ef0488895",
    "startBlock": "433466536",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCursor",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "f061d240-cec6-45b1-9841-656bc1d2ae16",
    "startBlock": "157665145",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readTerm()Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "4a082b02-5955-4935-a9bf-1e24a4f887cb",
    "startBlock": "354120264",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "LobStorageBackend",
    "methodSignature": "init()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e4b01fa1-5a11-4e48-a65c-abe0664254ea",
    "startBlock": "1590560846",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "prepareLocal(Ljava/lang/String;)Lorg/h2/command/Command;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "e74a91b4-7731-4456-9f56-b6c27bf6028b",
    "startBlock": "1848199200",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Query",
    "methodSignature": "finishResult(Lorg/h2/result/LocalResult;JIZLorg/h2/result/ResultTarget;)Lorg/h2/result/LocalResult;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "4cb1e243-b966-4c9c-a529-3b54aac2420b",
    "startBlock": "1399497971",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readParameter()Lorg/h2/expression/Parameter;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "1f6e512e-3ac7-49e0-8e6a-75c3634928ff",
    "startBlock": "321180528",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "02e4998a-8cc4-4c30-b1b1-48a6340156e0",
    "startBlock": "1841950728",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "flushAppendBuffer(Lorg/h2/mvstore/RootReference;Z)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "0df03218-92e3-4394-9050-cecdd64a5de8",
    "startBlock": "1617253954",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "isEvacuatable()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "997aa197-3328-4ff5-a574-70177e5b4460",
    "startBlock": "513930869",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "AlterTableRename",
    "methodSignature": "update()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9f0ad0e6-06cc-4574-b363-a8ce8be37127",
    "startBlock": "499043082",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "updatePageAndLockedStatus(Lorg/h2/mvstore/Page;ZI)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "dd7f2436-54f8-41ee-82e9-01fb3d28c386",
    "startBlock": "1132795252",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "setLength(J)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "5cc096a2-922f-4ca4-ab12-81c8f0084bd7",
    "startBlock": "136749293",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "writeHead()V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "c71c8321-a827-46c4-994a-35214d35e417",
    "startBlock": "1830438571",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "Trace",
    "methodSignature": "info(Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ece6b6e6-f597-45f2-9c40-0df4762d1bcf",
    "startBlock": "39654699",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDelegateIndex",
    "methodSignature": "getColumnIndex(Lorg/h2/table/Column;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "54cee16a-9000-44b9-9059-8a9b58330011",
    "startBlock": "2056235731",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "Expression",
    "methodSignature": "writeExpressions(Ljava/lang/StringBuilder;[Lorg/h2/expression/Expression;Z)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "a629ff8c-f799-4b78-98ad-639d56989a60",
    "startBlock": "1674129838",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$Leaf",
    "methodSignature": "insertLeaf(ILjava/lang/Object;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "8fa40ea9-0f56-4553-9153-4b539ae718c6",
    "startBlock": "1548172423",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$IncompleteNonLeaf",
    "methodSignature": "writeUnsavedRecursive(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "21c6b27d-52e4-4d94-8547-51553029143e",
    "startBlock": "1511790584",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "DataType",
    "methodSignature": "isLargeObject(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "a3d024cf-4e69-48aa-b706-343ee6fb516c",
    "startBlock": "1152268249",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "getTargetFillRate()I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "2863394c-a5a6-4190-8ce4-c957057604f3",
    "startBlock": "1439521168",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "rollback()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "8cfcfa70-2b13-4beb-be97-4df6c0611185",
    "startBlock": "682853228",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "<init>(Lorg/h2/command/ddl/CreateTableData;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "b4b5a31a-51cd-48e6-8967-86d9ccc35982",
    "startBlock": "1524865791",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueStringFixed",
    "methodSignature": "trimRight(Ljava/lang/String;I)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e667ed49-ad0e-4a1b-917f-35ef545d43e9",
    "startBlock": "1608157218",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "getRowSize(Lorg/h2/store/Data;Lorg/h2/result/SearchRow;Z)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "ea91fe68-68b9-47a5-b9cd-59d90a786b1c",
    "startBlock": "273043196",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readExpressionWithGlobalConditions()Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "f369c6d6-e20f-4b23-836e-d742bcdf13bc",
    "startBlock": "2034997576",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "CommandWithColumns",
    "methodSignature": "generateSequences(Ljava/util/ArrayList;Z)Ljava/util/ArrayList;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e7983f29-5606-44a9-8951-2d67496068ce",
    "startBlock": "1377877888",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "Comparison",
    "methodSignature": "createIndexConditions(Lorg/h2/engine/Session;Lorg/h2/table/TableFilter;)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "d48ad60e-25ec-435d-a96c-144e5e58f099",
    "startBlock": "113872356",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "writeBack()V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "3af4f1e4-4b6f-4fa2-9933-abf3b26b2b72",
    "startBlock": "1366691079",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS",
    "methodSignature": "clear()V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "61be87de-04b2-40fc-83ca-dc1dd1f84ae9",
    "startBlock": "991687709",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "addLogPos(II)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "0af28994-ada5-4a88-9f05-43054d4a4742",
    "startBlock": "340720651",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "Trace",
    "methodSignature": "info(Ljava/lang/String;[Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "cb74f79a-645a-4067-a1e5-e91b50f8481c",
    "startBlock": "564751886",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FreeSpaceBitSet",
    "methodSignature": "allocate(IIIZ)I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "6fa05d4a-f2c5-4100-bbc9-9eb05db66077",
    "startBlock": "1568788712",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FreeSpaceBitSet",
    "methodSignature": "getMovePriority(I)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "ea4146d7-d743-4fbd-a9d9-bcf3afc7a002",
    "startBlock": "1371777126",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "DataType",
    "methodSignature": "isDateTimeType(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "b92f38f7-f035-4ee8-ba49-511442476a30",
    "startBlock": "1636150214",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "writeInBackground()V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "b6ab9ff3-15ef-440c-be8e-4e3b0219b979",
    "startBlock": "1263411086",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Query",
    "methodSignature": "sameResultAsLast(Lorg/h2/engine/Session;[Lorg/h2/value/Value;[Lorg/h2/value/Value;J)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "ee54f0b0-6c78-43ba-aebc-a51f492b5acd",
    "startBlock": "1909923365",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Update",
    "methodSignature": "update()I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "fcc20975-c268-458f-b130-f44c6d7ec2c5",
    "startBlock": "290947311",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "removeChild(I)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE || IFEXISTS || FORBID_CREATION || MV_STORE)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "8bb91d95-a2c1-4ab8-aa3d-081e6f4df0eb",
    "startBlock": "684832306",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "StringDataType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8252698a-825a-4f02-b5bd-11803fd4fa9b",
    "startBlock": "2032128432",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseColumnList(Lorg/h2/table/Table;)[Lorg/h2/table/Column;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "b2b8afdd-52f0-4fdd-a91d-bf2fcfc1e092",
    "startBlock": "410412044",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "operate(Ljava/lang/Object;Ljava/lang/Object;Lorg/h2/mvstore/MVMap$DecisionMaker;)Ljava/lang/Object;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "933072f1-9e24-4b8e-8fca-a4bd7ccff445",
    "startBlock": "520296852",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "e5a2c2c0-33b4-4084-a986-79b0fe6881b6",
    "startBlock": "1169499992",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "readChunkHeaderAndFooter(JI)Lorg/h2/mvstore/Chunk;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "6a2d744a-e185-474d-8f9d-556330cfa5ad",
    "startBlock": "1323805527",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Engine",
    "methodSignature": "createSessionAndValidate(Lorg/h2/engine/ConnectionInfo;)Lorg/h2/engine/Session;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "67b5f31d-216f-48d9-90a6-7dc8c5875141",
    "startBlock": "139710882",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex",
    "methodSignature": "needRebuild()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "d578fa4e-2ea7-406c-9fea-a66b32ee61a8",
    "startBlock": "1286292816",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.schema",
    "className": "Schema",
    "methodSignature": "getUniqueConstraintName(Lorg/h2/engine/Session;Lorg/h2/table/Table;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "db36f074-3581-47db-ab63-be971fb54aa9",
    "startBlock": "1018563934",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore$1",
    "methodSignature": "compare(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/Chunk;)I",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "e71354e5-21a5-443c-bac8-030eaaca2d21",
    "startBlock": "1686928524",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "getFillRate()I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "6c2f11e1-e657-4d32-8a39-c614f6999c2f",
    "startBlock": "415461677",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "quoteIdentifier(Ljava/lang/StringBuilder;Ljava/lang/String;Z)Ljava/lang/StringBuilder;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "1fc6ce35-8a9e-4796-a94c-6c9e816abad9",
    "startBlock": "1027964687",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseUpdate()Lorg/h2/command/dml/Update;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "9663ac19-b681-4135-9141-0ab5ec3ad31e",
    "startBlock": "1976413932",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ValueExpression",
    "methodSignature": "get(Lorg/h2/value/Value;)Lorg/h2/expression/ValueExpression;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "7d7b05ba-d856-4545-8920-1a15b1dffd57",
    "startBlock": "680503918",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "rollbackTo(Lorg/h2/engine/Session$Savepoint;)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "37706c81-b1a5-40b7-966b-6c65d655a8f5",
    "startBlock": "942851226",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "truncate(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d7e3b8ad-d702-4eab-a0df-a892440ae2a2",
    "startBlock": "1564730444",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "getChunksFillRate()I",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "817a142c-d28a-4398-a9e2-e885cf9650a1",
    "startBlock": "1580714323",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "isOk(Lorg/h2/expression/Expression;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "a2e287da-e971-4c62-af91-98fac0e8b776",
    "startBlock": "1251171987",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "parseMapValue(Ljava/lang/StringBuilder;Ljava/lang/String;II)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "97c6fccb-f32d-4bf2-b0fe-f675c04f96e3",
    "startBlock": "640570087",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexType",
    "methodSignature": "getSQL()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8731584e-297a-4b9e-8396-db098099b8cf",
    "startBlock": "1543298333",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "addRowTry(Lorg/h2/result/Row;)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "cc9f2e60-d176-4d96-a83a-725427105f2f",
    "startBlock": "2051475138",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Column",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "4560f0c2-3881-44a3-a61a-3b66f2f6450b",
    "startBlock": "2126124449",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "splitKeys(II)[Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "52c819de-dcc3-4032-a0ee-fb7ed3b35dce",
    "startBlock": "1478300491",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueDecimal",
    "methodSignature": "convertScale(ZI)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "06a28107-cf4f-4aec-923c-00cba808fdcf",
    "startBlock": "1165851779",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "checkSuspended(Lorg/h2/engine/Session$State;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "2ebf9c35-6185-4001-9451-f2942c32188d",
    "startBlock": "916967053",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "Parameter",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "845522ef-b637-45e3-a53c-24c90e4437b2",
    "startBlock": "1874827835",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "open(IILorg/h2/engine/ConnectionInfo;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "3cd86848-8416-4fa0-bd2e-04c63b63bb5e",
    "startBlock": "1321468037",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$PageReference",
    "methodSignature": "clearPageReference()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1fe28274-acfb-418d-8d31-919d437a1275",
    "startBlock": "35343299",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "fireAfterRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;Lorg/h2/result/Row;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "469c9573-91cb-4295-93c9-cee9a160d067",
    "startBlock": "1133375184",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueTimestamp",
    "methodSignature": "<init>(JJ)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f7ceb9fa-2163-467e-b854-eac0839f4bf1",
    "startBlock": "933139289",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "getChildPage(I)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "b01ba9a9-bb32-4b13-bfa1-d5df71d8b0a3",
    "startBlock": "2061849398",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIdentifierWithSchema2(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ad6343fa-5f8c-4e0a-91f2-1cfbe6d3ae0d",
    "startBlock": "300874555",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseDrop()Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "156ce222-b1cf-4216-b8a6-ae6bfa4ec88f",
    "startBlock": "190337863",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "gatherGroup(II)V",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "a6189cd1-6b24-49fa-b55a-3c3e61fdbd4a",
    "startBlock": "175755888",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "moveTo(Lorg/h2/store/fs/FilePath;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "663487fa-acbf-4a85-a9bb-c51b734ce86d",
    "startBlock": "1917556920",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageFreeList",
    "methodSignature": "allocate(Ljava/util/BitSet;I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "873c638b-70e8-4d07-a577-ce1ed630cf62",
    "startBlock": "559004349",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "TempFileDeleter",
    "methodSignature": "deleteAll()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "b2ec903d-1312-42cb-8bef-1e1c7b08a3af",
    "startBlock": "1726604415",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "moveTo(Lorg/h2/engine/Session;I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f1f38a2c-4aa5-44ca-8ee6-2d31ed177088",
    "startBlock": "1824508617",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueInt",
    "methodSignature": "checkRange(J)Lorg/h2/value/ValueInt;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "51c8b057-5bc1-4162-a39f-e2c527d650ad",
    "startBlock": "176298791",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "get(Lorg/h2/mvstore/Page;Ljava/lang/Object;)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "a873f9db-ce63-42db-8499-584bc885cca4",
    "startBlock": "653079367",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "ColumnNamer",
    "methodSignature": "getColumnName(Lorg/h2/expression/Expression;ILjava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "321c5270-1e80-43af-9638-3ce27ff95a83",
    "startBlock": "1990274676",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "unlock(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "1ee13d93-1bf5-403e-8283-87024b04d816",
    "startBlock": "1616025921",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "getCostRangeIndex([IJ[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;ZLorg/h2/command/dml/AllColumnsForPlan;)J",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "7af85eeb-51fb-4c1a-9594-3e8f8188127c",
    "startBlock": "1478478228",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Optimizer",
    "methodSignature": "calculateBestPlan()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "3eb2da34-9d7f-465b-a35f-48041164ea79",
    "startBlock": "486846793",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "getFooterBytes()[B",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "56678fce-65dc-4843-8941-878b18ea85ea",
    "startBlock": "1839490913",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "DbException",
    "methodSignature": "addSQL(Ljava/lang/String;)Lorg/h2/message/DbException;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "eacd6830-68e1-4750-acec-fd0fee53d7fe",
    "startBlock": "1024185453",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "Expression",
    "methodSignature": "addFilterConditions(Lorg/h2/table/TableFilter;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "28a1ef48-ae95-45d9-90bb-c74558c05574",
    "startBlock": "1158571337",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "fireRow()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "20b3f8b4-92f2-4233-9087-0fc4b24c2359",
    "startBlock": "907202916",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "writeVarLong(Ljava/nio/ByteBuffer;J)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "7110947c-acc2-4aaf-9c45-dd19dfdb5a1c",
    "startBlock": "587380008",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "ParserUtil",
    "methodSignature": "isSimpleIdentifier(Ljava/lang/String;ZZ)Z",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "50abea76-7cd8-4e10-b340-246474718ad2",
    "startBlock": "2104317835",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "writeUnsavedRecursive(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1109e649-da30-43da-9300-37e0b5068496",
    "startBlock": "1976196484",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "isPersistent()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "75396ba4-4289-45ce-ac4e-c16f5d96549d",
    "startBlock": "2019993510",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "openMap(Ljava/lang/String;Lorg/h2/mvstore/MVMap$MapBuilder;)Lorg/h2/mvstore/MVMap;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b2cc8227-eb30-4b21-91b6-32990783a193",
    "startBlock": "1224886539",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "readRow(Lorg/h2/store/Data;IZZ)Lorg/h2/result/SearchRow;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "680da542-6980-491f-bc8c-f52efe8c5842",
    "startBlock": "196289767",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "close(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "809f20fb-24fa-4b8d-8b2a-e61ca890bed8",
    "startBlock": "1745797042",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "CompareMode",
    "methodSignature": "getInstance(Ljava/lang/String;IZZ)Lorg/h2/value/CompareMode;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "78f393d9-9bcf-4e6e-b2b2-5d646f26c2da",
    "startBlock": "2138841371",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "doLock2(Lorg/h2/engine/Session;IZ)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "96db696d-837e-4e73-8ff7-9c955187e5ab",
    "startBlock": "1547844059",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "isLeafPosition(J)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && COMPRESS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !COMPRESS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "561e68da-257d-416f-8280-1dab1b23c497",
    "startBlock": "1593684101",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parsePrepared()Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "b17127fb-6368-4116-ae7c-b9a7532334d0",
    "startBlock": "691298232",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "copyWithGap(Ljava/lang/Object;Ljava/lang/Object;II)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "13922735-5c27-4f37-8b22-fa7ffd10e1f7",
    "startBlock": "1727038877",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "scrubMetaMap()V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "1c3f04f0-25bf-433d-b06c-aca986afb503",
    "startBlock": "1747907724",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "init(Lorg/h2/pagestore/db/PageBtree;Lorg/h2/result/SearchRow;Lorg/h2/pagestore/db/PageBtree;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "5027a4eb-369c-4f74-b584-9aa43d08f72c",
    "startBlock": "603136798",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "DropTable",
    "methodSignature": "<init>(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "f6af3aa9-2af4-4e2b-92c6-553ccb46dea1",
    "startBlock": "915121701",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "validateConvertUpdateSequence(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "5cb2abc8-6894-4461-8e19-83c1e8f03c20",
    "startBlock": "303672897",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "writeVarInt(Ljava/nio/ByteBuffer;I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "10848c22-b311-47f9-97ff-9b8f16a81b3b",
    "startBlock": "31779543",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "read(Ljava/nio/ByteBuffer;JLorg/h2/mvstore/MVMap;)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "6c531cd2-bbfd-4d1a-bc6d-9de0b7ebd7bd",
    "startBlock": "1557731544",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "removePage(J)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "0dddebb6-9904-4fa7-852c-6b378cd6fb59",
    "startBlock": "1118429535",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "getTrace()Lorg/h2/message/Trace;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "126f7196-5353-4264-a65c-7a2df1abcb34",
    "startBlock": "2061318787",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readExpression()Lorg/h2/expression/Expression;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "98f6131b-f9fb-4265-b8e1-1e594f5b8f80",
    "startBlock": "1190553177",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "RollbackDecisionMaker",
    "methodSignature": "decide([Ljava/lang/Object;[Ljava/lang/Object;)Lorg/h2/mvstore/MVMap$Decision;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "30447bce-ee4d-452a-b516-e6a91a248d5b",
    "startBlock": "518055761",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$Leaf",
    "methodSignature": "expand(I[Ljava/lang/Object;[Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "fdf44be6-9f5b-4fb1-8163-5752798a6bbb",
    "startBlock": "1365604646",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "onRollback(Lorg/h2/mvstore/MVMap;Ljava/lang/Object;Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "5c7b8ce4-2a96-43f6-bd14-2b7e1a11a652",
    "startBlock": "1089393705",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "User",
    "methodSignature": "checkRight(Lorg/h2/table/Table;I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "82abf45e-8c94-469b-a982-93ed510099e6",
    "startBlock": "128730006",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "getFromMap(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "718d8c72-5d4d-4617-b8af-37b55fd33107",
    "startBlock": "1029925131",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "cloneLobs([Lorg/h2/value/Value;)V",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "f5ed551b-a8ba-4776-a0c1-1060cab5dce1",
    "startBlock": "362298040",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "compact(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "368fe44b-faac-4f79-bb51-4b3e6022fe1a",
    "startBlock": "472825052",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "canOverwriteChunk(Lorg/h2/mvstore/Chunk;J)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "2d82993b-a5a2-4043-add8-27e3ee198439",
    "startBlock": "2072652940",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "createFile()Z",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "eaba7547-16f5-480f-9cc2-777bcfe04a55",
    "startBlock": "1270283525",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.analysis",
    "className": "DataAnalysisOperation",
    "methodSignature": "getGroupData(Lorg/h2/command/dml/SelectGroups;Z)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6e54eb43-3232-41a9-86f7-82c030718dfe",
    "startBlock": "1989310546",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileLock",
    "methodSignature": "lock(Lorg/h2/store/FileLockMethod;)V",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "fc41809e-8bdb-425b-a664-d4bf3d8b5c84",
    "startBlock": "9741347",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcResultSet",
    "methodSignature": "checkOnValidRow()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "4d3fef0c-5d50-4833-8a83-6b4d85563fa6",
    "startBlock": "816645918",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "MetaTable",
    "methodSignature": "generateRows(Lorg/h2/engine/Session;Lorg/h2/result/SearchRow;Lorg/h2/result/SearchRow;)Ljava/util/ArrayList;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9f4010b9-99bc-4dfc-8930-0a18dfaacc5d",
    "startBlock": "1420741234",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore$RemovedPageInfo",
    "methodSignature": "isPinned()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "dd086327-f80c-4be3-bc01-40531bfb665e",
    "startBlock": "395038722",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "SelectGroups",
    "methodSignature": "setCurrentGroupExprData(Lorg/h2/expression/Expression;Ljava/lang/Object;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "c1580ed4-785a-40e3-8256-d43fe59b3bbc",
    "startBlock": "343261889",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType$LongType",
    "methodSignature": "getMemory(Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "d10ea3a2-fd7e-4926-a156-028db2f6ce93",
    "startBlock": "1763466348",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "removeSession(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "a3dc616d-bd79-4363-9167-e245349020a3",
    "startBlock": "1417466327",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseIndexColumnList()[Lorg/h2/table/IndexColumn;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "1b6610dd-ab9e-4f6f-b54e-d19ce38f7366",
    "startBlock": "150946438",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "needFlush()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "ac77b96c-15f7-4210-92bf-e737e954c334",
    "startBlock": "2046211130",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "readVarIntRest(Ljava/nio/ByteBuffer;I)I",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "dfd3827d-6f99-4417-8c66-1656717c7c7e",
    "startBlock": "2053611494",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "throttle()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "d09efd67-9e2f-4f10-9926-30b90ae02ab9",
    "startBlock": "325920424",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceObject",
    "methodSignature": "debugCodeCall(Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "bc6c9a0e-7e92-45d3-a1b6-580f787da340",
    "startBlock": "8600640",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "areEqual(Lorg/h2/value/Value;Lorg/h2/value/Value;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "f09c07f6-3412-4890-af5a-c4c179474a25",
    "startBlock": "1828342558",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "closeIt()V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "7bc655b2-728b-4331-bfa2-67afd6733bf8",
    "startBlock": "2087737014",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowList",
    "methodSignature": "add(Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "c34fe3c5-306d-41f1-8004-922cf23a4e6e",
    "startBlock": "1105365697",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "setMaxMemory(I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!IFEXISTS && CACHE_SIZE && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !CACHE_SIZE && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "aa0ecee7-bd22-405e-a791-1dfe5756eda3",
    "startBlock": "1006905449",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "BinaryOperation",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "bf361c1b-4b25-4e68-8a3f-c52438edae5e",
    "startBlock": "1725511296",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "getBestPlanItem(Lorg/h2/engine/Session;[I[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;Lorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "4a513208-d407-46ef-86c1-cb1f121d53cb",
    "startBlock": "2017395690",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "findInsertionPoint(J)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1914810f-929c-42eb-8ac4-2b86bc85b90b",
    "startBlock": "922068816",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "SessionRemote",
    "methodSignature": "connectEmbeddedOrServer(Z)Lorg/h2/engine/SessionInterface;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "05bc8c55-423c-44fa-a192-7d91e0798d16",
    "startBlock": "546417932",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "setRowCountStored(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1a95e217-8841-4605-998c-ff31fb028519",
    "startBlock": "1944556703",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "getKey(Lorg/h2/result/SearchRow;JJ)J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "17fc2151-9a7b-4438-95cb-a2772ec880e1",
    "startBlock": "417236208",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "writeRowCount()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "93cb9350-f4d4-4517-963a-95edba1e3250",
    "startBlock": "1051521166",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueString",
    "methodSignature": "get(Ljava/lang/String;Lorg/h2/engine/CastDataProvider;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b3daa38f-83de-49f0-8d53-bcdd4066ff9d",
    "startBlock": "451035830",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseAlter()Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "60c27061-fd7b-470a-9ec1-5d52e96395a5",
    "startBlock": "826228455",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcResultSet",
    "methodSignature": "closeInternal()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "d15631dd-5cfe-4011-b0ec-4911c695795c",
    "startBlock": "1425987055",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "readPayLoad(Ljava/nio/ByteBuffer;)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "d7e8aec6-c2e6-410d-9884-ad4f62ca01a1",
    "startBlock": "1764710993",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueDecimal",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "94d69eb0-931e-4c6d-a491-e78ff771dd8c",
    "startBlock": "1035073160",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "readVarIntRest(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((PAGE_SIZE && IFEXISTS) || (!PAGE_SIZE && (IFEXISTS || !DEFRAG_ALWAYS)) || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d501fb4e-20e2-4897-9aae-85bfda2d9053",
    "startBlock": "950996357",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "doMaintenance(I)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "8fde48d6-1bf4-48e2-9ca7-d15f7e491e73",
    "startBlock": "449779742",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "<init>(Lorg/h2/jdbc/JdbcConnection;Ljava/lang/String;IIIZLjava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "e760f993-260b-438a-be7c-8d97d154b60e",
    "startBlock": "514202006",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "readFully(Ljava/nio/channels/FileChannel;JLjava/nio/ByteBuffer;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1816df59-5a97-4e46-b044-5e030166bcda",
    "startBlock": "1578753139",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "decodePageLength(I)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "790b00a8-372c-4fa4-8531-ab0e7ea01b66",
    "startBlock": "864821248",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "checkIndexColumnTypes([Lorg/h2/table/IndexColumn;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "6e92c919-5447-44c0-a774-779054622522",
    "startBlock": "1631210013",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType$IntegerType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "396c8b2d-bb1b-4a6e-bda1-087cc7cc4031",
    "startBlock": "598097486",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "init()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "6c543a19-2dd2-46a4-9ea4-f623bd138f3b",
    "startBlock": "997154093",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType$StringType",
    "methodSignature": "getMemory(Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "e52af480-a1b5-479a-ad27-d74d2b6e6370",
    "startBlock": "550002107",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "addFilterCondition(Lorg/h2/expression/Expression;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9937170f-5c35-4b75-aa8b-49109df9f6a9",
    "startBlock": "524078852",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "3e6e2767-dfb7-4b2a-9211-a86d5fb745eb",
    "startBlock": "1509220174",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore$RemovedPageInfo",
    "methodSignature": "createRemovedPageInfo(JZ)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "d20db029-700e-4484-8062-ff232e127f51",
    "startBlock": "34653566",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "ValueDataType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "39bd7a2e-5079-4fa5-b451-ed106d6b9e60",
    "startBlock": "1421798724",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "createStatement()Ljava/sql/Statement;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "6363b658-1c5f-4c5d-a7c3-276e9c5d2650",
    "startBlock": "2066427793",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileUtils",
    "methodSignature": "writeFully(Ljava/nio/channels/FileChannel;Ljava/nio/ByteBuffer;)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "78dfa13e-aeb1-4b22-a3fc-cf6abc137c0f",
    "startBlock": "439848763",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "CommandWithColumns",
    "methodSignature": "addConstraintCommand(Lorg/h2/command/ddl/DefineCommand;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9516e254-c443-4753-89f4-43c4f55d1870",
    "startBlock": "1925064858",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "AggregateDataDefault",
    "methodSignature": "add(Lorg/h2/engine/Database;Lorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "e6cc2c4f-7e1a-470b-b3c1-c0c4a3240814",
    "startBlock": "1843220592",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$Leaf",
    "methodSignature": "writeUnsavedRecursive(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "eea5199c-fb70-49ab-a421-a7975760144f",
    "startBlock": "232018910",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap$UncommittedIterator",
    "methodSignature": "fetchNext()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)"
    ],
    "id": "8ba3bd37-486e-4a82-a436-4601371d6bf4",
    "startBlock": "1933691905",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "checkRunOver(III)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "b527fcaf-8368-443a-a49b-a816c94a55e1",
    "startBlock": "1071730252",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readJoin(Lorg/h2/table/TableFilter;)Lorg/h2/table/TableFilter;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "1c33e317-8421-4eba-95ef-7234396e23d4",
    "startBlock": "1554935064",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "setInt(II)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "c5e77de9-d7a3-471d-a5f5-a1b53a42c00b",
    "startBlock": "738166758",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "DataType",
    "methodSignature": "isStringType(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "41e0b45a-458d-4024-a4fd-3501736aa672",
    "startBlock": "81607115",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "removeTemporaryLobs(Z)V",
    "decisionIndex": 1,
    "info": [
      "((FORBID_CREATION && AUTOCOMMIT) || (IFEXISTS && AUTOCOMMIT && !FORBID_CREATION))",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "((FORBID_CREATION && !AUTOCOMMIT) || (IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION))",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "19a737f1-384e-44df-9e88-768e143b3602",
    "startBlock": "1377637372",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseInsertGivenTable(Lorg/h2/command/dml/Insert;Lorg/h2/table/Table;)Lorg/h2/command/dml/Insert;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "ea9f11d0-c3bd-48c9-8b6d-3104badc007c",
    "startBlock": "1050486905",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "addTry(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "75c55b68-816e-4ea0-ba7a-046527c3d912",
    "startBlock": "693192214",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "AggregateDataCollecting",
    "methodSignature": "add(Lorg/h2/engine/Database;Lorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "7bb5b489-9334-4c21-a2f3-b703972db0ff",
    "startBlock": "2044103988",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "init()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "823acb7e-9a91-44d6-81b8-7ddad46c2cff",
    "startBlock": "1802605",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.security",
    "className": "SHA256",
    "methodSignature": "getHash([BZ)[B",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "6db90c07-89ae-4610-acc6-b61370cfca1f",
    "startBlock": "1381934638",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS",
    "methodSignature": "<init>(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Config;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "71944485-ee36-456a-be3d-da64c7ce2a4d",
    "startBlock": "1060681581",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Utils",
    "methodSignature": "scaleForAvailableMemory(I)I",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "866c4c0e-0118-46a4-840a-6ca2a545ab5e",
    "startBlock": "1817793776",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "prepare()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "c5229067-cde5-4f6c-b8be-8891ac4263a9",
    "startBlock": "55623482",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "logUndo()V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "5e205111-3886-4b92-80fc-0acfffbfe718",
    "startBlock": "1372593068",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "StringDataType",
    "methodSignature": "read(Ljava/nio/ByteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "a5ef5233-d20a-41b7-860c-c0ec6f58fad5",
    "startBlock": "1688314413",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "compactMoveChunks(Ljava/lang/Iterable;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "5689e762-c035-494e-97b7-5d923dc76b36",
    "startBlock": "1268379935",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "parseHexLong(Ljava/lang/String;)J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e1fd49f3-e9d5-426b-a849-25624eefd605",
    "startBlock": "1283089929",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "readStoreHeader()V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "e7972c15-d0b2-40c9-b27d-da165232a8a4",
    "startBlock": "2006920211",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Permutations",
    "methodSignature": "reverseAfter(I)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "4d8a34e7-e3fd-4557-a4cc-0ae2fc7dcd53",
    "startBlock": "376924781",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "seek(J)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f84f5f03-58a4-46cd-8b85-1ac5bba1acbd",
    "startBlock": "1958090153",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "canWriteInternal(Ljava/io/File;)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "e7bf2c96-a3bb-4fb6-8f61-51e3aa2dcc6b",
    "startBlock": "538129343",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "AllColumnsForPlan",
    "methodSignature": "add(Lorg/h2/table/Column;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "a1e740ee-9ca3-471a-aa6d-ff292d71ea48",
    "startBlock": "503159856",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "appendMap(Ljava/lang/StringBuilder;Ljava/lang/String;Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "93ca11fd-cf55-436d-8247-4246c06a6c17",
    "startBlock": "1796413709",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getAllTablesAndViews(Z)Ljava/util/ArrayList;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e0294263-0599-4763-8741-c63617afbe70",
    "startBlock": "422445593",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "readVarLong()J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((PAGE_SIZE && IFEXISTS) || (!PAGE_SIZE && (IFEXISTS || !DEFRAG_ALWAYS)) || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8df68f59-6c5d-4d12-a803-460fff06eedd",
    "startBlock": "1179172855",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "<init>(Lorg/h2/engine/ConnectionInfo;Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "9d812ea7-46a7-4927-8e7e-4582feff5124",
    "startBlock": "1013216912",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getPageStore()Lorg/h2/pagestore/PageStore;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "fe46ab94-7695-418a-89b9-dbe19f4ff51a",
    "startBlock": "292131080",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "writeFully(Ljava/nio/channels/FileChannel;JLjava/nio/ByteBuffer;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8890b851-494e-4e54-9f74-e44447571859",
    "startBlock": "1770798312",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Set",
    "methodSignature": "addOrUpdateSetting(Lorg/h2/engine/Session;Ljava/lang/String;Ljava/lang/String;I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "0cdfb9f0-df4f-463d-9b46-bc0719d2ce2d",
    "startBlock": "137490491",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.schema",
    "className": "Schema",
    "methodSignature": "getUniqueIndexName(Lorg/h2/engine/Session;Lorg/h2/table/Table;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "a4905795-4850-4e56-8833-e361a5c9a938",
    "startBlock": "1067360446",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "convertToDecimal()Lorg/h2/value/ValueDecimal;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "aeda13d3-3b1c-44d8-bd5b-c0d5f6060bfd",
    "startBlock": "518764051",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "VersionedValueType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "b5fcfda6-37dd-4dde-aa40-47762b6b5436",
    "startBlock": "21732879",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "getTimeAbsolute()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b30d4cd8-0ea1-4b46-b089-290a9554f4a4",
    "startBlock": "288350104",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "registerTableAsLocked(Lorg/h2/table/Table;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "d555dd4f-8447-43c1-b072-de3beb11f914",
    "startBlock": "2006904786",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "traceLock(Lorg/h2/engine/Session;ZLjava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "54e59221-abe8-48e4-bb1a-fa486b6f19fd",
    "startBlock": "1187377750",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowImpl",
    "methodSignature": "getByteCount(Lorg/h2/store/Data;)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "58af1029-31d3-4b78-90ca-0bf5f64653ba",
    "startBlock": "270549116",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVPrimaryIndex",
    "methodSignature": "remove(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "b8a91ff7-4dfd-44ad-ac17-31ffc0500401",
    "startBlock": "576182813",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap",
    "methodSignature": "set(Ljava/lang/Object;Lorg/h2/mvstore/tx/TxDecisionMaker;)Ljava/lang/Object;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)"
    ],
    "id": "2dcdd375-06d1-4e3d-af48-2ab46a4af84f",
    "startBlock": "2103466586",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Command",
    "methodSignature": "executeUpdate(Ljava/lang/Object;)Lorg/h2/result/ResultWithGeneratedKeys;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "8bd10566-2e2a-491e-aeea-82a5dfc55775",
    "startBlock": "1395961675",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "updateGroupAggregates(Lorg/h2/engine/Session;I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "9fe0bdbb-53a8-4895-b91d-447f10ed9ef6",
    "startBlock": "113508098",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "read(Ljava/nio/ByteBuffer;I)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "af284ffb-0e62-4ac3-8df0-ffe3adc78674",
    "startBlock": "194792558",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "MetaTable",
    "methodSignature": "createColumns([Ljava/lang/String;)[Lorg/h2/table/Column;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7cdb3db7-2b25-440e-88ec-c855e9c1e043",
    "startBlock": "847745146",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "LobStorageMap",
    "methodSignature": "removeAllForTable(I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "99bca38b-6779-4c52-bce5-77c25ebe8fe8",
    "startBlock": "473380917",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseColumn(Lorg/h2/table/Table;)Lorg/h2/table/Column;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "1f7e3c8a-8e57-4b1f-a586-22661094e9a7",
    "startBlock": "1703911642",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "setLogFirstPage(III)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "959a2a57-e512-4d6b-819b-c4f698a4fe9d",
    "startBlock": "490397621",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "RegularTable",
    "methodSignature": "isLockedExclusivelyBy(Lorg/h2/engine/Session;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "((!FORBID_CREATION && !IFEXISTS && MV_STORE) || (!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE))",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "221c4b94-871a-4547-a498-b70d2d101497",
    "startBlock": "1616033911",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "CommandWithColumns",
    "methodSignature": "changePrimaryKeysToNotNull(Ljava/util/ArrayList;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "91c7286a-acb2-4e5e-bb25-8ddfdc106304",
    "startBlock": "903782022",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "equalsTokenIgnoreCase(Ljava/lang/String;Ljava/lang/String;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "a94f55c5-47c2-479a-b4a9-16f15b0ca3a3",
    "startBlock": "1417670854",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowImpl",
    "methodSignature": "hasSharedData(Lorg/h2/result/Row;)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "b95bb399-80fe-4eab-bc76-3cbbd2b41066",
    "startBlock": "1669259232",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "replacePage(Lorg/h2/mvstore/CursorPos;Lorg/h2/mvstore/Page;Lorg/h2/mvstore/MVMap$IntValueHolder;)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "045ae05b-d019-47b0-9ee3-5de30d2b044c",
    "startBlock": "1300752371",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "setWriteDelay(I)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "d1fbf948-bb77-41dd-9db4-9502bf1d9f2e",
    "startBlock": "1456657011",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "remove(Lorg/h2/result/SearchRow;)Lorg/h2/result/SearchRow;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE || IFEXISTS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "e32d932a-7439-49a2-b19a-c5f23b8f1002",
    "startBlock": "393087726",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "flipCommittingTransactionsBit(IZ)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "2316eb7c-1c16-4126-b053-d617356ebf15",
    "startBlock": "1822492590",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceSystem",
    "methodSignature": "getTrace(I)Lorg/h2/message/Trace;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "fb1d6679-96c2-4ead-83e7-4f95d53ea329",
    "startBlock": "1048034883",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "getAggregatedValue(Lorg/h2/engine/Session;Ljava/lang/Object;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "94d42652-4619-4f23-87c3-800025fbbc8c",
    "startBlock": "2046046903",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "commit()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "bcfeaffd-d798-4cd9-85ed-5a014f4a1507",
    "startBlock": "1620187081",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStreamTrunk",
    "methodSignature": "getPageData(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b56e4e18-4d09-4d94-84f1-c6731ef36733",
    "startBlock": "855552633",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVPrimaryIndex",
    "methodSignature": "add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "69bc109c-bbeb-4f50-a065-7b9a49531b41",
    "startBlock": "1492241622",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "checkNotClosed()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "b5120bf1-39f4-4240-8630-5e87a0ad8934",
    "startBlock": "414700620",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "update(Lorg/h2/pagestore/Page;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "dfb5e9ca-7912-4949-bfe7-3d42392d8c9c",
    "startBlock": "712733554",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "newInputStream()Ljava/io/InputStream;",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "6e8639e7-6630-4785-8ea6-386a46ac2898",
    "startBlock": "382357503",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeCursor",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "1fc6ce20-fe39-4d4b-9c29-26db2a1e1dda",
    "startBlock": "654293262",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "DataType",
    "methodSignature": "getAddProofType(I)I",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "96682b05-3196-415b-a49d-610c36dc0931",
    "startBlock": "117578020",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileUtils",
    "methodSignature": "newDirectoryStream(Ljava/lang/String;)Ljava/util/List;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "2f9b9053-77a0-4802-8b9a-bea3428d56de",
    "startBlock": "1353807932",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "read()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "f503f255-c8c5-40c2-b492-4e16020b1b35",
    "startBlock": "1503623911",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "setTimestamp(ILjava/sql/Timestamp;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "df2b1829-053a-4e22-b81b-2741d3023291",
    "startBlock": "965741373",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readSimpleTableFilter(ILjava/util/Collection;)Lorg/h2/table/TableFilter;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "038d7466-5a66-4d26-a992-6d509c2a2aaa",
    "startBlock": "1018152372",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore$ArrayType",
    "methodSignature": "getMemory(Ljava/lang/Object;)I",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "e65bfc4b-50e5-4770-843f-d8aa42d5a4bb",
    "startBlock": "807018168",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "AbstractAggregate",
    "methodSignature": "mapColumnsAnalysis(Lorg/h2/table/ColumnResolver;II)V",
    "decisionIndex": -1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "1573e5a4-96a6-45f9-980d-4a58b7e64a8c",
    "startBlock": "1810057036",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "Page",
    "methodSignature": "insert([IIII)[I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "75eae9c4-8f4e-43c9-8f50-8bded0e7cf90",
    "startBlock": "1366172402",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "writeEnd()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b7de18ba-3c05-43cb-a99d-3de94e89070f",
    "startBlock": "373191336",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "findSchema(Ljava/lang/String;)Lorg/h2/schema/Schema;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0af10b55-725d-4c09-b122-78a218aaccae",
    "startBlock": "23225288",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "mayHaveNullDuplicates(Lorg/h2/result/SearchRow;)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "71bb3989-1c31-4d7a-b19d-505dfdab69ce",
    "startBlock": "338205809",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceSystem",
    "methodSignature": "write(ILjava/lang/String;Ljava/lang/String;Ljava/lang/Throwable;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "db612511-d042-4174-a625-e64327baed93",
    "startBlock": "5701922",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseColumnForTable(Ljava/lang/String;ZZ)Lorg/h2/table/Column;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ec2d68d2-e1b5-4944-960f-019b90ada6a3",
    "startBlock": "1024405188",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "getSpecialType2(CC)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "6ad022b5-b840-49e6-bc88-343eb310024f",
    "startBlock": "1720975162",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Plan",
    "methodSignature": "<init>([Lorg/h2/table/TableFilter;ILorg/h2/expression/Expression;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "97dcb402-a7fe-4c98-8c9c-6d5a5c75bb00",
    "startBlock": "1601423512",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "getPage(I)Lorg/h2/pagestore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "3f2005cd-f79c-4293-aa6e-b081a15ddde4",
    "startBlock": "969882590",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "openDatabase(IIZLorg/h2/engine/ConnectionInfo;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "18c56e45-44fe-49d3-b104-1479928d6a4c",
    "startBlock": "1274667771",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "flushAndGetRoot()Lorg/h2/mvstore/RootReference;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "98a358ce-4348-4440-b97f-12e5f36ccafa",
    "startBlock": "362023079",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataCursor",
    "methodSignature": "checkMax()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)"
    ],
    "id": "8a7881e0-2ef2-498e-86b2-9d3798a727cd",
    "startBlock": "1728623328",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "split(I)Lorg/h2/pagestore/db/PageBtree;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "fe890b72-6c17-4265-9c2c-ba46449f97bd",
    "startBlock": "961196100",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Prepared",
    "methodSignature": "needRecompile()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "b8a6590b-e156-4db4-8878-9700148185b5",
    "startBlock": "710039762",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "checkOpen()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "af3f7b2c-7355-4ea8-bce7-06328600c085",
    "startBlock": "1538221238",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "RegularTable",
    "methodSignature": "<init>(Lorg/h2/command/ddl/CreateTableData;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "8a3fb25e-0456-469d-8748-5eb2a572b0da",
    "startBlock": "318871424",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "memoryChange(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "ec9a4ef6-2ad5-4a59-94f0-552e6486b791",
    "startBlock": "1393215235",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex$MVStoreCursor",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "a8eb5a74-167d-4443-a037-69a2dd528fc4",
    "startBlock": "974111339",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "TypeInfo",
    "methodSignature": "getTypeInfo(I)Lorg/h2/value/TypeInfo;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "68036cf0-54da-4144-b7c2-583cd0918100",
    "startBlock": "121668767",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "WriteBuffer",
    "methodSignature": "ensureCapacity(I)Ljava/nio/ByteBuffer;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "bda2f6a7-8de0-4634-980f-6af73538a9c0",
    "startBlock": "7720782",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "getSpecialType1(C)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "c24191e1-89f6-4669-9114-bce8bad9c865",
    "startBlock": "2092907770",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVDelegateIndex",
    "methodSignature": "getColumnIndex(Lorg/h2/table/Column;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "82a29425-3c51-440f-8f68-ef55cc94116d",
    "startBlock": "70212305",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "done()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "32489ec5-e74f-4b4b-a038-112955a8950a",
    "startBlock": "532171317",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Prepared",
    "methodSignature": "setCurrentRowNumber(J)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "099279f0-0656-426f-85f9-8e1d0181a545",
    "startBlock": "162348772",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "rewrite(Lorg/h2/mvstore/Page;Ljava/util/Set;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "556606d4-abd6-461e-a187-7e714a5ca0bc",
    "startBlock": "1752313315",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "DbException",
    "methodSignature": "filterSQL(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "68e58b06-2161-437c-9326-b8d2a947f644",
    "startBlock": "1014058864",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "freeFileSpace(JI)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "35b3cff7-0b18-4432-b956-4c34ee112dbc",
    "startBlock": "1286626979",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "updateRowCount(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "327cc80e-20e6-4610-93e3-60adae123bfa",
    "startBlock": "1484150931",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readSum()Lorg/h2/expression/Expression;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "18931b63-1d81-48f1-a629-188fdb283e54",
    "startBlock": "681122702",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "addRow([Lorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "a2584a12-35b9-4812-ba7d-d39b124dd7be",
    "startBlock": "587181802",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "WriterThread",
    "methodSignature": "run()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "274e983b-ddd9-44ab-9f15-5f4696f85d44",
    "startBlock": "1743552484",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "ConditionAndOr",
    "methodSignature": "optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "aed09a91-2261-46c8-a2b2-0a6d7c7c1cb2",
    "startBlock": "2127376526",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "initialize(Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "e85cbdd6-7faa-4938-bddf-4b43110ebf86",
    "startBlock": "378134594",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "ConnectionInfo",
    "methodSignature": "getProperty(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9f15e5c8-ed1a-4555-9e12-ccff14535ef6",
    "startBlock": "2019426940",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "<init>(Lorg/h2/store/DataHandler;Ljava/lang/String;Ljava/lang/String;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "98f681a6-ccaf-4f45-b6a3-ec3f160dccbc",
    "startBlock": "1354500452",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "updateAgg(II)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6e781b0d-082b-427c-b060-6cec0a6380e0",
    "startBlock": "1975283620",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "addRow(Lorg/h2/result/SearchRow;Z)I",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "ac8362b7-b9a6-4f52-8869-f7f14d9aafd8",
    "startBlock": "821581491",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "allocatePage(Ljava/util/BitSet;I)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "4422eb21-bcbd-493c-a409-5beaa0dab904",
    "startBlock": "689102421",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "getSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "aa27704d-d13e-4168-9bb2-660a359b957e",
    "startBlock": "235839163",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "closeStore(ZJ)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "2f88a151-b7fd-4c5e-9821-c040f551c6bb",
    "startBlock": "1002227452",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCursor",
    "methodSignature": "find(Lorg/h2/engine/Session;Ljava/util/ArrayList;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "fb70723d-ffc1-42e0-81bd-f0055c5a5254",
    "startBlock": "2143471202",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isClosed()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "fb8c82b5-c201-4474-8eb2-218a9f3fa4cf",
    "startBlock": "1680542198",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseSelectExpressions(Lorg/h2/command/dml/Select;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "3e19bdf6-8443-4af6-a2ee-23d528eaf76e",
    "startBlock": "131535193",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "writeHead()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "e67e938f-479a-4847-9a39-11ecdfe5e0b7",
    "startBlock": "636242099",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "markTransactionEnd()V",
    "decisionIndex": 1,
    "info": [
      "((FORBID_CREATION && AUTOCOMMIT) || (IFEXISTS && AUTOCOMMIT && !FORBID_CREATION))",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "((FORBID_CREATION && !AUTOCOMMIT) || (IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION))"
    ],
    "id": "1241593b-dfdb-4587-9705-b900e1af17a5",
    "startBlock": "1555639907",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDelegateIndex",
    "methodSignature": "isFirstColumn(Lorg/h2/table/Column;)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e23e1cdb-e32f-4cf5-83dd-3d58ce9e0648",
    "startBlock": "443125943",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "hasRollback(J)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6cc48be5-9ae8-4407-8c01-1a6caa3d7805",
    "startBlock": "1514764310",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "UndoLog",
    "methodSignature": "getLast()Lorg/h2/engine/UndoLogRecord;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "50204269-5a7f-416f-8a20-4f6a968510e4",
    "startBlock": "22576084",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS$Segment",
    "methodSignature": "access(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Entry;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e06ea438-ec48-4fa9-84cd-998de12288da",
    "startBlock": "1376186889",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Plan",
    "methodSignature": "calculateCost(Lorg/h2/engine/Session;Lorg/h2/command/dml/AllColumnsForPlan;)D",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "31d4ee39-123b-4e0e-ba1a-d5c6cd6c5e6d",
    "startBlock": "772790252",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "Comparison",
    "methodSignature": "compare(Lorg/h2/engine/Database;Lorg/h2/value/Value;Lorg/h2/value/Value;I)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "c387efd0-b3e9-45b4-9673-e7c1289164c8",
    "startBlock": "1136245396",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "init()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "db90ac95-c391-4e93-b711-2f18c402c1e4",
    "startBlock": "1024557828",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "setTimeoutMillis(I)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6ae537d0-72c6-439f-a82b-518445aa2735",
    "startBlock": "86889884",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "close()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "e2e47613-517f-4597-9427-a42b01131804",
    "startBlock": "1386499884",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "ValueDataType",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9616ccc8-7dad-4d2d-a981-eafa49b6fda6",
    "startBlock": "1960400937",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "setChild(ILorg/h2/mvstore/Page;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "71eb451e-9318-4b6c-a0eb-0f592a5af0ed",
    "startBlock": "187298262",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "sysIdentifier(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "a8bae8f1-0b7c-4cbb-9348-072889139cb2",
    "startBlock": "930479847",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "getFirstFree(I)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "fdced69f-394e-412b-bb55-c6c140d59f5f",
    "startBlock": "357120209",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "containsUncommitted()Z",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "772bc66e-39f2-4a76-92f1-becdb76b4287",
    "startBlock": "1409383238",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Set",
    "methodSignature": "isTransactional()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "4e380b50-c396-449b-beb4-cb07e450d28f",
    "startBlock": "1864543269",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readPrecision()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "fd60e681-b4d1-4123-8706-2761eb608eeb",
    "startBlock": "1071280988",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionColumn",
    "methodSignature": "getValue(Lorg/h2/engine/Session;)Lorg/h2/value/Value;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b8b045b2-e95b-4f6c-b5ec-30b6e655e240",
    "startBlock": "1776330881",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "getVarIntLen(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "66c7a86e-07f8-487e-ba5b-7b237e05a793",
    "startBlock": "2125115658",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "TransactionCommand",
    "methodSignature": "update()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "d9701746-0753-4477-a99a-e298b3f75705",
    "startBlock": "1835181887",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "<init>(Lorg/h2/pagestore/db/PageStoreTable;ILjava/lang/String;[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;ZLorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "ea820952-222c-43fe-84d2-f9b913d6b69b",
    "startBlock": "1368041567",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.analysis",
    "className": "DataAnalysisOperation",
    "methodSignature": "mapColumns(Lorg/h2/table/ColumnResolver;II)V",
    "decisionIndex": -1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "5d086548-5506-4135-bfb2-38649594d5da",
    "startBlock": "1423471571",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "lock(Lorg/h2/engine/Session;ZZ)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "6188c52f-8c91-4c90-bb98-073631ffcd42",
    "startBlock": "55484372",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "markUsedTablesAsUpdated()V",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "9f939897-d74a-48c0-be69-06001c92b274",
    "startBlock": "2088759129",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "addToFront(Lorg/h2/util/CacheObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "7ab5bd34-fdf4-41cf-99d7-67f2d20ceded",
    "startBlock": "1295927785",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "trimLast()V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "0ec33941-1382-44bd-99cc-0c3fc47e1b14",
    "startBlock": "816421765",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "appendMap(Ljava/lang/StringBuilder;Ljava/util/HashMap;)Ljava/lang/StringBuilder;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b6ac87e7-6412-403c-92ab-8f4a1edd8d8a",
    "startBlock": "274142871",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseSelectUnion()Lorg/h2/command/dml/Query;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "1b4bef47-59c4-4c6d-bc65-2b6cf99947a9",
    "startBlock": "334469104",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePathDisk",
    "methodSignature": "size()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "18d82bdf-e9d1-475d-9b50-e2caddab0ca5",
    "startBlock": "1272479805",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "rebuildIndexBlockMerge(Lorg/h2/engine/Session;Lorg/h2/mvstore/db/MVIndex;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "3f5a5e9a-81b8-4e0d-991d-8b517aecf8f9",
    "startBlock": "1530800608",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "checkLiterals(Z)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "4e299b7b-a8a4-478f-aae5-37c38d2585e7",
    "startBlock": "1894614033",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "commit(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "0b539443-d770-4e51-8ea2-add472fa56c1",
    "startBlock": "312843043",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "read()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b6ae586d-f290-43e5-a34b-7cbde1d2dfe1",
    "startBlock": "1876769548",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "getVersion()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "45b64bbe-6360-4ab4-946f-df153450d2fc",
    "startBlock": "1860390884",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "getNextPage()Lorg/h2/pagestore/db/PageDataLeaf;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d4480c72-bd6c-430f-b015-8fa2f5651dc5",
    "startBlock": "570985780",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIf(Ljava/lang/String;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "121878e5-dd1b-47a3-9080-c78d6db9a01d",
    "startBlock": "1105157261",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcResultSet",
    "methodSignature": "get(I)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "2fd6d305-1530-42ac-ac4d-f1bde76a264a",
    "startBlock": "347523049",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readFilterAndOver(Lorg/h2/expression/aggregate/AbstractAggregate;)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "b51c738b-673b-4c89-8ea6-ccc19aff5ee2",
    "startBlock": "1461365269",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap$ContainsDecisionMaker",
    "methodSignature": "decide(Ljava/lang/Object;Ljava/lang/Object;)Lorg/h2/mvstore/MVMap$Decision;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "c9383f13-6b3a-4b54-b46a-25887ab1ecc5",
    "startBlock": "559257579",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "UndoLogRecord",
    "methodSignature": "isStored()Z",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "e9d62282-c8f7-40cd-a7af-63d6bfa5c443",
    "startBlock": "1117726977",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "CompareMode",
    "methodSignature": "compareString(Ljava/lang/String;Ljava/lang/String;Z)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "692de4ee-1856-4ec0-b23d-a9001a031c1b",
    "startBlock": "366705764",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType",
    "methodSignature": "switchType(Ljava/lang/Object;)Lorg/h2/mvstore/type/ObjectDataType$AutoDetectDataType;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "4af38213-ce21-456d-90de-df090c76759e",
    "startBlock": "466037843",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "read()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((PAGE_SIZE && IFEXISTS) || (!PAGE_SIZE && (IFEXISTS || !DEFRAG_ALWAYS)) || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "c7d961eb-d252-4f6b-ad6e-4e4bf5f532ec",
    "startBlock": "1103664384",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "initMetaTables()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "59d22977-5aab-478c-b1bb-cbec11859069",
    "startBlock": "1722724135",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableView",
    "methodSignature": "clearIndexCaches(Lorg/h2/engine/Database;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "141df3d1-e764-4864-9ff9-6b3b48d76299",
    "startBlock": "1328438594",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCursor",
    "methodSignature": "setIndex(Lorg/h2/index/Index;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "84513986-34bc-426d-8455-6532f1140015",
    "startBlock": "1230667731",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "writeChildren(Lorg/h2/mvstore/WriteBuffer;Z)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b4fe7cae-c5d9-4213-b0fd-0b88d1e24e89",
    "startBlock": "1966524625",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "insertNode(ILjava/lang/Object;Lorg/h2/mvstore/Page;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "73811653-a649-4aad-a79c-134cba28fbe4",
    "startBlock": "1348730562",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "rollback()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "0ce97a73-e096-43d7-9880-7f992f114463",
    "startBlock": "360362343",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "removeUnusableIndexConditions()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "ec48fb3a-4753-455a-b8ac-58009cec9072",
    "startBlock": "302606541",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "getRowFromVersionedValue(Lorg/h2/mvstore/db/MVTable;JLorg/h2/value/VersionedValue;)Lorg/h2/result/Row;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && COMPRESS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !COMPRESS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "d800774e-27bf-409f-a1da-52bba08f1e1d",
    "startBlock": "791170798",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readNonNegativeLong()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "79907814-4058-46c2-84cd-606ce1e56fb8",
    "startBlock": "1004871482",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "DateTimeUtils",
    "methodSignature": "convertScale(JIJ)J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "fa6877c3-44a0-4896-b785-31eb96c6bb60",
    "startBlock": "2039672100",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "getFileLengthInUse()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "73cd1e0f-5b83-4bb5-9b95-7647a52bed6f",
    "startBlock": "1558772929",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "readRow(Lorg/h2/store/Data;II)Lorg/h2/result/Row;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "05efb9a6-aff8-44d5-b31a-ac584552b30d",
    "startBlock": "1600844986",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Prepared",
    "methodSignature": "trace(JI)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "8577188a-5957-4a68-a36e-bbb16b2c8960",
    "startBlock": "1923105673",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "newStringMap()Ljava/util/HashMap;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "a4e36712-3716-4f5c-87a2-e54d225b9afd",
    "startBlock": "460877125",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore$ArrayType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;Ljava/lang/Object;)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "2be94519-fcc8-4172-9a0b-e702c593b6ac",
    "startBlock": "1456982785",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "collectUndoLogRootReferences()[Lorg/h2/mvstore/RootReference;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "219c7074-73b3-426b-897d-72dbfd959547",
    "startBlock": "972010558",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "addJoin(Lorg/h2/table/TableFilter;ZLorg/h2/expression/Expression;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "dc5de4c6-afa9-4d62-b9a2-b0c6fc722178",
    "startBlock": "901664343",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "reserve(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "64210510-f156-49db-80b4-18ea943f84a6",
    "startBlock": "1555605021",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.schema",
    "className": "Schema",
    "methodSignature": "getUniqueName(Lorg/h2/engine/DbObject;Ljava/util/Map;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "027ecc72-5a63-489c-b1bd-84ffdcc005c1",
    "startBlock": "1376333760",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseUpdateSetClause(Lorg/h2/command/dml/Update;Lorg/h2/table/TableFilter;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "624659d7-cdd7-4b9b-abe9-a0ed0a8e4bbc",
    "startBlock": "100252608",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTableEngine$Store",
    "methodSignature": "removeTemporaryMaps(Ljava/util/BitSet;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "86a7f9d5-3f31-486d-944b-92472c1c8b03",
    "startBlock": "599263725",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "isToken(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "49ea3554-e63a-4708-a63e-64c7590980f0",
    "startBlock": "1228867680",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "endTransaction(Z)V",
    "decisionIndex": -1,
    "info": [
      "((FORBID_CREATION && AUTOCOMMIT) || (IFEXISTS && AUTOCOMMIT && !FORBID_CREATION))",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "((FORBID_CREATION && !AUTOCOMMIT) || (IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION))",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "e25804e7-7703-48e7-8af8-113c31c4caa1",
    "startBlock": "1069434367",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isRewritable(Lorg/h2/mvstore/Chunk;J)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "e803bdf1-6661-4ee8-99df-c222c1cbf98b",
    "startBlock": "1017442975",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "SettingsBase",
    "methodSignature": "get(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "b6ad7642-910c-4401-9ef5-3be01f9af033",
    "startBlock": "1590456972",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "rewriteChunks(I)Z",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "59477759-cb53-4896-ade8-b32eec358666",
    "startBlock": "616891717",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "writeRowCount()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "d9136aef-5961-458c-9bb8-23bb6de8bd76",
    "startBlock": "704127989",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "User",
    "methodSignature": "setUserPasswordHash([B)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "1ad19627-f37e-4d32-ba0d-5c2b68afb359",
    "startBlock": "284037593",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "LobStorageBackend",
    "methodSignature": "prepare(Ljava/lang/String;)Ljava/sql/PreparedStatement;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ce0674c9-c279-4404-a694-bfe8e67453c1",
    "startBlock": "1248913366",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "accountForRemovedPage(IZJJ)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "48751514-99d3-4aba-bec5-5706867956db",
    "startBlock": "1117517206",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.schema",
    "className": "Schema",
    "methodSignature": "createTable(Lorg/h2/command/ddl/CreateTableData;)Lorg/h2/table/Table;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "5dd09d8a-2a6c-4806-abe0-c5c8ba49d655",
    "startBlock": "1280194910",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStreamTrunk",
    "methodSignature": "contains(I)Z",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "a1726734-f8f0-4dcf-9541-fbb6dfb59248",
    "startBlock": "572450088",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseDelete()Lorg/h2/command/dml/Delete;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "00e93290-207e-46c0-857b-7a83e6534bed",
    "startBlock": "1277769255",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "moveTo(Lorg/h2/engine/Session;I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "61ba0326-75b1-4bf1-8c59-c0ff091c1494",
    "startBlock": "247156978",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "compact(II)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "0077f691-f698-4e3f-a4d4-81324c9f0eab",
    "startBlock": "1747359058",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "recompileInvalidViews(Lorg/h2/engine/Session;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "de4bc71e-6f23-49d6-9b1d-4709ad637185",
    "startBlock": "2019675115",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "readVarLong(Ljava/nio/ByteBuffer;)J",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "3bc95e99-ceaf-4960-b6e9-0c1f58f20968",
    "startBlock": "1719658443",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.constraint",
    "className": "ConstraintUnique",
    "methodSignature": "getConstraintType()Lorg/h2/constraint/Constraint$Type;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "fac48e80-8654-4ca4-9cc9-867a99ccf864",
    "startBlock": "795337578",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "remove(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "e4b5e1eb-79b4-4a71-a1b4-f2db85e5c13b",
    "startBlock": "817168996",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "ValueDataType",
    "methodSignature": "compare(Ljava/lang/Object;Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "318c8e26-6916-44f3-ab7d-89b3056468f2",
    "startBlock": "271875588",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "logUndo(Lorg/h2/pagestore/Page;Lorg/h2/store/Data;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "80a5ebec-9168-480e-aa2d-cf6063de17d8",
    "startBlock": "7141654",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "compareRows(Lorg/h2/result/SearchRow;Lorg/h2/result/SearchRow;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "92406b19-e288-4251-b60f-a44832514e82",
    "startBlock": "526189724",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Column",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "b7b8257b-1595-4b57-9f88-3dbf55f752e7",
    "startBlock": "1827866553",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStoreTool$GenericDataType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "b2590c85-dc3b-4ba1-a96b-6ca64885d3d9",
    "startBlock": "2001154301",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$NonLeaf",
    "methodSignature": "writeChildrenRecursive(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d11ad7c3-590d-41cd-a43c-feeb1cf3540e",
    "startBlock": "1060152102",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIf(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "294ac5be-7abf-4d66-b119-14ef6b6cd2ff",
    "startBlock": "426465681",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "AlterTableAlterColumn",
    "methodSignature": "checkClustering(Lorg/h2/table/Column;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "a6a32a3a-8784-46a5-a351-749fc14e005c",
    "startBlock": "811606716",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "moveTo(Lorg/h2/engine/Session;I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "65c2323c-e34a-433c-bc8d-4ed50a56fd43",
    "startBlock": "2090681404",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "storePage()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "3588dd96-46bc-4ce7-957a-1942367e6954",
    "startBlock": "482927394",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "isSaved()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "631b7ecc-1bd3-40a4-b639-ad5d0ceb7e1a",
    "startBlock": "852977253",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "AggregateData",
    "methodSignature": "create(Lorg/h2/expression/aggregate/AggregateType;ZI)Lorg/h2/expression/aggregate/AggregateData;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "8b5c076a-4746-4c1e-a5c7-8198182c3e1f",
    "startBlock": "949345367",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap",
    "methodSignature": "getFromSnapshot(Ljava/lang/Object;)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)"
    ],
    "id": "a4283e2b-0112-4db1-b0f6-912cc2dea32b",
    "startBlock": "400766927",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "close(Lorg/h2/engine/Session;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "c2f7a1a5-4496-488d-aa9d-a20f31ce5351",
    "startBlock": "184423728",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "<init>(Lorg/h2/engine/ConnectionInfo;Z)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "b108a4fa-bb26-4307-922a-b416b369ca59",
    "startBlock": "1670122703",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getSessions(Z)[Lorg/h2/engine/Session;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "bba5fe33-6cb2-4dc8-83d1-77dbb9886cb3",
    "startBlock": "1117046896",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStoreTool$GenericDataType",
    "methodSignature": "read(Ljava/nio/ByteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "37572681-a891-4957-b425-5f33fb4f0040",
    "startBlock": "1776912940",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "free(IZ)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "5014397c-0eb0-4a29-bb07-4102a4b53c2a",
    "startBlock": "1435907852",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "handleUpgradeIssues()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "f0f7ddb8-d90d-4f7d-8f5c-2d5d84652982",
    "startBlock": "710452216",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "SelectGroups$Grouped",
    "methodSignature": "next()Lorg/h2/value/ValueRow;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "18813215-e7f5-4322-a670-76a2ac1f9028",
    "startBlock": "1873124172",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$Leaf",
    "methodSignature": "calculateMemory()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "59edf379-298c-4b88-a56e-f1f343bbdf08",
    "startBlock": "173961269",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType",
    "methodSignature": "getTypeId(Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "4e6ae63e-8163-4936-aedb-09299ced1f7c",
    "startBlock": "1716701908",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "updateData(Lorg/h2/engine/Session;Lorg/h2/expression/aggregate/AggregateData;Lorg/h2/value/Value;[Lorg/h2/value/Value;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "c1158f17-add3-42a7-96ba-b676f16f4d4b",
    "startBlock": "305262010",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "writeData()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8272d8bd-12e0-4bd1-aa34-be8f8564ea97",
    "startBlock": "959337682",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FilePath",
    "methodSignature": "get(Ljava/lang/String;)Lorg/h2/store/fs/FilePath;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)"
    ],
    "id": "55b7e5cf-3291-4d50-a036-6e266bd62bec",
    "startBlock": "1097023336",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "ConnectionInfo",
    "methodSignature": "getDbSettings()Lorg/h2/engine/DbSettings;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ecd79303-4326-4d2c-9776-73ac6c2f294b",
    "startBlock": "1703512755",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "isAfterLast()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b9266ebf-e349-4802-a3bd-388550855e13",
    "startBlock": "712701384",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "moveChunk(Lorg/h2/mvstore/Chunk;JJ)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "9b0a372b-bf19-4e7c-b21b-5b3dc62b8253",
    "startBlock": "1669510441",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS$Segment",
    "methodSignature": "find(JI)Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Entry;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "3ac71422-390a-47f3-b0d8-c50bc50d05af",
    "startBlock": "1108116420",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "setPageSize(I)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "8451119f-3687-4a4b-b38e-05a8f39cf1d1",
    "startBlock": "961442081",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "check()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "264a3e24-a128-4b9e-9928-dc1f1b2347f2",
    "startBlock": "138185644",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS",
    "methodSignature": "resizeIfNeeded(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Segment;I)Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Segment;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8d4647d7-9faf-4435-9f16-13e8d8f06840",
    "startBlock": "2129195260",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "isToken(Ljava/lang/String;)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "94ac8a11-915a-45fe-9b8f-cb48ae381478",
    "startBlock": "757245744",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "CommandContainer",
    "methodSignature": "update(Ljava/lang/Object;)Lorg/h2/result/ResultWithGeneratedKeys;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "f01f5445-d8d4-444d-91c3-b9358b94f585",
    "startBlock": "1480163887",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex",
    "methodSignature": "find(Lorg/h2/engine/Session;Lorg/h2/result/SearchRow;ZLorg/h2/result/SearchRow;)Lorg/h2/index/Cursor;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "68cc800c-d4d4-4c81-a759-24d8b1b50bb3",
    "startBlock": "1720066187",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "close(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "657bf971-b121-455f-ae58-a5a21c83fafb",
    "startBlock": "2086878771",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readConcat()Lorg/h2/expression/Expression;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "7c639d24-97f5-4343-87e5-9e25b58a197d",
    "startBlock": "2002138248",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Query",
    "methodSignature": "getParameterValues()[Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "84b87f26-7638-47e8-afa6-5483ae905176",
    "startBlock": "1524666439",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "compactFile(J)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "aa9417c3-162f-4acb-99f1-fb440fe5349e",
    "startBlock": "1866885420",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "ConditionAndOr",
    "methodSignature": "optimizeConstant(Lorg/h2/engine/Session;Lorg/h2/expression/Expression;ILorg/h2/expression/Expression;Lorg/h2/expression/Expression;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "f57fd437-0513-47ec-af48-1b14c2837c3c",
    "startBlock": "1300857951",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableBase",
    "methodSignature": "getMainIndexColumn(Lorg/h2/index/IndexType;[Lorg/h2/table/IndexColumn;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "39172dae-60a1-4d89-ba7c-5da6d5da3e0d",
    "startBlock": "1413164912",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "SmallLRUCache",
    "methodSignature": "removeEldestEntry(Ljava/util/Map$Entry;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "092329ab-129a-40d7-b51b-165530e14671",
    "startBlock": "215240666",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Command",
    "methodSignature": "filterConcurrentUpdate(Lorg/h2/message/DbException;J)J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "e0f0634c-137b-417e-8cc9-ca6f9f6680b5",
    "startBlock": "1126144882",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "writePage(ILorg/h2/store/Data;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d11bff46-6b4d-4220-a947-5c9bc6ec30cc",
    "startBlock": "323586378",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "mapCondition(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "052df681-3431-4a42-92d5-7203e42c15f7",
    "startBlock": "1006528972",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Optimizer",
    "methodSignature": "optimize(Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "cd040ebd-c36d-47a9-8a30-19659a281802",
    "startBlock": "875544028",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "write(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/WriteBuffer;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "bef3dbfc-94a4-4280-8bbf-a49a90be5f41",
    "startBlock": "593269779",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "VersionedValueUncommitted",
    "methodSignature": "<init>(JLjava/lang/Object;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "187e563f-dbbf-496e-bdbd-4752a76ba18c",
    "startBlock": "1660371181",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "deleteOldTempFiles()V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "93fc4e0f-09c7-4d4a-ab19-7e8c571217ff",
    "startBlock": "2033289314",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "compareTo(Lorg/h2/value/Value;Lorg/h2/engine/CastDataProvider;Lorg/h2/value/CompareMode;)I",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "b03ebb91-a692-428c-9024-11dbf3d79866",
    "startBlock": "1701083299",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntIntHashMap",
    "methodSignature": "remove(I)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b38eacd1-6e58-4f22-b92f-492e26dde1d0",
    "startBlock": "1501548748",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "AbstractAggregate",
    "methodSignature": "optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;",
    "decisionIndex": -1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "0e3f45c8-71a8-4343-b670-a3b4f9ff5e3d",
    "startBlock": "509975623",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDelegateIndex",
    "methodSignature": "<init>(Lorg/h2/pagestore/db/PageStoreTable;ILjava/lang/String;Lorg/h2/index/IndexType;Lorg/h2/pagestore/db/PageDataIndex;ZLorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "2d3e82fa-bee9-45c8-8d51-61b4e3cba74b",
    "startBlock": "1994307255",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "tryLock(Lorg/h2/mvstore/RootReference;I)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "798d7a7e-36de-4837-8501-2c02e575ad7e",
    "startBlock": "1855307811",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "allocatePage()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "695efab4-500a-44b9-b1e7-0e8136ee8953",
    "startBlock": "878716485",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCursor",
    "methodSignature": "getSearchRow(Lorg/h2/result/SearchRow;ILorg/h2/value/Value;Z)Lorg/h2/result/SearchRow;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "3e2b6989-6231-4ad1-b8ea-661b07c76afd",
    "startBlock": "1509404211",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "addUndo(ILorg/h2/store/Data;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "91281ffb-047e-4e4c-964b-53ceeaed93d2",
    "startBlock": "81649319",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "unlockAll()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "3de518c9-b4c6-4a2a-9970-bbdd15a6a3d8",
    "startBlock": "1632681585",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "getConfigParam(Ljava/util/Map;Ljava/lang/String;I)I",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)"
    ],
    "id": "87856151-a771-481c-a500-f2ba974776c9",
    "startBlock": "2006364602",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "VersionedValueCommitted",
    "methodSignature": "getInstance(Ljava/lang/Object;)Lorg/h2/value/VersionedValue;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "e52c36d8-70c3-4162-ab4e-d503699cf9cf",
    "startBlock": "1938393770",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "doneWithParameters()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "a19bb6fe-2cfe-4bae-abb1-b052fe724960",
    "startBlock": "2056465411",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "readOrCreateRootPage(J)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "c8bca4c6-c1da-4490-bf97-98d15b8b40ed",
    "startBlock": "1451785359",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "gatherSnapshots(Ljava/util/HashSet;ZLjava/util/Map;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "558fca83-672c-4e5d-8098-26d0b6081656",
    "startBlock": "1151623001",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "Trace",
    "methodSignature": "isEnabled(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "c5078505-3568-4acb-9cdd-6d84814924b6",
    "startBlock": "2066997024",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "checkpoint()V",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE)"
    ],
    "id": "213073bb-28f4-436a-a8b2-2ccdbf3a1fb5",
    "startBlock": "923732634",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Optimizer",
    "methodSignature": "calculateBruteForceAll()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "4e9ff70e-346c-473f-8b28-fec089e55858",
    "startBlock": "791639175",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "getAllChanged()Ljava/util/ArrayList;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "71b61d46-6f80-4372-a244-fed5f2ea45e4",
    "startBlock": "1100746446",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "updateRows(Lorg/h2/command/Prepared;Lorg/h2/engine/Session;Lorg/h2/result/RowList;)V",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "4a39a28c-341b-4142-a93d-62f66df1ae4a",
    "startBlock": "1670978809",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "addChildTry(Lorg/h2/result/SearchRow;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "72a51864-81fb-4849-b5ab-9ad3eac58314",
    "startBlock": "1638408450",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "<init>(Lorg/h2/engine/Database;Lorg/h2/expression/function/FunctionInfo;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "49f708e5-d7bc-4b28-9411-f208c3cce698",
    "startBlock": "1674249146",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore$2",
    "methodSignature": "compare(Lorg/h2/mvstore/Chunk;Lorg/h2/mvstore/Chunk;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "8c27440d-3f04-43f1-a534-d60988e5743e",
    "startBlock": "343785609",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "traceLock(Lorg/h2/engine/Session;ZLorg/h2/mvstore/db/MVTable$TraceLockEvent;Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "8f57059f-7ec2-49b3-bd02-3d52018a77b7",
    "startBlock": "615921392",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "DataType",
    "methodSignature": "isIntervalType(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "86d83b22-bb1e-48f5-8837-6d3c8f3376df",
    "startBlock": "326728569",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "ValueDataType",
    "methodSignature": "getMemory(Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "1878e3fb-1ebb-490b-9575-a06911cbbff4",
    "startBlock": "467176084",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "unlockMetaDebug(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "((FORBID_CREATION && AUTOCOMMIT) || (IFEXISTS && AUTOCOMMIT && !FORBID_CREATION))",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "((FORBID_CREATION && !AUTOCOMMIT) || (IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION))",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "9ab6a962-58eb-49e2-8a31-44c87e861a6a",
    "startBlock": "885814799",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "cache(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "3affc1a2-1939-4ef7-acde-af709a55fa65",
    "startBlock": "435087753",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "<init>(Lorg/h2/expression/aggregate/AggregateType;[Lorg/h2/expression/Expression;Lorg/h2/command/dml/Select;Z)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "e7bd46b1-fc14-4bc2-9502-4bf34042e014",
    "startBlock": "1274784860",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseQuerySub()Lorg/h2/command/dml/Query;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "c7e6cf80-d5ba-44bd-a24f-b14f016190e9",
    "startBlock": "1340772584",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "688d18e2-a06f-4e72-a9cb-ba34e42252a8",
    "startBlock": "1577070815",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "<init>(Lorg/h2/pagestore/db/PageStoreTable;I[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;ZLorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "3f643377-23f5-47b0-b94f-179c35eb9a62",
    "startBlock": "478373165",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "shrinkFileIfPossible(I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1a9586ea-6be6-41c4-a155-678c0475fc13",
    "startBlock": "1583096453",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "isLeaf()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "21af59c0-9f48-4877-9730-f09f7939301e",
    "startBlock": "442790822",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcStatement",
    "methodSignature": "checkClosed(Z)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "763b9e92-a27b-44d3-af38-d94153610c61",
    "startBlock": "40376496",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap$TMIterator",
    "methodSignature": "registerCurrent(Ljava/lang/Object;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "189e5011-cbd3-416c-801e-958dc1579fc2",
    "startBlock": "2092019621",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "commit(Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "d342f794-7533-45a2-a2d5-867da17c19db",
    "startBlock": "482879789",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readTableColumn(Lorg/h2/table/TableFilter;)Lorg/h2/table/Column;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0b2d9869-fdbc-4d75-904d-f25da1857e16",
    "startBlock": "1316194623",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "CommitDecisionMaker",
    "methodSignature": "selectValue(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/value/VersionedValue;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "74574e7a-52e8-40c0-afb1-cf789cb642cb",
    "startBlock": "631338835",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "ConditionAndOr",
    "methodSignature": "addFilterConditions(Lorg/h2/table/TableFilter;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "85d5eddd-a47b-4c0a-a7dd-b73c3eae23b9",
    "startBlock": "682378997",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "readVarInt()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((PAGE_SIZE && IFEXISTS) || (!PAGE_SIZE && (IFEXISTS || !DEFRAG_ALWAYS)) || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "db900b7e-1407-4dce-81b9-0ed7235a47e5",
    "startBlock": "833455388",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "LobStorageBackend",
    "methodSignature": "reuse(Ljava/lang/String;Ljava/sql/PreparedStatement;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "cd005bba-5eca-4e4c-85b8-6061f83da4c6",
    "startBlock": "1127676710",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "addUndoLogRecord(IJ[Ljava/lang/Object;)J",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "b83b4921-71d9-4b28-864e-c88ec0341ae1",
    "startBlock": "1245286779",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "nextPage(Lorg/h2/pagestore/db/PageBtreeCursor;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "df9fbe84-d8f8-4231-9708-ad72d99508ce",
    "startBlock": "20453179",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "trace(Ljava/lang/String;Ljava/lang/String;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "51027ccb-57f6-46b2-9690-ceab065438d1",
    "startBlock": "157778799",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "getCompareType(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "1218f8f6-d2ca-4a3e-ab6c-e46111acaa80",
    "startBlock": "724727531",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseColumnWithType(Ljava/lang/String;Z)Lorg/h2/table/Column;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "fe7482bf-49e7-4d80-a53f-7ea91ef55c0b",
    "startBlock": "1830700518",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "Comparison",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "f35b460e-1c8d-4e09-bace-e09db4ad57ed",
    "startBlock": "152457622",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isBackgroundThread()Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !DEFRAG_ALWAYS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && FORBID_CREATION && MV_STORE)"
    ],
    "id": "d0f348fe-ae0f-4f89-93cd-0dc7f6f0aba4",
    "startBlock": "852426793",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FreeSpaceBitSet",
    "methodSignature": "getProjectedFillRate(I)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "316a9b57-15c8-4cf0-9481-3a65a301e170",
    "startBlock": "96519922",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "composeState(IJZ)J",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "45590c2a-b574-4ded-b187-b23bc706522f",
    "startBlock": "236020698",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "get(I)Lorg/h2/util/CacheObject;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "accd20b3-b510-4edb-9182-4b93d6027921",
    "startBlock": "1036693191",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "ConditionAndOr",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "9ea7543e-a8ca-4aa6-832a-be1579c55571",
    "startBlock": "675679153",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "binarySearch(Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "2e77ae35-3005-46b2-9f14-84073e91330f",
    "startBlock": "1630106906",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "isPageRemoved(J)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "216fb9a9-8d53-4144-85d9-ed21ce15c955",
    "startBlock": "2124534978",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceSystem",
    "methodSignature": "writeFile(Ljava/lang/String;Ljava/lang/Throwable;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "b4e91b69-646d-4827-8232-1d989260c61c",
    "startBlock": "2064990985",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "copyExcept(Ljava/lang/Object;Ljava/lang/Object;II)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "8deb695d-be5c-482d-9e7b-de3153bad81b",
    "startBlock": "1112817737",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "registerTransaction(IILjava/lang/String;JIILorg/h2/mvstore/tx/TransactionStore$RollbackListener;)Lorg/h2/mvstore/tx/Transaction;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "37b4c64f-a553-4ffb-abd3-6c439c1550ea",
    "startBlock": "1328502085",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "allocateObjectId()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "d18b995d-fa9e-4c81-8e64-bc9be3074893",
    "startBlock": "560057865",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "getColumnIndex(Lorg/h2/table/Column;)I",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "a3342df1-9e5c-4ba3-bceb-f756dd5eeb22",
    "startBlock": "455418087",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileLock",
    "methodSignature": "run()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !FILE_LOCK && !FORBID_CREATION)",
      "(IFEXISTS && FILE_LOCK && FORBID_CREATION)",
      "(IFEXISTS && FILE_LOCK && !FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && FORBID_CREATION)",
      "(IFEXISTS && !FILE_LOCK && !FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)",
      "(!IFEXISTS && !FILE_LOCK && FORBID_CREATION)",
      "(IFEXISTS && !FILE_LOCK && FORBID_CREATION)"
    ],
    "id": "1bf94693-18f0-4da0-a514-08eb9cd2dbcd",
    "startBlock": "2136594180",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "setWriteVersion(J)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "a9ecb4df-09fe-4a3c-9177-3b65f44c010f",
    "startBlock": "1186984840",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Prepared",
    "methodSignature": "checkParameters()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0c55a9a3-7a6b-4a07-bc4b-8a68413f5314",
    "startBlock": "1527301809",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "HashBase",
    "methodSignature": "checkSizePut()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "2253c35b-61ac-4423-ad4c-20ac89b34545",
    "startBlock": "1785178257",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "getBestPlanItem(Lorg/h2/engine/Session;[Lorg/h2/table/TableFilter;ILorg/h2/command/dml/AllColumnsForPlan;)Lorg/h2/table/PlanItem;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "62fe7980-4442-4042-b61e-02bdf8133d73",
    "startBlock": "199024960",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcResultSet",
    "methodSignature": "isOnValidRow()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "16104a66-7790-4f8c-9efa-7aeeff37817c",
    "startBlock": "1668638928",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "remove(I)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "3f5fb9b3-fc2b-4b94-9e41-90f8d9ba4724",
    "startBlock": "792756779",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "open()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "b00f635e-4974-48d2-b057-c78a77d231dc",
    "startBlock": "962711322",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "analyzeIfRequired(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!AUTOCOMMIT && ANALYZE_AUTO && !IFEXISTS && !FORBID_CREATION)",
      "(!AUTOCOMMIT && !ANALYZE_AUTO && !IFEXISTS && !FORBID_CREATION)",
      "(!IFEXISTS && ANALYZE_AUTO && FORBID_CREATION)",
      "(IFEXISTS && !ANALYZE_AUTO && !FORBID_CREATION)",
      "(IFEXISTS && ANALYZE_AUTO && !FORBID_CREATION)",
      "(AUTOCOMMIT && !ANALYZE_AUTO && !IFEXISTS && !FORBID_CREATION)",
      "(IFEXISTS && ANALYZE_AUTO && FORBID_CREATION)",
      "(AUTOCOMMIT && ANALYZE_AUTO && !IFEXISTS && !FORBID_CREATION)",
      "(IFEXISTS && !ANALYZE_AUTO && FORBID_CREATION)",
      "(!IFEXISTS && !ANALYZE_AUTO && FORBID_CREATION)"
    ],
    "id": "cebbac57-d6a3-4cac-b650-2ec467946b6e",
    "startBlock": "1710891545",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "UndoLog",
    "methodSignature": "add(Lorg/h2/engine/UndoLogRecord;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "b1b92261-bb95-4734-a5b9-598f9038efe1",
    "startBlock": "310350600",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "getCache(Lorg/h2/util/CacheWriter;Ljava/lang/String;I)Lorg/h2/util/Cache;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "bdedc0bf-822f-483d-9ed4-87b52f1c660d",
    "startBlock": "1675508227",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex",
    "methodSignature": "convertToSearchRow(Lorg/h2/value/ValueArray;)Lorg/h2/result/SearchRow;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "b822f361-d9a7-445c-9134-3f848b8bada9",
    "startBlock": "1447743369",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "MetaTable",
    "methodSignature": "getTablesByName(Lorg/h2/engine/Session;Ljava/lang/String;)Ljava/util/ArrayList;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "47923326-e514-49d2-b79c-3487bb2cc0f5",
    "startBlock": "1504557009",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "<init>(Lorg/h2/table/Table;ILjava/lang/String;[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "126c6105-681b-4a47-a97b-453fd99be575",
    "startBlock": "1923803010",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "close()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "ea15e823-bdcd-4dc6-968d-94e79c4babe8",
    "startBlock": "1230784931",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "addRow(Lorg/h2/result/SearchRow;)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "be407aba-b1f3-4f9e-ad17-9fab49673546",
    "startBlock": "2041316326",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "getGroupDataIfCurrent(Z)Lorg/h2/command/dml/SelectGroups;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "d9bd54ea-de9c-4b64-af4f-050542fbab90",
    "startBlock": "653954471",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "checkOpen(I)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "23fbb469-2970-4df6-8ca9-b97b9382bf0e",
    "startBlock": "167938418",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "markAsRemoved()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b2bd7056-937f-4778-8fe9-76f0eaaf38f8",
    "startBlock": "1242432640",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "convertToInt(JLjava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "a35f77a8-a5a7-4504-a8d8-82bc7b65e3e3",
    "startBlock": "476270588",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "increaseFileSize(I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE)"
    ],
    "id": "4d80d7b6-3342-47cb-a060-2933000234c0",
    "startBlock": "641434648",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex",
    "methodSignature": "checkUnique(Lorg/h2/mvstore/tx/TransactionMap;Lorg/h2/value/ValueArray;J)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "5c1f3b5e-c92e-48a9-8f4a-e76df6b849a5",
    "startBlock": "1175664188",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getTableOrViewByName(Ljava/lang/String;)Ljava/util/ArrayList;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "00fa99c0-cbdf-4c2a-9a1a-6f8a34341a75",
    "startBlock": "1018539757",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LazyResult",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "4953f673-8e2e-450f-abd6-d3dd1c5fa982",
    "startBlock": "1556627249",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "trimSubstring(Ljava/lang/StringBuilder;Ljava/lang/String;II)Ljava/lang/StringBuilder;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "72723042-debf-4c7f-8f8f-e16ce1d8eb2e",
    "startBlock": "612364322",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "createFunction(Lorg/h2/engine/Database;Lorg/h2/expression/function/FunctionInfo;[Lorg/h2/expression/Expression;)Lorg/h2/expression/function/Function;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "d4e9d150-a599-4289-add5-240dc0b0d8d7",
    "startBlock": "1187032365",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseCreateTable(ZZZ)Lorg/h2/command/ddl/CreateTable;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "210ba251-9f64-4831-bad0-a858b9d9614f",
    "startBlock": "557179181",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "getMemory()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "2298d483-42e6-4c29-8141-0ec4c7db7856",
    "startBlock": "1573874170",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "User",
    "methodSignature": "getCreateSQL(Z)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "82a6a145-b47b-4899-9208-1b7015cc3f5a",
    "startBlock": "1572031651",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "isLocked()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "eff9c35f-7d43-455c-b93a-a0dcd5b931c9",
    "startBlock": "217658289",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "createLeaf(Lorg/h2/mvstore/MVMap;[Ljava/lang/Object;[Ljava/lang/Object;I)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "fa8c01d7-fce5-4406-8516-6c815c6212e2",
    "startBlock": "2147205383",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "fireConstraints(Lorg/h2/engine/Session;Lorg/h2/result/Row;Lorg/h2/result/Row;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6a03be57-9151-45d2-b60b-ffd78c5a5f7e",
    "startBlock": "323706190",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "remove(J)Z",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "7cfa311e-4f35-4842-948f-9119d72d498e",
    "startBlock": "1262459942",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "find(Lorg/h2/pagestore/db/PageBtreeCursor;Lorg/h2/result/SearchRow;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "b80497fb-e010-4624-8a76-fab930b54034",
    "startBlock": "502005490",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "removeOldIfRequired()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "fbc9bd7e-a44f-4adb-b180-ed06bb21cbc1",
    "startBlock": "761520658",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "markStatementEnd()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "183f7ada-ca9f-46cd-8b33-3ea319545e95",
    "startBlock": "2058190473",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "setCacheSize(I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "c8d99980-6020-4086-865a-932a140207cc",
    "startBlock": "1492410526",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "JSR310Utils$WithTimeZone8",
    "methodSignature": "epochSecondsForCalendar(J)Ljava/time/Instant;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "bc5846d3-8b9a-480b-953f-4a280f7ed7fb",
    "startBlock": "1622074570",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "checkPowerOff()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "31e1b10d-52b4-4403-bdd8-64d70eaf4963",
    "startBlock": "650062415",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseEndOfQuery(Lorg/h2/command/dml/Query;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "bb607d4f-97a0-4704-8f28-105bb449c693",
    "startBlock": "30560994",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isOpenOrStopping()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "8bd68e75-7424-44fd-816e-9e9104aa22fa",
    "startBlock": "1396015973",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "IndexColumn",
    "methodSignature": "wrap([Lorg/h2/table/Column;)[Lorg/h2/table/IndexColumn;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "adcc39cc-ae46-4967-974a-3bface58dc82",
    "startBlock": "1077607480",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionColumn",
    "methodSignature": "mapColumns(Lorg/h2/table/ColumnResolver;II)V",
    "decisionIndex": -1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "90d9c962-8373-4f9e-97e0-4c0bb4f71985",
    "startBlock": "1647641627",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueDecimal",
    "methodSignature": "get(Ljava/math/BigDecimal;)Lorg/h2/value/ValueDecimal;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "4928c7bd-1636-46bd-8887-e0d684c6b980",
    "startBlock": "1007561968",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "isOpen()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "55b26cbe-3e11-438f-bf5a-6c4f41ae28b5",
    "startBlock": "1198994959",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readExpressionOrDefault()Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "8bbc3a85-5704-4041-b824-c11af9283b18",
    "startBlock": "1315728456",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "flush()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e3524541-7414-43b3-b82f-e83f45df971b",
    "startBlock": "481658063",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "MathUtils",
    "methodSignature": "getSecureRandom()Ljava/security/SecureRandom;",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "d9d86e6e-2db3-43f4-a990-5405e2251f6e",
    "startBlock": "1160292773",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntIntHashMap",
    "methodSignature": "rehash(I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d9560a1d-6e26-4c9c-9387-5d65b987aaee",
    "startBlock": "988808306",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getMap(I)Ljava/util/Map;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "5e4ccb1e-f79f-4a2b-bf65-f33050a2520b",
    "startBlock": "354510624",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readIfNotExists()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8653ef4a-f98d-4f07-b7da-99e309f47c18",
    "startBlock": "89504844",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "removeUntil(I)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "715e8f6e-982f-4956-9156-3ff0db70dd4e",
    "startBlock": "238176457",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "checkCanceled()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "32b613d7-1063-4283-80b9-31042a70d2b8",
    "startBlock": "50770476",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "lockRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;)Lorg/h2/result/Row;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "4875a30f-a78c-43ab-82bf-a468d9cff320",
    "startBlock": "313102574",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "ConnectionInfo",
    "methodSignature": "getName()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "d2f92ec2-371b-4064-b6e3-7a2bd00e8002",
    "startBlock": "868912563",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueTimestampTimeZone",
    "methodSignature": "<init>(JJI)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "3db79cf9-2a0c-494c-bf24-7e320c5d6e51",
    "startBlock": "627223830",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "getNextPage(J)Lorg/h2/pagestore/db/PageDataLeaf;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((PAGE_SIZE && IFEXISTS) || (!PAGE_SIZE && (IFEXISTS || !DEFRAG_ALWAYS)) || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "2839cd6f-a6ea-4ce3-8954-d2ae9953a99d",
    "startBlock": "1624439449",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "asString(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "de47ec00-679b-4574-bdad-87c7dbbc47b8",
    "startBlock": "1904487960",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page",
    "methodSignature": "insertKey(ILjava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "ad62f5b4-79bd-48d9-842d-5e76af580fca",
    "startBlock": "1530299881",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcStatement",
    "methodSignature": "closeOldResultSet()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "74998700-28a1-4d79-840a-1fc694daab4d",
    "startBlock": "1965678869",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "DateTimeUtils",
    "methodSignature": "dateValueFromLocalMillis(J)J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "150332ae-89e1-4c3b-8ab6-eda246ee1660",
    "startBlock": "1092116357",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "toUpperEnglish(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "4dfd4472-5d83-4eb8-bd18-6665e076f8bd",
    "startBlock": "195292733",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "syncLastModificationIdWithDatabase()V",
    "decisionIndex": -1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "27766c1a-d2a6-4d1c-9b86-db661f5c4756",
    "startBlock": "1831825143",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVSecondaryIndex",
    "methodSignature": "<init>(Lorg/h2/engine/Database;Lorg/h2/mvstore/db/MVTable;ILjava/lang/String;[Lorg/h2/table/IndexColumn;Lorg/h2/index/IndexType;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e371b658-cf95-4e42-8b6a-f6a4a082f33e",
    "startBlock": "2014401236",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "setRowCountStored(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "ac5f2c39-1640-4657-8f39-b2839ae9759a",
    "startBlock": "946070594",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "readChunkFooter(J)Lorg/h2/mvstore/Chunk;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "e3fd4203-ccdf-4e24-9c22-a22d65c08b70",
    "startBlock": "1045721036",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "getPage(II)Lorg/h2/pagestore/db/PageData;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "95d5500a-b3c7-4332-b64f-ddf6b64daf4b",
    "startBlock": "232240674",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "hasChanges(J)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "35c72189-2806-4dfd-a2a6-31093bae0111",
    "startBlock": "1024159510",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "TableFilter",
    "methodSignature": "getValue(Lorg/h2/table/Column;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "77397b30-4947-4ff4-add4-1f89a119904a",
    "startBlock": "1181711763",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueCollectionBase",
    "methodSignature": "getMemory()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "8f7799a7-a461-4db8-8f00-527c61b0b525",
    "startBlock": "1452331478",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore$ArrayType",
    "methodSignature": "write(Lorg/h2/mvstore/WriteBuffer;[Ljava/lang/Object;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "71a0d156-247b-41de-ac46-bb4151cc7ccf",
    "startBlock": "263849613",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "BaseIndex",
    "methodSignature": "getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8cf29ea0-c41e-4bb9-a6e4-d91df1526964",
    "startBlock": "1300654678",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "writeIndexRowCounts()V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "b600f554-d08c-4797-b25a-dbd6b85f328b",
    "startBlock": "1984987465",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeIndex",
    "methodSignature": "add(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "c6123937-20c5-40f3-bac5-86bd4f7f7f4b",
    "startBlock": "371046917",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "readPage(Lorg/h2/mvstore/MVMap;J)Lorg/h2/mvstore/Page;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "33672689-16f7-41bb-87f3-d2705d8be4fb",
    "startBlock": "1389375501",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "getOperationId(IJ)J",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "a677dec6-d745-4e54-bebe-783372ec29b3",
    "startBlock": "594413595",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseQuery()Lorg/h2/command/dml/Query;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "9161997f-cbf9-43f5-83b1-892237451891",
    "startBlock": "62159554",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Utils",
    "methodSignature": "newBytes(I)[B",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "3a487e22-e325-464e-9018-24948b61cabb",
    "startBlock": "619997015",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "nextPage(Lorg/h2/pagestore/db/PageBtreeCursor;I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((PAGE_SIZE && IFEXISTS) || (!PAGE_SIZE && (IFEXISTS || !DEFRAG_ALWAYS)) || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "38062ce1-8af5-4645-ae81-0cae4ec3a1eb",
    "startBlock": "128081890",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "findChunksToMove(JJ)Ljava/lang/Iterable;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "89252d52-75c4-47e2-8197-18ef312877f0",
    "startBlock": "1008673361",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "addRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "f03fa8d0-2178-456c-98ee-e3ac71667213",
    "startBlock": "964954361",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "find(Lorg/h2/pagestore/db/PageBtreeCursor;Lorg/h2/result/SearchRow;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8bed3155-a422-46e2-960d-bc9e13859d2f",
    "startBlock": "1440785330",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStoreTool$GenericDataType",
    "methodSignature": "read(Ljava/nio/ByteBuffer;)Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "64ccf4eb-853e-401f-9a1c-9375c714a3a7",
    "startBlock": "1858591904",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStreamTrunk$Iterator",
    "methodSignature": "next()Lorg/h2/pagestore/PageStreamTrunk;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "0ccbe1ed-979a-4d41-b36b-662d38a153b8",
    "startBlock": "619812341",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "closeImpl(Z)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "969d05f0-0879-4f41-8e7e-867eec6f411e",
    "startBlock": "1452406932",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FreeSpaceBitSet",
    "methodSignature": "isFragmented()Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "1ca9b571-ee6d-41ea-beeb-eb5ff0a25353",
    "startBlock": "1108762975",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "TypeInfo",
    "methodSignature": "cast(Lorg/h2/value/Value;Lorg/h2/engine/CastDataProvider;ZZLjava/lang/Object;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "40c3034f-6266-462f-bf26-12623d713d8b",
    "startBlock": "704958114",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "readHexLong(Ljava/util/Map;Ljava/lang/String;J)J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "3d0fc0f2-ea29-40d5-bf4e-fa01430d0e4c",
    "startBlock": "1227489132",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "removeUntil(II)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "43579c7b-b04c-4aa4-a8ab-cfec9701c0b6",
    "startBlock": "1793504989",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "getTemplateSimpleRow(Z)Lorg/h2/result/SearchRow;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "c5232b01-9508-46e3-8fc5-f7841ec35060",
    "startBlock": "844252660",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseNotNullConstraint()Lorg/h2/command/Parser$NullConstraintType;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "72a04c69-58be-44e4-a856-6f44fcb920b6",
    "startBlock": "455791527",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeIndex",
    "methodSignature": "writeRow(Lorg/h2/store/Data;ILorg/h2/result/SearchRow;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "228799d8-e86f-4f7b-a5f3-ece8ee425d3e",
    "startBlock": "819780602",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Insert",
    "methodSignature": "insertRows()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "42115ace-7fe0-4cf0-b6da-e447938bd95c",
    "startBlock": "1159799176",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readAggregate(Lorg/h2/expression/aggregate/AggregateType;Ljava/lang/String;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "5ed3d82a-af56-4430-aca2-c2cefd8ccf3d",
    "startBlock": "150625027",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.type",
    "className": "ObjectDataType$LongType",
    "methodSignature": "compare(Ljava/lang/Object;Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "6ae36a24-9729-4dd1-b70f-efee33d6a7df",
    "startBlock": "753037651",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "mapColumns(Lorg/h2/table/ColumnResolver;I)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "85cd3abf-db65-432f-951c-353ad76d97e0",
    "startBlock": "738778157",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "readFully([BII)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "cafea81e-f7a8-4444-9864-a20b34959499",
    "startBlock": "164059057",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "dropUnusedChunks()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5a3d51d9-cf7c-4ff7-b02e-2cec345cffe3",
    "startBlock": "36676423",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCondition",
    "methodSignature": "isSpatialIntersects()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "47ecfd4f-bb05-48df-a34b-08cb45692ee4",
    "startBlock": "313829701",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "edu.cmu.cs.mvelezce.bench",
    "className": "Database",
    "methodSignature": "getSQL(Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "b2753e8c-ac60-482a-90c7-27728f6fbb1d",
    "startBlock": "1471577421",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseRollback()Lorg/h2/command/dml/TransactionCommand;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "72d27f9e-7fe2-4702-b197-2754a4c97a03",
    "startBlock": "937655421",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Prepared",
    "methodSignature": "setProgress()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "a0c77438-faea-4437-8543-341dee8cdcec",
    "startBlock": "784964821",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageStoreTable",
    "methodSignature": "analyzeIfRequired(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && ANALYZE_AUTO)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK)",
      "(!IFEXISTS && ANALYZE_AUTO && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && ANALYZE_AUTO)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !ANALYZE_AUTO && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && ANALYZE_AUTO)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !ANALYZE_AUTO && !FILE_LOCK)",
      "(!IFEXISTS && ANALYZE_AUTO && FORBID_CREATION)",
      "(IFEXISTS && !ANALYZE_AUTO && !FORBID_CREATION)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && ANALYZE_AUTO)",
      "(IFEXISTS && ANALYZE_AUTO && !FORBID_CREATION)",
      "(IFEXISTS && ANALYZE_AUTO && FORBID_CREATION)",
      "(!IFEXISTS && !ANALYZE_AUTO && FORBID_CREATION)",
      "(IFEXISTS && !ANALYZE_AUTO && FORBID_CREATION)"
    ],
    "id": "9dbd2b9b-2f63-46a9-814c-705687a55399",
    "startBlock": "1823991421",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "initNextData()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "cecd6ab8-0853-4a75-9916-ef71aad571aa",
    "startBlock": "1720966853",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "moveChild(II)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "6a8feb5f-c414-49ad-b056-5695daf72cbe",
    "startBlock": "197932766",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Set",
    "methodSignature": "update()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "f8af6fe6-0de7-400e-9e42-161c06ddcd5e",
    "startBlock": "1734567138",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "StringUtils",
    "methodSignature": "convertBytesToHex(Ljava/lang/StringBuilder;[BI)Ljava/lang/StringBuilder;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ae033dca-6138-48a7-af3c-eb6891c55ab7",
    "startBlock": "1286733509",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "getHigherOrder(II)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "4ea4cfa5-ae59-44af-9522-242b70768f34",
    "startBlock": "892939945",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionVisitor",
    "methodSignature": "allColumnsForTableFilters([Lorg/h2/table/TableFilter;Lorg/h2/command/dml/AllColumnsForPlan;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "d577cdbb-7254-4c0f-83bc-17fb95ad9ebb",
    "startBlock": "1455604767",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "CommandWithColumns",
    "methodSignature": "createConstraints()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "3b936110-dc9e-4883-8a51-b12eba1323d1",
    "startBlock": "1729543732",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "addTableToDependencies(Lorg/h2/mvstore/db/MVTable;Ljava/util/HashSet;)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "20a7c0b7-c795-457e-9b6f-cf587b66e253",
    "startBlock": "1757593984",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseCommit()Lorg/h2/command/dml/TransactionCommand;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "079949b0-00f4-4a48-86ed-423b2dae9800",
    "startBlock": "1039135609",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "writeVarLong(J)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "897d13a3-2244-40cd-b11e-97354bdc0d59",
    "startBlock": "1696786977",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "FileStore",
    "methodSignature": "checkWritingAllowed()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "f287418a-2366-45fd-9eb3-e3d5a1d49eb4",
    "startBlock": "445995104",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcResultSet",
    "methodSignature": "checkColumnIndex(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "962a4a21-c06a-4365-9e76-eb8c98ced758",
    "startBlock": "1738490283",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "edu.cmu.cs.mvelezce.bench.c",
    "className": "BenchCThread",
    "methodSignature": "processDelivery()V",
    "decisionIndex": -1,
    "info": [
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "7c6ba7c8-2f93-43d3-90d4-4dfc56ec1c43",
    "startBlock": "760300100",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "commit()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "03890659-2f07-4dc8-9dfd-ffefde105d64",
    "startBlock": "433062057",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "ConnectionInfo",
    "methodSignature": "getProperty(Ljava/lang/String;I)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "6d471086-4a2d-42df-9867-44ff2bfcb9eb",
    "startBlock": "267230253",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "FileStore",
    "methodSignature": "open(Ljava/lang/String;Z[C)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "5d18b0ca-0c28-4996-bcfb-1e6fb47109c7",
    "startBlock": "1293478389",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "read()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "1df4adfd-f155-4a51-8b6f-36764e455c90",
    "startBlock": "1845714048",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "<init>(Ljava/util/Map;)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!PAGE_SIZE && !FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && PAGE_SIZE && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)"
    ],
    "id": "65da204f-6316-4101-a232-703f2adac30a",
    "startBlock": "68923552",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "DbException",
    "methodSignature": "translate(Ljava/lang/String;[Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "IFEXISTS"
    ],
    "id": "f9384e10-0a6d-4c56-9f31-ebeb518690fe",
    "startBlock": "196756939",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "setWriteVersion(J)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "58c37245-bc29-4a4f-8b40-0c13088758ec",
    "startBlock": "91911555",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "88719286-a3c8-44de-9605-f0ed04792c3c",
    "startBlock": "2122686682",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "Expression",
    "methodSignature": "getUnenclosedSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "14ca897b-8e29-4f9d-9a78-a09d4e87a99c",
    "startBlock": "1416866780",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "ColumnNamer",
    "methodSignature": "getColumnName(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "dc2d392e-f303-4900-a849-81e16fa6576f",
    "startBlock": "310696972",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "getVarLongLen(J)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8e26dc68-3ee4-4a92-b0d4-c2e5ac55a0ad",
    "startBlock": "454279498",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "Trace",
    "methodSignature": "error(Ljava/lang/Throwable;Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "299c11d8-b01b-48ed-8a03-db7379586129",
    "startBlock": "1041330885",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Utils",
    "methodSignature": "newIntArray(I)[I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8ff1fc15-0894-484b-b688-6d2953f136d3",
    "startBlock": "876910115",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataNode",
    "methodSignature": "remove(J)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "21ac6ea2-71da-4076-b4c8-f744b9098256",
    "startBlock": "66148223",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileDisk",
    "methodSignature": "force(Z)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "2536bc5b-5e26-439f-81c3-321e710960bb",
    "startBlock": "678872839",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readCondition()Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "ac3b58aa-0129-4395-9461-b48ffd87ce4b",
    "startBlock": "593828081",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "registerVersionUsage()Lorg/h2/mvstore/MVStore$TxCounter;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "6a48b24f-cf8a-4d15-a324-3d4461f7cda5",
    "startBlock": "618108977",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVPrimaryIndex",
    "methodSignature": "getKey(Lorg/h2/result/SearchRow;Lorg/h2/value/ValueLong;Lorg/h2/value/ValueLong;)Lorg/h2/value/ValueLong;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "1d46b288-6212-4b4c-be8a-1f685e605e08",
    "startBlock": "1216284822",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "markStatementStart(Ljava/util/HashSet;Ljava/util/HashSet;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "78de5996-db80-49b4-92eb-60ec4624469d",
    "startBlock": "134200466",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcStatement",
    "methodSignature": "executeQuery(Ljava/lang/String;)Ljava/sql/ResultSet;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "d480e96c-4d35-4882-8f42-642b3ba56908",
    "startBlock": "521257110",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "hasUnsavedChanges()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "77809c6e-7450-4e6b-87cb-f4a51c078acd",
    "startBlock": "1004993898",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtree",
    "methodSignature": "getRow(I)Lorg/h2/result/SearchRow;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "2772e946-2b26-4bff-8f3d-65da87a3907a",
    "startBlock": "1434946507",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "OnExitDatabaseCloser",
    "methodSignature": "unregister(Lorg/h2/engine/Database;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7f11a831-19f7-4a67-b02f-67964e771e21",
    "startBlock": "906745948",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "writeData()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "23192757-a4d5-4dae-aeb8-0b5db9b7d0a8",
    "startBlock": "489015671",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ValueExpression",
    "methodSignature": "isEverything(Lorg/h2/expression/ExpressionVisitor;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "c49d324b-775f-4d56-bc65-65363d6a05ce",
    "startBlock": "915228810",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCursor",
    "methodSignature": "prepare(Lorg/h2/engine/Session;Ljava/util/ArrayList;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "7f090faa-b876-4b71-8141-9268d812b3ef",
    "startBlock": "1746302463",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "Page",
    "methodSignature": "remove([Ljava/lang/Object;II)[Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "836a8afc-83b9-4b49-a0c3-039ddf20b491",
    "startBlock": "1390029945",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "fireBeforeSelectTriggers()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "2f35ef8c-fa78-46f2-b73c-b2d33d10d0b7",
    "startBlock": "976554421",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "readChunkHeaderOptionally(JI)Lorg/h2/mvstore/Chunk;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "46e85c67-2d85-4e90-9beb-3c6fbe934b97",
    "startBlock": "1913536025",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "updateRootPage(Lorg/h2/mvstore/Page;J)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5a6ba844-1a5e-4a16-be32-c1b2f71ef27c",
    "startBlock": "1434106728",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "writeVariableHeader()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "710cf48a-d43e-46f3-8af2-ba0ceb2ac744",
    "startBlock": "1572216922",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "DateTimeUtils",
    "methodSignature": "nanosFromLocalMillis(J)J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "fa270c44-7704-4cf6-8ec3-aea19e492ee3",
    "startBlock": "1414490835",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store.fs",
    "className": "FileDisk",
    "methodSignature": "read(Ljava/nio/ByteBuffer;)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "09ed8161-4601-4eb2-814f-a8b68dd75719",
    "startBlock": "1175246808",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "LocalResultImpl",
    "methodSignature": "<init>(Lorg/h2/engine/Session;[Lorg/h2/expression/Expression;II)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "a8d6cab3-7faf-4a4a-9a27-8610196a1a4b",
    "startBlock": "611281668",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "addMeta(Lorg/h2/engine/Session;Lorg/h2/engine/DbObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8f5c8873-1dd5-4e6a-9814-8dc94b72a6c8",
    "startBlock": "1216945711",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataCursor",
    "methodSignature": "nextRow()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "fc704dc1-ef19-47d4-a677-193ee0dc02d3",
    "startBlock": "544375998",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "removeMetaIndex(Lorg/h2/index/Index;Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "38a58ba1-dfcb-4905-a84a-4d034edac749",
    "startBlock": "1946755501",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "executeQuery()Ljava/sql/ResultSet;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "7ba2be2e-5b79-4c29-912e-6df8590d5fb0",
    "startBlock": "516814823",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readColumnIdentifier()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "c3976a3f-cc4b-4b52-81e2-7bc1d23a9cb4",
    "startBlock": "664571685",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "UndoLog",
    "methodSignature": "removeLast()V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "33e439a3-8846-4ab1-86be-6425bf9381cf",
    "startBlock": "1475681013",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap",
    "methodSignature": "chooseIterator(Ljava/lang/Object;Ljava/lang/Object;Z)Ljava/util/Iterator;",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "70ea31c1-880e-4873-bccc-ce649f4ab429",
    "startBlock": "1774041788",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "VersionedValueType",
    "methodSignature": "getMemory(Ljava/lang/Object;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)"
    ],
    "id": "26b543b2-4ee7-4ab4-acc7-b56c1e59b779",
    "startBlock": "1620394978",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "MetaIndex",
    "methodSignature": "getCost(Lorg/h2/engine/Session;[I[Lorg/h2/table/TableFilter;ILorg/h2/result/SortOrder;Lorg/h2/command/dml/AllColumnsForPlan;)D",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "89d1d902-e3af-4f46-8010-8677261344e6",
    "startBlock": "390038913",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "isQuery()Z",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "48583e23-ef19-40eb-bf66-96d1be302ddc",
    "startBlock": "1577102829",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCursor",
    "methodSignature": "canUseIndexFor(Lorg/h2/table/Column;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "cde35d12-2e08-44e5-8f8c-005fda7b90fa",
    "startBlock": "525510067",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseSortType()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "db307433-9c3a-498b-90a3-da7573d9a7e4",
    "startBlock": "661781937",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap",
    "methodSignature": "sizeAsLong()J",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "8d3dbdaf-a4c0-43c3-bb55-acd4e15cd677",
    "startBlock": "1086905468",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "setEvaluatableRecursive(Lorg/h2/table/TableFilter;)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "8f4c6269-32b2-4bb3-befd-0f7dfc3be84e",
    "startBlock": "1316344794",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "ValueDataType",
    "methodSignature": "compareValues(Lorg/h2/value/Value;Lorg/h2/value/Value;I)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "ebf44ceb-77b7-4b5a-be6d-0d74cd2dd2f3",
    "startBlock": "355694868",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "writeData()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "0bb6ed79-844c-4281-8f09-e432f20a0f5b",
    "startBlock": "110070768",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "IndexColumn",
    "methodSignature": "mapColumns([Lorg/h2/table/IndexColumn;Lorg/h2/table/Table;)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "33040eb0-ad25-4aa2-9b75-84944c780755",
    "startBlock": "1324281764",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap$CommittedIterator",
    "methodSignature": "fetchNext()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "36087f4d-11d2-4bdf-bd9f-b7b2f9a7b2a4",
    "startBlock": "484350747",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageFreeList",
    "methodSignature": "getLastUsed()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "869b4243-c8bc-4fb6-8e0d-7d316185709b",
    "startBlock": "633166461",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "tryLock(I)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "d3caabd5-b5b2-430b-b200-38b6cd889009",
    "startBlock": "173389049",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntIntHashMap",
    "methodSignature": "internalPut(II)V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "075b065e-6ea1-4ae3-966e-cac8c6274f47",
    "startBlock": "1056301846",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueTimestampTimeZone",
    "methodSignature": "equals(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "d6b40ca1-386f-4cba-aa1b-961dc7668162",
    "startBlock": "722391498",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "hasChangesSince(J)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "6b563db6-95ca-4b16-93e8-36d0a65eda31",
    "startBlock": "56702195",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "Page",
    "methodSignature": "insert([Ljava/lang/Object;IILjava/lang/Object;)[Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "d9fdc527-07f1-404b-ac12-4f86aad0219a",
    "startBlock": "749119140",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readDistinctAgg()Z",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "3c78e315-9576-46a4-82e3-d81420741320",
    "startBlock": "1864369998",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "AggregateDataDefault",
    "methodSignature": "getValue(Lorg/h2/engine/Database;I)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "cc1d6bb3-2127-44aa-a385-78758ae8a3f3",
    "startBlock": "56677219",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "asString()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "52b8866b-ed68-4716-beaf-c90bf1fc7d71",
    "startBlock": "191803353",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "getOldestVersionToKeep()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f551a79f-72c2-47e0-a343-3206df43c97b",
    "startBlock": "1518551883",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseAlterTable()Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "0116b1cd-aa4e-4ec8-b2bf-86825152f0f1",
    "startBlock": "1917711177",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionVisitor",
    "methodSignature": "getQueryLevel()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "c004e1ea-95eb-4d84-9b51-76d98f443c57",
    "startBlock": "2000196542",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "getValueWithArgs(Lorg/h2/engine/Session;[Lorg/h2/expression/Expression;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "797be338-b316-4efe-a491-bbc568710c79",
    "startBlock": "1168717285",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseCreate()Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "c1a60508-a834-46ef-88ea-f557c4393a06",
    "startBlock": "1717256574",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "transitionToState(Lorg/h2/engine/Session$State;Z)Lorg/h2/engine/Session$State;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "c1bff862-2297-44c4-b76b-02000117736d",
    "startBlock": "1306228515",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "addChild(IILorg/h2/result/SearchRow;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "15312603-b264-428d-8079-70b9aaaf1bb7",
    "startBlock": "2060447392",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TxDecisionMaker",
    "methodSignature": "reset()V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "0b4e1cf0-2e23-43c4-aac6-29fb1ffe0e08",
    "startBlock": "804856636",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "commit(Lorg/h2/mvstore/tx/Transaction;Z)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "2e76a20b-df62-47be-a67a-97c64aeb5702",
    "startBlock": "624509871",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "CommandContainer",
    "methodSignature": "recompileIfRequired()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "f7ba74dd-2314-4dcb-aacb-4f5f58c2c62d",
    "startBlock": "134665413",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "check()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "da76698c-ba09-4e33-9f6b-39e63fbe2a52",
    "startBlock": "1362296359",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.schema",
    "className": "Schema",
    "methodSignature": "add(Lorg/h2/schema/SchemaObject;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "23d127ab-e340-4e6e-aa0e-aa3e35728215",
    "startBlock": "1320829670",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "setAutoCommit(Z)V",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "d449061c-7355-4001-96d7-ad7d87381ab6",
    "startBlock": "2144042983",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "CommitDecisionMaker",
    "methodSignature": "decide(Lorg/h2/value/VersionedValue;Lorg/h2/value/VersionedValue;)Lorg/h2/mvstore/MVMap$Decision;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "72fff5df-b01b-410d-a919-792d88a42cc9",
    "startBlock": "2076793145",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "onVersionChange(J)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "65936519-5772-41d1-aa66-927187278523",
    "startBlock": "1358982136",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "<init>(Lorg/h2/mvstore/RootReference;JI)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "db2350ad-489b-4f80-b575-5a7a8ec2f55d",
    "startBlock": "1832487788",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "compactMoveChunks(IJ)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "f8588a9f-bfbc-4065-9884-cccb6ece0827",
    "startBlock": "1249611692",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "rewrite(Ljava/util/Set;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "a3b5fb3e-0273-4ee0-a7d3-13a156086643",
    "startBlock": "1206325076",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Permutations",
    "methodSignature": "rightmostDip()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e1f2ac31-d57c-4476-b7ff-c362f0b1c8fb",
    "startBlock": "1978477233",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "stopBackgroundThread(Z)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "de19c32c-5164-4cb8-8598-f1af21d0056e",
    "startBlock": "1566076111",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "openFile(Ljava/lang/String;Ljava/lang/String;Z)Lorg/h2/store/FileStore;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "197785c9-ab3a-4fe1-bdd9-73e9d6459081",
    "startBlock": "2071349770",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "removeRow(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "ebb68da7-0ff8-4f51-9cd4-bc7a35bf509e",
    "startBlock": "1214454345",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "MathUtils",
    "methodSignature": "secureRandomBytes(I)[B",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS || !FILE_LOCK || FORBID_CREATION)",
      "(!IFEXISTS && FILE_LOCK && !FORBID_CREATION)"
    ],
    "id": "75caadc5-ae92-4f00-b8ea-2901f75930ff",
    "startBlock": "1724648508",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Update",
    "methodSignature": "setAssignment(Lorg/h2/table/Column;Lorg/h2/expression/Expression;)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0d8ca6fa-5096-4f17-bde1-aa51d97532c0",
    "startBlock": "1521710857",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Prepared",
    "methodSignature": "getObjectId()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "e1ffbc5a-c8d0-44a8-8772-bb0ffc50d4a9",
    "startBlock": "1490758119",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "waitIfExclusiveModeEnabled()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "c3e34067-aef4-4d81-90bb-1a358bd67cbd",
    "startBlock": "1345944919",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "addTableFilter(Lorg/h2/table/TableFilter;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "7fda79fc-5783-45a0-a7e7-037b4914a1ed",
    "startBlock": "726427800",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "queryWithoutCache(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b339842a-5f23-4feb-977b-d4ec8aabfda1",
    "startBlock": "1941945388",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "rewrite(Ljava/lang/Object;)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "6bd1464d-dcf9-4023-a94f-1749cc7a64ff",
    "startBlock": "665598829",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "verifyMetaLocked(Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "((!FORBID_CREATION && !IFEXISTS && MV_STORE) || (!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE))",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "47e5511a-fed5-4b9b-bfa5-5284f9536f13",
    "startBlock": "1893832129",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataIndex",
    "methodSignature": "memoryChange(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "97c0e95f-c949-48b9-930c-640dd6e91e5e",
    "startBlock": "1098385231",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Engine",
    "methodSignature": "openSession(Lorg/h2/engine/ConnectionInfo;ZZLjava/lang/String;)Lorg/h2/engine/Session;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "26c449a2-67ed-4558-b6aa-70e19efee0a9",
    "startBlock": "866874587",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "updateAggregate(Lorg/h2/engine/Session;Ljava/lang/Object;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "7a079889-92f1-4165-8841-f7934fe9258e",
    "startBlock": "56206142",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "User",
    "methodSignature": "checkAdmin()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "42461fd4-8ac9-46ef-8a21-704ee9968662",
    "startBlock": "884551629",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeIndex",
    "methodSignature": "set(Lorg/h2/pagestore/db/TreeNode;ZLorg/h2/pagestore/db/TreeNode;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "ab2cb3c7-612e-4215-9fdd-b44bea62a6f1",
    "startBlock": "2076280781",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Optimizer",
    "methodSignature": "canStop(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "31c7452b-5aed-40e7-9a27-c8dde6796216",
    "startBlock": "522328932",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "preparePlan(Z)D",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "9a52a88f-70b7-492d-8a18-18cd3eb7722e",
    "startBlock": "2034213834",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "addDependencies(Ljava/util/HashSet;)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "cb267e57-35b6-4582-babc-4ae3e826af29",
    "startBlock": "818506520",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "readString(Ljava/nio/ByteBuffer;I)Ljava/lang/String;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "2add69ee-5f4b-41c4-a90a-114df2c179d5",
    "startBlock": "503111985",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionVisitor",
    "methodSignature": "addDataModificationId(J)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "3ea0b980-d364-40f8-b364-13872e146305",
    "startBlock": "1998689767",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "DateTimeUtils",
    "methodSignature": "getTimeZoneOffsetMillis(J)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "893285b2-e0b8-4619-945e-127c2671f08c",
    "startBlock": "1690905077",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "removeRow(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "445c80d7-7c75-4a3d-8697-cb8e8a7d9145",
    "startBlock": "491833216",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "getSQL(Ljava/lang/StringBuilder;Z)Ljava/lang/StringBuilder;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "4511068f-2fd9-493b-8e26-2106fd04c493",
    "startBlock": "608832165",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "getVarIntLen(I)I",
    "decisionIndex": 1,
    "info": [
      "((MV_STORE && (IFEXISTS || (!DEFRAG_ALWAYS && !COMPRESS) || FORBID_CREATION)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "((!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS) || (!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE))",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "587a2a4f-c2fa-40f6-bfae-a969f1270c59",
    "startBlock": "1104853063",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "readValue()Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "6045d96b-8c13-41ac-9c99-1c17f86885f0",
    "startBlock": "1880888056",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "constructGroupResultRow([Lorg/h2/value/Value;I)[Lorg/h2/value/Value;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "ea6c601d-02d9-46b7-b389-1ecf01293bfe",
    "startBlock": "2074842537",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "checkPrecision(J)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "6c9af96a-718a-4695-93a2-11c674fdc4c4",
    "startBlock": "2126563764",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "parseChecksummedMap([B)Ljava/util/HashMap;",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "fc15d661-d91e-48d5-b33b-bbea1d5765bf",
    "startBlock": "1366782575",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "76ecc9fa-d4a5-49bd-80b9-268fb6026aef",
    "startBlock": "2147455357",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "SimpleRowValue",
    "methodSignature": "getValue(I)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "7be14e50-05d2-4dde-a1c6-3aa89b21fb2d",
    "startBlock": "190818495",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "endTransaction(Lorg/h2/mvstore/tx/Transaction;Z)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "1bb3da3d-65df-4c7d-ad06-2ad0ba7bc234",
    "startBlock": "1636355334",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseAlterTableAddConstraintIf(Ljava/lang/String;Lorg/h2/schema/Schema;Z)Lorg/h2/command/ddl/DefineCommand;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "320abdc7-cffc-479e-8dcb-c581ad0c2999",
    "startBlock": "906802220",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntIntHashMap",
    "methodSignature": "put(II)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "7d8fd6ca-9698-46e1-9690-f92e1387d6ba",
    "startBlock": "1451014142",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Permutations",
    "methodSignature": "next()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9356b882-1fad-4f44-8fc1-8fddba6e20d6",
    "startBlock": "1066104528",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Cursor",
    "methodSignature": "hasNext()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && IFEXISTS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && COMPRESS && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && IFEXISTS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)"
    ],
    "id": "d86478ae-410d-4a38-8503-effed48505d6",
    "startBlock": "338689182",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeLeaf",
    "methodSignature": "remove(Lorg/h2/result/SearchRow;)Lorg/h2/result/SearchRow;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "7fd80d9a-4975-4c6d-8f50-a72180a7eecc",
    "startBlock": "1224591411",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "readHexInt(Ljava/util/Map;Ljava/lang/String;I)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(FORBID_CREATION && IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && !MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "4af1dd34-0f6a-473d-876a-88717c5f58ae",
    "startBlock": "1927222161",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "commit()V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "087e9f4f-ffb4-4d36-b72f-8a2390cd6055",
    "startBlock": "1574845920",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.compress",
    "className": "CompressLZF",
    "methodSignature": "compress([BI[BI)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "((MV_STORE && (IFEXISTS || (!DEFRAG_ALWAYS && !COMPRESS) || FORBID_CREATION)) || (!MV_STORE && (FORBID_CREATION || IFEXISTS)))",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "ca193b71-b61b-4a1b-b890-2adbfe43e187",
    "startBlock": "164803369",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "IndexCondition",
    "methodSignature": "isAlwaysFalse()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "01ede4c0-5572-4414-90aa-db78ee4f0ea1",
    "startBlock": "1904736253",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageDataLeaf",
    "methodSignature": "getRowLength(Lorg/h2/result/Row;)I",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "9cc752ca-e5c0-4dfe-87b1-3873eb55d4b7",
    "startBlock": "1243061874",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "createIdSet(Ljava/lang/Iterable;)Ljava/util/HashSet;",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "85675841-5f7e-48ea-9289-b82fe0097ce0",
    "startBlock": "414264518",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcStatement",
    "methodSignature": "executeInternal(Ljava/lang/String;Ljava/lang/Object;)Z",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8feeb2c2-cdd1-4d30-8586-bb585951f0ff",
    "startBlock": "318235698",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Session",
    "methodSignature": "log(Lorg/h2/table/Table;SLorg/h2/result/Row;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "b7f7d71e-2f87-43a1-a148-0937cb252c23",
    "startBlock": "56456954",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionMap$ValidationIterator",
    "methodSignature": "isApplicable(Lorg/h2/value/VersionedValue;)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "1cc27952-8860-4bfc-9b97-48b0e2a4cd80",
    "startBlock": "535407635",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "convertToInt(Ljava/lang/Object;)Lorg/h2/value/ValueInt;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "465b8aec-97fc-4f72-bdd6-91434f6bcf5d",
    "startBlock": "374105691",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "<init>(Lorg/h2/util/CacheWriter;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && CACHE_SIZE && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !CACHE_SIZE && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "f73634b1-4b03-4078-8d4e-c4cab05315f8",
    "startBlock": "921033080",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "readVarInt(Ljava/nio/ByteBuffer;)I",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "b5d675f4-99fa-4af5-820e-56c9d0b9409e",
    "startBlock": "452352344",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "tryCommit()J",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "94f67844-dce9-4b7e-85ed-88fbc6eded1b",
    "startBlock": "563272163",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseSelectFromPart(Lorg/h2/command/dml/Select;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "97ac76ff-d3c0-4437-9850-0f39325f08e8",
    "startBlock": "1960904494",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readLong()J",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "fed4194d-e079-4bf8-9ea6-995aebfcd6b8",
    "startBlock": "481249701",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.store",
    "className": "Data",
    "methodSignature": "checkCapacity(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "25697492-7701-4e3e-bbe7-cb48e620d046",
    "startBlock": "186119748",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "JdbcUtils",
    "methodSignature": "loadUserClass(Ljava/lang/String;)Ljava/lang/Class;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "5f10c46a-5e06-4e36-925b-97530b956567",
    "startBlock": "1101095496",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "getFreeList(I)Lorg/h2/pagestore/PageFreeList;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(PAGE_SIZE && !IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && FORBID_CREATION && MV_STORE)",
      "(!PAGE_SIZE && IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "230f3308-757d-48aa-b630-0e5226aaa9a2",
    "startBlock": "1164255122",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.constraint",
    "className": "ConstraintUnique",
    "methodSignature": "getCreateSQLForCopy(Lorg/h2/table/Table;Ljava/lang/String;Z)Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "c9367bd0-26de-4e24-b237-254280e0e53b",
    "startBlock": "157307311",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "processGroupResult(ILorg/h2/result/LocalResult;JZZ)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "c7c244ca-5b4e-4759-9ad5-fcd89a9b2808",
    "startBlock": "1479327692",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVMap",
    "methodSignature": "unlockRoot(Lorg/h2/mvstore/Page;I)Lorg/h2/mvstore/RootReference;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "a5053044-1c8b-4b00-a468-b2f5d4173e32",
    "startBlock": "476182975",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "checksumTest([BII)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "712fda7e-b057-47fa-9a6b-e552e627c4e2",
    "startBlock": "602144845",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "removeRow(Lorg/h2/engine/Session;Lorg/h2/result/Row;)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "a51f647b-54ea-42f5-bcd3-983e3655051d",
    "startBlock": "175359829",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.compress",
    "className": "CompressLZF",
    "methodSignature": "expand([BII[BII)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)"
    ],
    "id": "53e262cf-85ea-4bdc-aaa3-db9433497d54",
    "startBlock": "97151522",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Utils",
    "methodSignature": "newLongArray(I)[J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "((PAGE_SIZE && IFEXISTS) || (!PAGE_SIZE && (IFEXISTS || !DEFRAG_ALWAYS)) || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8c41bf4e-1872-4047-a1f1-1346e2773a36",
    "startBlock": "944924752",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "SelectGroups",
    "methodSignature": "getInstance(Lorg/h2/engine/Session;Ljava/util/ArrayList;Z[I)Lorg/h2/command/dml/SelectGroups;",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "a94af438-4ed0-4e71-ba82-44ed37a44e3f",
    "startBlock": "370767913",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "getIndexForColumn(Lorg/h2/table/Column;ZZ)Lorg/h2/index/Index;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "47d85165-8a80-4f48-a0cb-5be5bd3ba3ad",
    "startBlock": "666662503",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Chunk",
    "methodSignature": "readChunkHeader(Ljava/nio/ByteBuffer;J)Lorg/h2/mvstore/Chunk;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "7c6d15bf-b795-4208-bf8d-3a4e2e5cdd0b",
    "startBlock": "9393151",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "compactRewrite(Ljava/util/Set;)I",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "44843a54-f451-4ad7-8600-bfdfb004efb7",
    "startBlock": "1578670865",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "ExpressionVisitor",
    "methodSignature": "incrementQueryLevel(I)Lorg/h2/expression/ExpressionVisitor;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "38a03173-0a0f-4fdf-ab4f-87d8b246ec8a",
    "startBlock": "2147230844",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Query",
    "methodSignature": "getOffsetFetch(I)Lorg/h2/command/dml/Query$OffsetFetch;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "0d4a2b93-af38-47e9-85e6-4c8b9140ac27",
    "startBlock": "126868858",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "store()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8a406e50-dfd5-439d-a31a-c3bfef85c409",
    "startBlock": "162147669",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "TreeNode",
    "methodSignature": "isFromLeft()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "1e313e44-8425-4a01-a54c-d4d08bb4904c",
    "startBlock": "941436610",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseAlterTableDrop(Lorg/h2/schema/Schema;Ljava/lang/String;Z)Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "cd99848e-fbe3-4735-a261-613b2a7e987e",
    "startBlock": "992500094",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "checkClosed(Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && AUTOCOMMIT)"
    ],
    "id": "0f727c15-ae64-414e-b18f-3068b92cb293",
    "startBlock": "378007565",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "moveChunkInside(Lorg/h2/mvstore/Chunk;J)Z",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "0e6d8083-27a2-4cfd-8cc3-ab238824733e",
    "startBlock": "1731800940",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageFreeList",
    "methodSignature": "getFirstFree(I)I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "56d14f85-6bac-4bbb-bd03-ad303114e89c",
    "startBlock": "1521896773",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Page$PageReference",
    "methodSignature": "resetPos()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "73f8c546-7d16-4e1e-aba5-3cd5ddc5c97a",
    "startBlock": "788577640",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "LobStorageMap",
    "methodSignature": "init()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "88fd80a5-dd07-49ca-90a3-ae3f3e5a68f3",
    "startBlock": "967504317",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "MetaTable",
    "methodSignature": "<init>(Lorg/h2/schema/Schema;II)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "899d4c50-0b36-4529-b104-116a636f1e8d",
    "startBlock": "21034905",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "rebuildIndex(Lorg/h2/engine/Session;Lorg/h2/mvstore/db/MVIndex;Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "c8853e70-f9e4-49a2-94e4-a7bb94f85ebd",
    "startBlock": "426980762",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Update",
    "methodSignature": "prepare()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "1f99a072-c103-4dd3-9173-b910f1d5f309",
    "startBlock": "753066636",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "getNullOrValue(Lorg/h2/engine/Session;[Lorg/h2/expression/Expression;[Lorg/h2/value/Value;I)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "d21c5550-101a-46c7-a481-a4d9f9ea185d",
    "startBlock": "891872468",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "read()V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "eb45d734-c682-401a-8bc5-930cb5e71aa6",
    "startBlock": "1089472857",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceObject",
    "methodSignature": "debugCodeCall(Ljava/lang/String;Ljava/lang/String;)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "54e543cd-ce67-4e93-afb2-b3eca8b25a6c",
    "startBlock": "466624865",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parse(Ljava/lang/String;Z)Lorg/h2/command/Prepared;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "21f3ea43-5400-45a4-839c-a43617b7e421",
    "startBlock": "1210385644",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "Transaction",
    "methodSignature": "isActive(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "f2e6085a-1883-4f82-9677-e289a6665c1a",
    "startBlock": "689690966",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageFreeList",
    "methodSignature": "write()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "ee1acff2-7a77-41b9-a177-c71bffd2260b",
    "startBlock": "912292491",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "Comparison",
    "methodSignature": "getAdditionalAnd(Lorg/h2/engine/Session;Lorg/h2/expression/condition/Comparison;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "cdb6197a-b6ee-421f-baa0-16c33afb05e9",
    "startBlock": "2010396398",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readOver(Lorg/h2/expression/analysis/DataAnalysisOperation;)V",
    "decisionIndex": 1,
    "info": [
      "(PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)"
    ],
    "id": "33f1a3d6-ddf2-40f1-be97-264a6e20b2e3",
    "startBlock": "1472768305",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "Value",
    "methodSignature": "convertTo(ILorg/h2/value/ExtTypeInfo;Lorg/h2/engine/CastDataProvider;ZLjava/lang/Object;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "4b2e6d55-73f6-415b-8710-ca803b5971f9",
    "startBlock": "1521042166",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "CreateTable",
    "methodSignature": "update()I",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "793187b4-bf4a-4fe9-9e2c-2e49a9221568",
    "startBlock": "1598205503",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "setAutoCommitDelay(I)V",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "3a3dffb7-f097-4119-a667-08f6c484ebc2",
    "startBlock": "204114023",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcResultSet",
    "methodSignature": "nextRow()Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK && !AUTOCOMMIT)"
    ],
    "id": "fe23696a-458d-4ddf-8838-d97b6531280e",
    "startBlock": "1258461535",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtreeNode",
    "methodSignature": "removeChild(I)V",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE || IFEXISTS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)"
    ],
    "id": "184e5ab3-1b1b-4767-a468-b5f885d610ac",
    "startBlock": "684316632",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore.db",
    "className": "PageBtree",
    "methodSignature": "find(Lorg/h2/result/SearchRow;ZZZ)I",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "5fa0e687-5ec3-4ba2-9a09-38e8ff91686b",
    "startBlock": "1010262689",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "Page",
    "methodSignature": "insert([JIIJ)[J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "ab407807-0e28-47d5-a0f7-a20a69f23660",
    "startBlock": "78166353",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getTableEngine(Ljava/lang/String;)Lorg/h2/api/TableEngine;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7b0e20ea-e57d-40ac-9db4-a03b73db7796",
    "startBlock": "993017412",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.condition",
    "className": "Comparison",
    "methodSignature": "optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "fc15dc94-67d1-4872-94f8-2e48b23f197a",
    "startBlock": "1184448423",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTable",
    "methodSignature": "doLock1(Lorg/h2/engine/Session;IZ)V",
    "decisionIndex": -1,
    "info": [
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "bbee4788-8428-40cb-bd31-02809aac70d9",
    "startBlock": "1211977982",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.cache",
    "className": "CacheLongKeyLIRS$Segment",
    "methodSignature": "<init>(Lorg/h2/mvstore/cache/CacheLongKeyLIRS$Segment;I)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "af38a59d-97ff-4840-bc27-37bf9b3ded50",
    "startBlock": "432627197",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseSimpleSortType()I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "8b1fbf42-16c2-42f5-8aa9-1e911fea188b",
    "startBlock": "947884677",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "CacheLRU",
    "methodSignature": "update(ILorg/h2/util/CacheObject;)Lorg/h2/util/CacheObject;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && CACHE_SIZE && IFEXISTS)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS)",
      "(FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && CACHE_SIZE && IFEXISTS && MV_STORE)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS)",
      "(!PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION && !CACHE_SIZE && PAGE_SIZE && !IFEXISTS && MV_STORE)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && CACHE_SIZE && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !CACHE_SIZE && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "3a0ca0f4-93f0-444f-94fb-ddc4d7b5439c",
    "startBlock": "528301039",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceSystem",
    "methodSignature": "openWriter()Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "d4bb0aae-21c7-4be5-9589-cbee666b37e9",
    "startBlock": "61271390",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.engine",
    "className": "Database",
    "methodSignature": "getDatabasePath()Ljava/lang/String;",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "fb20110d-804a-4e99-b322-44a746e0cf0d",
    "startBlock": "1101012974",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.function",
    "className": "Function",
    "methodSignature": "mapColumns(Lorg/h2/table/ColumnResolver;II)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "7d6d4859-42d7-4511-8e73-2b1c78d1e40b",
    "startBlock": "932145579",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "isOpen()Z",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "638c3829-4594-41f6-965a-093b82f4af28",
    "startBlock": "441299851",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Query",
    "methodSignature": "queryWithoutCacheLazyCheck(ILorg/h2/result/ResultTarget;)Lorg/h2/result/ResultInterface;",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "d6bea36a-5ad6-47dc-b7cc-bb8a6936f47d",
    "startBlock": "1592066120",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "SortOrder",
    "methodSignature": "typeToString(Ljava/lang/StringBuilder;I)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "53bbefec-7eb8-4db6-ab33-1d47f226f484",
    "startBlock": "1312960128",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "prepare()V",
    "decisionIndex": -1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "bff499a9-9323-4028-8eed-ddef944263fd",
    "startBlock": "1264757456",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "checkClosed(Z)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "3b0cbb99-56ec-4578-a2e9-6e851c857a69",
    "startBlock": "51409853",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "RowList",
    "methodSignature": "hasNext()Z",
    "decisionIndex": 1,
    "info": [
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && !IFEXISTS && FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && MV_STORE && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && FILE_LOCK)",
      "(FORBID_CREATION && PAGE_SIZE && IFEXISTS && MV_STORE && FILE_LOCK)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && IFEXISTS && !FILE_LOCK)",
      "(!PAGE_SIZE && !FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && IFEXISTS && !FILE_LOCK)",
      "(!FORBID_CREATION && PAGE_SIZE && !IFEXISTS && MV_STORE && FILE_LOCK && !AUTOCOMMIT)",
      "(!PAGE_SIZE && FORBID_CREATION && IFEXISTS && MV_STORE && !FILE_LOCK)"
    ],
    "id": "b20c9378-c162-40de-8ed2-cd54d2e8dfba",
    "startBlock": "451507072",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "IntArray",
    "methodSignature": "add(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!PAGE_SIZE && !IFEXISTS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "b7ceb3ad-8412-457a-be7e-547768008dd5",
    "startBlock": "1273265250",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "Cursor",
    "methodSignature": "next()Ljava/lang/Object;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !IFEXISTS && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "0126d5a2-b091-432e-bd03-f95e17070dca",
    "startBlock": "272755650",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcConnection",
    "methodSignature": "getMetaData()Ljava/sql/DatabaseMetaData;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "5d4575f9-ae7b-4430-abd5-f826934630c5",
    "startBlock": "127100733",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageOutputStream",
    "methodSignature": "<init>(Lorg/h2/pagestore/PageStore;ILjava/util/BitSet;IZ)V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!FORBID_CREATION && !IFEXISTS && !MV_STORE)"
    ],
    "id": "503c63fd-8968-4ca5-a784-2813885fdef9",
    "startBlock": "868041798",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.ddl",
    "className": "DropTable",
    "methodSignature": "prepareDrop()Z",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "cb08c711-6f80-4d29-b0ef-cf1359207918",
    "startBlock": "163274801",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.tx",
    "className": "TransactionStore",
    "methodSignature": "getOpenTransactions()Ljava/util/List;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "9e4f4754-49f7-4d94-a07c-9e93a4bc02bb",
    "startBlock": "1052929616",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "logAddOrRemoveRow(Lorg/h2/engine/Session;ILorg/h2/result/Row;Z)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "8a66d5ac-5431-4e4c-b9e1-fc43e04ed787",
    "startBlock": "1329359480",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.index",
    "className": "MetaIndex",
    "methodSignature": "getColumnIndex(Lorg/h2/table/Column;)I",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "cd46316e-a5e1-40c2-b4d6-0e1548d0788d",
    "startBlock": "615991818",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore.db",
    "className": "MVTableEngine$Store",
    "methodSignature": "open(Lorg/h2/engine/Database;Lorg/h2/mvstore/MVStore$Builder;Z)V",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)"
    ],
    "id": "a7538919-d27b-4572-be49-d2caee6d237a",
    "startBlock": "110478661",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "MathUtils",
    "methodSignature": "nextPowerOf2(I)I",
    "decisionIndex": 1,
    "info": [
      "(!IFEXISTS && CACHE_SIZE && !FORBID_CREATION && !MV_STORE)",
      "(!IFEXISTS && !CACHE_SIZE && !FORBID_CREATION && !MV_STORE)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)"
    ],
    "id": "3dfed9c2-7502-435e-97a7-4541f157ea2e",
    "startBlock": "783094730",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "removeMeta(Lorg/h2/index/Index;Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "eb28e8be-7274-4afd-8962-53f4dfe43761",
    "startBlock": "168083067",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "MVStore",
    "methodSignature": "getProjectedFillRate()I",
    "decisionIndex": -1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "5d4a2895-4a41-4148-8be4-93025b181181",
    "startBlock": "494013484",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "DateTimeUtils",
    "methodSignature": "dateValueFromAbsoluteDay(J)J",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "f988904c-4c42-41b1-aeed-f7fb989e3788",
    "startBlock": "1917940349",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "prepareCommand(Ljava/lang/String;)Lorg/h2/command/Command;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "b59ce694-67c7-47f8-977b-dee7541c2c44",
    "startBlock": "1817076613",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "TraceObject",
    "methodSignature": "logAndConvert(Ljava/lang/Throwable;)Ljava/sql/SQLException;",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "4911c7c4-37de-4e21-a13f-34435bb0e71a",
    "startBlock": "2072277015",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "DateTimeUtils",
    "methodSignature": "timestampTimeZoneFromMillis(J)Lorg/h2/value/ValueTimestampTimeZone;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "c343bbd7-609b-41bc-8434-812a48394bae",
    "startBlock": "1794436954",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.api",
    "className": "ErrorCode",
    "methodSignature": "isCommon(I)Z",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "39816890-958d-410e-a165-cc670d0041e8",
    "startBlock": "32476009",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression.aggregate",
    "className": "Aggregate",
    "methodSignature": "getValueQuick(Lorg/h2/engine/Session;)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "f0ffb7dc-f905-4833-a380-dc55db6ff74e",
    "startBlock": "1168834428",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Command",
    "methodSignature": "stop()V",
    "decisionIndex": 1,
    "info": [
      "(IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION)",
      "(!IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(IFEXISTS && AUTOCOMMIT && FORBID_CREATION)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && FORBID_CREATION)",
      "(IFEXISTS && AUTOCOMMIT && !FORBID_CREATION)"
    ],
    "id": "9d338b94-f88d-4e28-ba85-5cb13a20d633",
    "startBlock": "75296511",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.expression",
    "className": "BinaryOperation",
    "methodSignature": "optimize(Lorg/h2/engine/Session;)Lorg/h2/expression/Expression;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "d04d51c7-7a13-4e7f-a89a-d6eb3d88494e",
    "startBlock": "1759778461",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "ColumnNamer",
    "methodSignature": "isAllowableColumnName(Ljava/lang/String;)Z",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "2ce39acb-6bda-430a-9680-5d45010eed9d",
    "startBlock": "1022440318",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageStore",
    "methodSignature": "addMeta(Lorg/h2/pagestore/db/PageIndex;Lorg/h2/engine/Session;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!IFEXISTS && !DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "e383ab04-d447-4826-b445-af5f6356881c",
    "startBlock": "145104717",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command.dml",
    "className": "Select",
    "methodSignature": "queryFlat(ILorg/h2/result/ResultTarget;JJZZ)Lorg/h2/result/LazyResult;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "020de025-863a-46f8-b32f-aff60e8eb7b5",
    "startBlock": "300718153",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "readTableFilter()Lorg/h2/table/TableFilter;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "00056660-2c85-4385-ba89-cc162c155200",
    "startBlock": "1997641118",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "DataUtils",
    "methodSignature": "writeStringData(Ljava/nio/ByteBuffer;Ljava/lang/String;I)V",
    "decisionIndex": -1,
    "info": [
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!IFEXISTS && DEFRAG_ALWAYS && !FORBID_CREATION && !MV_STORE)",
      "((MV_STORE && (FORBID_CREATION || IFEXISTS)) || (!MV_STORE && (IFEXISTS || !DEFRAG_ALWAYS || FORBID_CREATION)))",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && DEFRAG_ALWAYS && !AUTOCOMMIT)"
    ],
    "id": "5d1bbea9-1b14-4159-869f-4c6b61ff5413",
    "startBlock": "961149730",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.value",
    "className": "ValueTimestamp",
    "methodSignature": "convertScale(ZI)Lorg/h2/value/Value;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)"
    ],
    "id": "62a6ee0c-3993-40f5-b09a-fbf221b7c826",
    "startBlock": "2092245151",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.command",
    "className": "Parser",
    "methodSignature": "parseSelect()Lorg/h2/command/dml/Select;",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "d4951fa7-c6c6-456d-8f56-1eab4eb5bdaf",
    "startBlock": "566066815",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.jdbc",
    "className": "JdbcPreparedStatement",
    "methodSignature": "setBigDecimal(ILjava/math/BigDecimal;)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!IFEXISTS && AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && AUTOCOMMIT)",
      "(FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && IFEXISTS)",
      "(!IFEXISTS && !AUTOCOMMIT && !FORBID_CREATION && MV_STORE)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && IFEXISTS)"
    ],
    "id": "65c9fd20-a3ee-47ba-b672-f31f57508bda",
    "startBlock": "53108431",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.pagestore",
    "className": "PageLog",
    "methodSignature": "commit(I)V",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(FORBID_CREATION || IFEXISTS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !AUTOCOMMIT)"
    ],
    "id": "61cc8e14-167f-414e-9a7c-e9c7aef70063",
    "startBlock": "200743800",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.table",
    "className": "Table",
    "methodSignature": "getColumn(Ljava/lang/String;Z)Lorg/h2/table/Column;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "22e4d60a-ac49-42f3-bb9e-80e3be12faa2",
    "startBlock": "68352989",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.util",
    "className": "Permutations",
    "methodSignature": "moveIndex()V",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION || IFEXISTS)"
    ],
    "id": "0060643b-84e5-4b5c-9c29-485f39dbd18f",
    "startBlock": "724256528",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.mvstore",
    "className": "RootReference",
    "methodSignature": "<init>(Lorg/h2/mvstore/RootReference;I)V",
    "decisionIndex": 1,
    "info": [
      "(FORBID_CREATION || IFEXISTS || !MV_STORE)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && !AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && AUTOCOMMIT)",
      "(!FORBID_CREATION && COMPRESS && !IFEXISTS && MV_STORE && !AUTOCOMMIT)",
      "(!FORBID_CREATION && !IFEXISTS && MV_STORE && !COMPRESS && AUTOCOMMIT)"
    ],
    "id": "5f9cb227-da54-4fc2-bb93-f95d378f887f",
    "startBlock": "1233554936",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.result",
    "className": "SimpleRow",
    "methodSignature": "getMemory()I",
    "decisionIndex": 1,
    "info": [
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !PAGE_SIZE && !CACHE_SIZE && !FORBID_CREATION && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && FILE_LOCK && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && !AUTOCOMMIT)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !DEFRAG_ALWAYS && !IFEXISTS && !FILE_LOCK)",
      "(!MV_STORE && !PAGE_SIZE && !FORBID_CREATION && !IFEXISTS && CACHE_SIZE && FILE_LOCK && DEFRAG_ALWAYS)",
      "(FORBID_CREATION || IFEXISTS || MV_STORE)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && DEFRAG_ALWAYS && AUTOCOMMIT)",
      "(!MV_STORE && !CACHE_SIZE && !FORBID_CREATION && PAGE_SIZE && !IFEXISTS && !FILE_LOCK && DEFRAG_ALWAYS && !AUTOCOMMIT)",
      "(!MV_STORE && !FORBID_CREATION && PAGE_SIZE && !DEFRAG_ALWAYS && !IFEXISTS && CACHE_SIZE && !FILE_LOCK && AUTOCOMMIT)"
    ],
    "id": "26fc6344-107e-4096-8f8a-263aeeba7aa4",
    "startBlock": "912556872",
    "endBlocks": [
      "exit"
    ]
  },
  {
    "packageName": "org.h2.message",
    "className": "DbException",
    "methodSignature": "getJdbcSQLException(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;ILjava/lang/Throwable;Ljava/lang/String;)Ljava/sql/SQLException;",
    "decisionIndex": 1,
    "info": [
      "(!FORBID_CREATION && !IFEXISTS)",
      "(FORBID_CREATION && !IFEXISTS)",
      "IFEXISTS"
    ],
    "id": "e5586bda-9036-4372-87ac-2c832bef2ca2",
    "startBlock": "1475584500",
    "endBlocks": [
      "exit"
    ]
  }
]